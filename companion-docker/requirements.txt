# RunMyCampus Companion (Docker) — v3.37.0 Agent 4 requirements.
# Strictly the platform-handshake + canonical-CSV ingest stack.
# No selenium / playwright / httpx-cookie-persistence / SIS scraping libs.
fastapi>=0.110,<0.115
uvicorn[standard]>=0.27,<0.31
httpx>=0.27,<0.28
pynacl>=1.5,<2.0
python-multipart>=0.0.9,<0.1
