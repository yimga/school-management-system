; pytest config for companion-docker sibling tests.
; Standalone: no Django, no pytest-django auto-DB.
[pytest]
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --tb=short
    -ra
    -p no:django
