/* Density on finance invoices is owned by platform rmc-data-table.js +
   rmc-table-intelligence.js (Compact / Cozy / Roomy). Legacy condensed class
   toggle removed — it never bound because the buttons sit outside the wrapper. */
(function () {
  "use strict";
})();
