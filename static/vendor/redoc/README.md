# Redoc standalone bundle

Run `python scripts/fetch_vendor_redoc.py` once on a machine with network access to populate
`redoc.standalone.js`. CI `verify_cdn_self_host_burndown.py` requires this file.
