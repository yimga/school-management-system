# Land-and-expand: legacy SIS read-only preview (BR-09)

## Product

**`/super/tools/legacy-csv-preview/`** — Superuser uploads **CSV export** from PowerSchool/IC/Skyward; platform renders **read-only** table (first 500 rows). No write-back.

**Sales use:** “See your roster in our UI before migration.”

## Lawful access

District must **own** the export; no credential stuffing. API connector phase = separate SKU.
