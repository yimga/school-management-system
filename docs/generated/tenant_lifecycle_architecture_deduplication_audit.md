# Tenant lifecycle architecture deduplication audit

Canonical provisioning: `apps/schools/tasks.py`. Canonical progress: `apps/schools/provisioning_progress.py`. Canonical operational FSM: `apps/lifecycle/unified_lifecycle.py`.
