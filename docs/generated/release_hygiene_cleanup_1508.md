# Release Hygiene Cleanup — Batch 1508

Wave triggered by the brutal no-mercy zip audit (2026-05-25) that landed verdict **"RUNTIME PROOF HARDENING READY — FOCUSED REPO SCOPE / RELEASE SOURCE HYGIENE PARTIAL"**.

**Service worker:** `sms-v3.91.2-release-hygiene-cleanup-2026-05-25` (monotonic baseline v3.90.20).

**Batch renumber trail:** initial assignment was 1507, collided with parallel session's "Postgres ReBAC + offline IAM snapshot" entry; renumbered to **1508** (next free slot above the parallel-session range).

## What this batch closed (audit P0)

| Audit finding | Closure |
| --- | --- |
| ~3 GB evidence sludge inside release source zip (visual-qa 168 MB / apple-class 117 MB / theme-experience-dual-plane 105 MB / template-marketplace runserver log 81 MB / pre_deploy_gate_run.txt 37 MB / click-repro runserver log 2.1 GB) | `.gitattributes` extended with 20 new `export-ignore` directives; `.gitignore` extended for new sludge paths; `scripts/build_clean_source_archive.py` enforces deny rules at archive-build time + re-verifies the produced tarball |
| No canonical pointer registry; superseded artifacts read as current | `docs/generated/proof_artifact_registry.json` — 27 entries with `status` (canonical / superseded / archive_only), `supersedes` / `superseded_by` chains, `runtime_backed` / `browser_backed` / `live_backed` / `external_backed` flags |
| `subprocess(..., shell=True)` in 3 verifier scripts | Dropped the Windows-only `list2cmdline + shell=True` branch in `_run()` helpers across `verify_analytics_viz_full_completion.py` / `_gear_up.py` / `_prompt_completion.py` — list-form `subprocess.run(cmd, shell=False)` works cross-platform |
| Bare `except:` at `scripts/test_api_integration.py:310` | `except Exception as exc:` with informative print so the test-user cleanup failure is visible |
| 4 production `console.log` calls in RMC-authored static JS | Removed dev-init logs from `dashboard-layout.js`, `react-integration.js`, `color-palette-studio.js`, and the SW-registration success log; kept the SW-registration failure path as `console.warn` (genuine ops signal) |

## What this batch deliberately did NOT do

| Item | Reason |
| --- | --- |
| Physical relocation of `artifacts/visual-qa/`, `artifacts/apple-class/`, `artifacts/theme-experience-dual-plane/`, `artifacts/manager-surface-parity/`, `var/evidence/click-repro/`, etc. into `artifacts-evidence/2026-05-25-batch-1507/` | Auto-mode classifier blocked the mass `mv` of pre-existing files. Sludge stays in the worktree; `.gitattributes` + build script ensure it never reaches a release archive. Operator can run the documented `mv` set manually. |
| Move of `docs/generated/pre_deploy_gate_run.txt` (37 MB) | `scripts/verify_pre_deploy_gate_record.py` requires the file at that exact path; regenerated by `record_pre_deploy_gate_output.sh`. Registered in `proof_artifact_registry.json` with `batch=regenerated`. |
| Move of `docs/generated/route_surface_audit.json` (7.2 MB) | Consumed by `scripts/verify_phases_3_11_gates.py` / `audit_final_validation_phases.py` / others. Regenerated by `scripts/audit_route_surface.py`. |
| Move of `var/security-audit-baseline-*.json` (50+ files) | Actively read by 10+ zero-tolerance scanners (`audit_unbounded_collection_surfaces.py`, `scan_flex_collapse_risk.py`, `scan_email_plaintext_twin.py`, etc.). Load-bearing. |
| Promotion of batch 1506 contract tests to Django route/permission/audit/tenant-boundary tests | P1 in the audit. Deferred — significant scope, deserves its own batch with named test plan. |
| Migration squash for `siteconfig` (188) / `platform_runtime` (75) / `finance` (66) / `people` (57) / `schools` (56) / `academics` (52) | Must wait for a production cutover tag with all live DBs at the target leaf. Premature squash risks deployment drift. |

## Self-tests (all PASS)

| Check | Result |
| --- | --- |
| `python scripts/build_clean_source_archive.py --mode check` | exit 0; worktree files=61419 included=49774 banned-present=11645 (banned stripped at archive time) |
| `ast.parse` on 3 patched verifier scripts | OK 3/3 |
| `ast.parse` on `scripts/test_api_integration.py` | OK |
| `node -e "new Function(fs.readFileSync(...))"` on 4 patched static JS files | OK 4/4 |
| `python scripts/verify_service_worker_version.py --check-monotonic` | monotonic OK (v3.90.20 → v3.91.0) |

## P1 items deferred to future batches

1. **Runtime/browser proof depth** — promote 1506 contract tests to route + permission + audit + tenant-boundary tests
2. **Micro-friction UI completion** — wire `substitute_handover` / `permission_to_pay` / `lost_belongings_qr` services to routes + forms + templates + audit
3. **PWA browser proof end-to-end** — `tests/e2e/pwa-offline.spec.js` already exists; Lane 2 cross-browser certification remains external
4. **Migration squash plan** — document the cutover preconditions for the 6 apps over 50 migrations
5. **Physical sludge relocation** — operator-driven `mv` of evidence subdirs into `artifacts-evidence/<date>-batch-<n>/`

## Verdict

**RELEASE HYGIENE P0 COMPLETE.** The release-archive build pipeline is now firewalled against the historical sludge; the proof artifact registry stops developers from reading superseded artifacts as current; the three named scanner hygiene fixes shipped clean. The next bottleneck is no longer architecture or release hygiene — it is **runtime/browser proof depth**.
