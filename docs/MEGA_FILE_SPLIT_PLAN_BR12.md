# Mega-file split plan (BR-12)

**Until `CODEX_STRICT=1` passes:** explicit waiver per file.

| File | Approx LOC | Status | Next split |
|------|------------|--------|------------|
| `apps/schools/super_views.py` | Large | **Partial** | New routes → `super_views_beyond_reach.py` (done for BR-04/07/09); **`build_command_center_data`** → `super_views_command_center_data.py`; **`CONTROL_PLANE_*`** tuples → `super_views_constants.py`; **geo / provinces / education profiles / system blueprint / plans configurator JSON** → **`super_views_geo_api.py`**; **per-school timeline / approve / lifecycle / policy bundles** → **`super_views_school_api.py`**; **policy diff / impact preview / sandbox bundle apply** → **`super_views_policy.py`**; **trust center / compliance / audit export / platform events / config redirect** → **`super_views_trust_surface.py`**; **global support dashboard / queue fragment / assign** → **`super_views_support.py`**; **AI model hub / global version / progress** → **`super_views_ai.py`**; **impersonation / switch-to-tenant** → **`super_views_impersonation.py`**; **runtime inspector / workflow simulator** → **`super_views_runtime_ops.py`**; **usage / pulse / tenant health / tenant 360 / control health hub** → **`super_views_platform_monitoring.py`**; **billing dashboard** → **`super_views_billing_console.py`**; **command center** → **`super_views_command_center_views.py`**; **schools list + analytics overview** → **`super_views_overview_surfaces.py`**; **super dashboard v1/v2 + layout API** → **`super_views_dashboard_surfaces.py`**; **dashboard/export shared helpers** (`parse_month_param`, `safe_registry_url`, …) → **`super_views_dashboard_helpers.py`**; **CSV/PDF exports** → **`super_views_exports.py`**; **`safe_platform_incidents_url`** / timeline URLs in **`super_views_helpers`** (re-exported from `super_views` where applicable); tests **`test_super_views_geo_api.py`** … **`test_super_views_billing_console.py`**, **`test_super_views_command_center_views.py`**, **`test_super_views_overview_surfaces.py`**, **`test_super_views_dashboard_surfaces.py`**, **`test_super_views_exports.py`** |
| `apps/schools/marketing_views.py` | ~3k | **Partial** | **Done:** `MARKETING_PAGE_DEFINITIONS`, `MARKETING_PAGE_EXTRAS`, `TOPICAL_LANDING_DEFINITIONS` → `marketing_page_definitions.py` (views keep loaders + `_marketing_context`) |
| Other hits | per `lint_mega_files.py` | Run with `CODEX_STRICT=1` in CI when ready | Extract catalog/migration already split |

**Waiver rule:** Any file over threshold without split must have **row here** + **owner** + **target quarter**.

**Template edits:** Use a single tree — [TEMPLATE_EDITING_CONVENTION.md](TEMPLATE_EDITING_CONVENTION.md) (forward slashes under `templates/`).
