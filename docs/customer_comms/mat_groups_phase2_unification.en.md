# mat_groups user — Phase 2 unification notice (EN)

Subject: MAT groups unified under the new Organization layer

Hi {{ tenant_admin_first_name }},

The `mat_groups` registry you use is being unified under the new `Organization` model. This gives you:

- The same MAT group hub you already use, now backed by a first-class organization row.
- Configurable per-domain inheritance (curriculum, fees, HR, EMIS, branding).
- Optional consolidated billing across member schools.

**What changes**

- URLs and console pages stay the same.
- Existing roles map automatically.

**Effective date**

The unification takes effect on {{ unification_effective_date }}. No action is required on your side. If you would like to delay, reply to this email within 14 days of the effective date.

— The RunMyCampus team
