# Country Readiness Matrix

> **Generated** from `apps/finance/country_readiness_register.py` (`python scripts/generate_country_readiness_matrix.py`). Do not hand-edit — it is a projection over the payment-profile, PSP-adapter, and local-experience SOT registers. The operator dashboard at `/super/.../country-readiness/` renders the same data.

## The headline

- **250 ISO countries**, but only **15 have a researched payment corridor** (real currency + dominant rails). The other **235 are placeholder stubs** — defining a corridor is *data* work, distinct from the *adapter* work below.
- **0 payment rails are LIVE.** A rail is only **config-only** (tenant just adds an API key) once its adapter is `live`. Until then a defined corridor is **config-blocked** (the platform must finish/certify the rail) — manual/offline receipts still work everywhere.
- Leverage / localization / risk figures below are computed over the **defined corridors only** — counting stubs would inflate every number (the "one card PSP covers 240 countries" artifact is really 200 stubs sharing a fake USD currency).

### Readiness distribution

| Tier | Countries | Meaning |
|---|---:|---|
| 🟢 `config_only` | 0 | Config-only (tenant self-serve) |
| 🟡 `adapter_in_progress` | 14 | Config-blocked (platform must finish the rail) |
| 🟠 `manual_only` | 1 | Manual-only today (live rail is a platform build) |
| 🔴 `platform_build` | 0 | Platform build required (no adapter) |
| ⚪ `corridor_undefined` | 235 | Corridor undefined (placeholder data — research first) |

### Highest-leverage adapters

Among the **defined** corridors, countries unblocked if this single adapter is promoted to `live`. This is the adapter build order. Note the two distinct workstreams: **(1) define the remaining corridors** (data — research currency + rails per market) and **(2) promote these adapters to live** (engineering). A country needs both before it becomes pure tenant config:

| Adapter | Countries unblocked |
|---|---:|
| Stripe | 7 |
| Orange Money | 2 |
| MTN MoMo | 2 |
| Pesapal | 2 |
| Mercado Pago | 1 |

### Localization

- 🟢 Localized: **9** · 🟡 Partial: **4** · ⚪ English-fallback: **2** _(of the defined corridors)_

### Risk tier (defined corridors)

**medium**: 9 · **high**: 6

## Per-country matrix

> Rows tagged **stub** carry placeholder currency/rails (corridor not yet researched). Currencies marked **0d** are zero-decimal (charge whole units, not cents).

| Country | Cur | Rails | Readiness | Risk | Tenant action | Platform action | Locale |
|---|---|---|---|---|---|---|---|
| Andorra **stub** (`AD`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the AD corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| United Arab Emirates **stub** (`AE`) | AED | CARD BANK | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the AE corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | localized |
| Afghanistan **stub** (`AF`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the AF corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Antigua and Barbuda **stub** (`AG`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the AG corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Anguilla **stub** (`AI`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the AI corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Albania **stub** (`AL`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the AL corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Armenia **stub** (`AM`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the AM corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Angola **stub** (`AO`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the AO corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Antarctica **stub** (`AQ`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the AQ corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Argentina **stub** (`AR`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the AR corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| American Samoa **stub** (`AS`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the AS corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Austria **stub** (`AT`) | EUR | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the AT corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Australia **stub** (`AU`) | AUD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the AU corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | localized |
| Aruba **stub** (`AW`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the AW corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Åland Islands **stub** (`AX`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the AX corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Azerbaijan **stub** (`AZ`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the AZ corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Bosnia and Herzegovina **stub** (`BA`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the BA corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Barbados **stub** (`BB`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the BB corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Bangladesh **stub** (`BD`) | BDT | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the BD corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | partial |
| Belgium **stub** (`BE`) | EUR | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the BE corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Burkina Faso **stub** (`BF`) | XOF **0d** | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the BF corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Bulgaria **stub** (`BG`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the BG corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Bahrain **stub** (`BH`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the BH corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Burundi **stub** (`BI`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the BI corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Benin **stub** (`BJ`) | XOF **0d** | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the BJ corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Saint Barthélemy **stub** (`BL`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the BL corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Bermuda **stub** (`BM`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the BM corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Brunei Darussalam **stub** (`BN`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the BN corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Bolivia, Plurinational State of **stub** (`BO`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the BO corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Bonaire, Sint Eustatius and Saba **stub** (`BQ`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the BQ corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Brazil (`BR`) | BRL | PIX CARD BANK | 🟡 adapter_in_progress | high | Use manual / offline receipts until the live rail ships. | Promote the Mercado Pago adapter (in_progress) to live (request-to-pay → webhook → reconcile → split). | partial |
| Bahamas **stub** (`BS`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the BS corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Bhutan **stub** (`BT`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the BT corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Bouvet Island **stub** (`BV`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the BV corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Botswana **stub** (`BW`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the BW corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Belarus **stub** (`BY`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the BY corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Belize **stub** (`BZ`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the BZ corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Canada **stub** (`CA`) | CAD | CARD INSTANT_BANK | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the CA corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | localized |
| Cocos (Keeling) Islands **stub** (`CC`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the CC corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| DR Congo (`CD`) | CDF | ORANGE_MOMO MTN_MOMO | 🟠 manual_only | medium | Configure manual payment instructions (bank / MoMo number); reconcile receipts. | Build a Orange Money / MTN MoMo adapter to enable live collection. | english_fallback |
| Central African Republic **stub** (`CF`) | XAF **0d** | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the CF corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Congo **stub** (`CG`) | XAF **0d** | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the CG corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Switzerland **stub** (`CH`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the CH corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Côte d'Ivoire (`CI`) | XOF **0d** | ORANGE_MOMO MTN_MOMO | 🟡 adapter_in_progress | medium | Use manual / offline receipts until the live rail ships. | Promote the Orange Money adapter (in_progress) to live (request-to-pay → webhook → reconcile → split). | localized |
| Cook Islands **stub** (`CK`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the CK corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Chile **stub** (`CL`) | CLP | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the CL corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | localized |
| Cameroon (`CM`) | XAF **0d** | MTN_MOMO ORANGE_MOMO BANK | 🟡 adapter_in_progress | high | Use manual / offline receipts until the live rail ships. | Promote the MTN MoMo adapter (in_progress) to live (request-to-pay → webhook → reconcile → split). | localized |
| China **stub** (`CN`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the CN corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | partial |
| Colombia **stub** (`CO`) | COP | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the CO corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | localized |
| Costa Rica **stub** (`CR`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the CR corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Cuba **stub** (`CU`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the CU corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Cabo Verde **stub** (`CV`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the CV corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Curaçao **stub** (`CW`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the CW corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Christmas Island **stub** (`CX`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the CX corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Cyprus **stub** (`CY`) | EUR | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the CY corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Czechia **stub** (`CZ`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the CZ corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Germany **stub** (`DE`) | EUR | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the DE corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | localized |
| Djibouti **stub** (`DJ`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the DJ corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Denmark **stub** (`DK`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the DK corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Dominica **stub** (`DM`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the DM corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Dominican Republic **stub** (`DO`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the DO corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Algeria **stub** (`DZ`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the DZ corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Ecuador **stub** (`EC`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the EC corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Estonia **stub** (`EE`) | EUR | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the EE corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Egypt **stub** (`EG`) | EGP | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the EG corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | localized |
| Western Sahara **stub** (`EH`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the EH corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Eritrea **stub** (`ER`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the ER corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Spain **stub** (`ES`) | EUR | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the ES corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | localized |
| Ethiopia **stub** (`ET`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the ET corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | partial |
| European Union (SEPA) (`EU`) | EUR | CARD BANK | 🟡 adapter_in_progress | medium | Use manual / offline receipts until the live rail ships. | Promote the Stripe adapter (in_progress) to live (request-to-pay → webhook → reconcile → split). | english_fallback |
| Finland **stub** (`FI`) | EUR | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the FI corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Fiji **stub** (`FJ`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the FJ corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Falkland Islands (Malvinas) **stub** (`FK`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the FK corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Micronesia, Federated States of **stub** (`FM`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the FM corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Faroe Islands **stub** (`FO`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the FO corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| France **stub** (`FR`) | EUR | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the FR corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | localized |
| Gabon **stub** (`GA`) | XAF **0d** | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the GA corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| United Kingdom (`GB`) | GBP | CARD BANK | 🟡 adapter_in_progress | medium | Use manual / offline receipts until the live rail ships. | Promote the Stripe adapter (in_progress) to live (request-to-pay → webhook → reconcile → split). | localized |
| Grenada **stub** (`GD`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the GD corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Georgia **stub** (`GE`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the GE corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| French Guiana **stub** (`GF`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the GF corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Guernsey **stub** (`GG`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the GG corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Ghana (`GH`) | GHS | BANK CARD | 🟡 adapter_in_progress | high | Use manual / offline receipts until the live rail ships. | Promote the Stripe adapter (in_progress) to live (request-to-pay → webhook → reconcile → split). | localized |
| Gibraltar **stub** (`GI`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the GI corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Greenland **stub** (`GL`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the GL corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Gambia **stub** (`GM`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the GM corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Guinea **stub** (`GN`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the GN corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Guadeloupe **stub** (`GP`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the GP corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Equatorial Guinea **stub** (`GQ`) | XAF **0d** | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the GQ corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Greece **stub** (`GR`) | EUR | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the GR corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| South Georgia and the South Sandwich Islands **stub** (`GS`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the GS corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Guatemala **stub** (`GT`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the GT corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Guam **stub** (`GU`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the GU corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Guinea-Bissau **stub** (`GW`) | XOF **0d** | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the GW corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Guyana **stub** (`GY`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the GY corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Hong Kong **stub** (`HK`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the HK corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | partial |
| Heard Island and McDonald Islands **stub** (`HM`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the HM corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Honduras **stub** (`HN`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the HN corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Croatia **stub** (`HR`) | EUR | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the HR corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Haiti **stub** (`HT`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the HT corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Hungary **stub** (`HU`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the HU corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Indonesia **stub** (`ID`) | IDR | WALLET BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the ID corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | partial |
| Ireland **stub** (`IE`) | EUR | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the IE corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | partial |
| Israel **stub** (`IL`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the IL corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Isle of Man **stub** (`IM`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the IM corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| India **stub** (`IN`) | INR | UPI BANK CARD | ⚪ corridor_undefined | high | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the IN corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | partial |
| British Indian Ocean Territory **stub** (`IO`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the IO corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Iraq **stub** (`IQ`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the IQ corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Iran, Islamic Republic of **stub** (`IR`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the IR corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Iceland **stub** (`IS`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the IS corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Italy **stub** (`IT`) | EUR | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the IT corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Jersey **stub** (`JE`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the JE corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Jamaica **stub** (`JM`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the JM corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Jordan **stub** (`JO`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the JO corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Japan **stub** (`JP`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the JP corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | localized |
| Kenya (`KE`) | KES | CARD BANK | 🟡 adapter_in_progress | high | Use manual / offline receipts until the live rail ships. | Promote the Stripe adapter (in_progress) to live (request-to-pay → webhook → reconcile → split). | localized |
| Kyrgyzstan **stub** (`KG`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the KG corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Cambodia **stub** (`KH`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the KH corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Kiribati **stub** (`KI`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the KI corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Comoros **stub** (`KM`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the KM corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Saint Kitts and Nevis **stub** (`KN`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the KN corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Korea, Democratic People's Republic of **stub** (`KP`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the KP corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Korea, Republic of **stub** (`KR`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the KR corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | partial |
| Kuwait **stub** (`KW`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the KW corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Cayman Islands **stub** (`KY`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the KY corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Kazakhstan **stub** (`KZ`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the KZ corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Lao People's Democratic Republic **stub** (`LA`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the LA corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Lebanon **stub** (`LB`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the LB corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Saint Lucia **stub** (`LC`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the LC corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Liechtenstein **stub** (`LI`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the LI corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Sri Lanka **stub** (`LK`) | LKR | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the LK corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | partial |
| Liberia **stub** (`LR`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the LR corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Lesotho **stub** (`LS`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the LS corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Lithuania **stub** (`LT`) | EUR | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the LT corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Luxembourg **stub** (`LU`) | EUR | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the LU corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Latvia **stub** (`LV`) | EUR | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the LV corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Libya **stub** (`LY`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the LY corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Morocco **stub** (`MA`) | MAD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the MA corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | localized |
| Monaco **stub** (`MC`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the MC corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Moldova, Republic of **stub** (`MD`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the MD corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Montenegro **stub** (`ME`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the ME corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Saint Martin (French part) **stub** (`MF`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the MF corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Madagascar **stub** (`MG`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the MG corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Marshall Islands **stub** (`MH`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the MH corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| North Macedonia **stub** (`MK`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the MK corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Mali **stub** (`ML`) | XOF **0d** | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the ML corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Myanmar **stub** (`MM`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the MM corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Mongolia **stub** (`MN`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the MN corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Macao **stub** (`MO`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the MO corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Northern Mariana Islands **stub** (`MP`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the MP corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Martinique **stub** (`MQ`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the MQ corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Mauritania **stub** (`MR`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the MR corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Montserrat **stub** (`MS`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the MS corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Malta **stub** (`MT`) | EUR | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the MT corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Mauritius **stub** (`MU`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the MU corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Maldives **stub** (`MV`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the MV corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Malawi **stub** (`MW`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the MW corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Mexico **stub** (`MX`) | MXN | INSTANT_BANK CARD VOUCHER | ⚪ corridor_undefined | high | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the MX corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | localized |
| Malaysia **stub** (`MY`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the MY corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | partial |
| Mozambique **stub** (`MZ`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the MZ corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Namibia **stub** (`NA`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the NA corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| New Caledonia **stub** (`NC`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the NC corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Niger **stub** (`NE`) | XOF **0d** | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the NE corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Norfolk Island **stub** (`NF`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the NF corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Nigeria (`NG`) | NGN | BANK CARD | 🟡 adapter_in_progress | high | Use manual / offline receipts until the live rail ships. | Promote the Stripe adapter (in_progress) to live (request-to-pay → webhook → reconcile → split). | localized |
| Nicaragua **stub** (`NI`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the NI corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Netherlands **stub** (`NL`) | EUR | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the NL corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | partial |
| Norway **stub** (`NO`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the NO corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Nepal **stub** (`NP`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the NP corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | partial |
| Nauru **stub** (`NR`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the NR corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Niue **stub** (`NU`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the NU corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| New Zealand **stub** (`NZ`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the NZ corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | partial |
| Oman **stub** (`OM`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the OM corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Panama **stub** (`PA`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the PA corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Peru **stub** (`PE`) | PEN | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the PE corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | localized |
| French Polynesia **stub** (`PF`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the PF corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Papua New Guinea **stub** (`PG`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the PG corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Philippines **stub** (`PH`) | PHP | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the PH corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | partial |
| Pakistan **stub** (`PK`) | PKR | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the PK corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | localized |
| Poland **stub** (`PL`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the PL corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Saint Pierre and Miquelon **stub** (`PM`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the PM corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Pitcairn **stub** (`PN`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the PN corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Puerto Rico **stub** (`PR`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the PR corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Palestine, State of **stub** (`PS`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the PS corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Portugal **stub** (`PT`) | EUR | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the PT corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | partial |
| Palau **stub** (`PW`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the PW corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Paraguay **stub** (`PY`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the PY corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Qatar **stub** (`QA`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the QA corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | localized |
| Réunion **stub** (`RE`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the RE corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Romania **stub** (`RO`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the RO corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Serbia **stub** (`RS`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the RS corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Russian Federation **stub** (`RU`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the RU corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Rwanda (`RW`) | RWF | MTN_MOMO CARD | 🟡 adapter_in_progress | medium | Use manual / offline receipts until the live rail ships. | Promote the Pesapal adapter (in_progress) to live (request-to-pay → webhook → reconcile → split). | partial |
| Saudi Arabia **stub** (`SA`) | SAR | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the SA corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | localized |
| Solomon Islands **stub** (`SB`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the SB corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Seychelles **stub** (`SC`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the SC corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Sudan **stub** (`SD`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the SD corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Sweden **stub** (`SE`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the SE corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Singapore **stub** (`SG`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the SG corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | partial |
| Saint Helena, Ascension and Tristan da Cunha **stub** (`SH`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the SH corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Slovenia **stub** (`SI`) | EUR | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the SI corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Svalbard and Jan Mayen **stub** (`SJ`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the SJ corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Slovakia **stub** (`SK`) | EUR | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the SK corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Sierra Leone **stub** (`SL`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the SL corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| San Marino **stub** (`SM`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the SM corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Senegal (`SN`) | XOF **0d** | ORANGE_MOMO MTN_MOMO | 🟡 adapter_in_progress | medium | Use manual / offline receipts until the live rail ships. | Promote the Orange Money adapter (in_progress) to live (request-to-pay → webhook → reconcile → split). | partial |
| Somalia **stub** (`SO`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the SO corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Suriname **stub** (`SR`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the SR corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| South Sudan **stub** (`SS`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the SS corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Sao Tome and Principe **stub** (`ST`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the ST corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| El Salvador **stub** (`SV`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the SV corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Sint Maarten (Dutch part) **stub** (`SX`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the SX corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Syrian Arab Republic **stub** (`SY`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the SY corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Eswatini **stub** (`SZ`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the SZ corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Turks and Caicos Islands **stub** (`TC`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the TC corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Chad **stub** (`TD`) | XAF **0d** | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the TD corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| French Southern Territories **stub** (`TF`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the TF corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Togo **stub** (`TG`) | XOF **0d** | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the TG corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Thailand **stub** (`TH`) | THB | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the TH corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | partial |
| Tajikistan **stub** (`TJ`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the TJ corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Tokelau **stub** (`TK`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the TK corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Timor-Leste **stub** (`TL`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the TL corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Turkmenistan **stub** (`TM`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the TM corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Tunisia **stub** (`TN`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the TN corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Tonga **stub** (`TO`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the TO corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Türkiye **stub** (`TR`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the TR corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | localized |
| Trinidad and Tobago **stub** (`TT`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the TT corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Tuvalu **stub** (`TV`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the TV corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Taiwan, Province of China **stub** (`TW`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the TW corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Tanzania (`TZ`) | TZS | MTN_MOMO CARD | 🟡 adapter_in_progress | medium | Use manual / offline receipts until the live rail ships. | Promote the Pesapal adapter (in_progress) to live (request-to-pay → webhook → reconcile → split). | localized |
| Ukraine **stub** (`UA`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the UA corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Uganda (`UG`) | UGX | MTN_MOMO CARD | 🟡 adapter_in_progress | medium | Use manual / offline receipts until the live rail ships. | Promote the MTN MoMo adapter (in_progress) to live (request-to-pay → webhook → reconcile → split). | localized |
| United States Minor Outlying Islands **stub** (`UM`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the UM corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| United States (`US`) | USD | CARD BANK | 🟡 adapter_in_progress | medium | Use manual / offline receipts until the live rail ships. | Promote the Stripe adapter (in_progress) to live (request-to-pay → webhook → reconcile → split). | localized |
| Uruguay **stub** (`UY`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the UY corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Uzbekistan **stub** (`UZ`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the UZ corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Holy See (Vatican City State) **stub** (`VA`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the VA corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Saint Vincent and the Grenadines **stub** (`VC`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the VC corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Venezuela, Bolivarian Republic of **stub** (`VE`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the VE corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Virgin Islands, British **stub** (`VG`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the VG corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Virgin Islands, U.S. **stub** (`VI`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the VI corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Viet Nam **stub** (`VN`) | VND **0d** | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the VN corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | partial |
| Vanuatu **stub** (`VU`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the VU corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Wallis and Futuna **stub** (`WF`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the WF corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Samoa **stub** (`WS`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the WS corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Yemen **stub** (`YE`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the YE corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Mayotte **stub** (`YT`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the YT corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| South Africa (`ZA`) | ZAR | CARD BANK | 🟡 adapter_in_progress | high | Use manual / offline receipts until the live rail ships. | Promote the Stripe adapter (in_progress) to live (request-to-pay → webhook → reconcile → split). | partial |
| Zambia **stub** (`ZM`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the ZM corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |
| Zimbabwe **stub** (`ZW`) | USD | BANK CARD | ⚪ corridor_undefined | medium | Manual / offline receipts work today; live rail awaits a defined corridor. | Research the ZW corridor (real currency, dominant rails, PSP) — this is data work, not an adapter build. | english_fallback |

