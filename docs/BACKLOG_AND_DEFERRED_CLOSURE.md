# Backlog and Deferred Closure

**Purpose:** ?9 and ?12 of [RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH.md](RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH.md). Every unchecked or deferred item from the plan is explicitly assigned a status so nothing is "save for later" without closure. Single reference for audit.

**Rule:** Each item is exactly one of: **DONE** | **PARTIAL** | **NOT DONE** | **BLOCKED** | **DEPRECATED**. No open loop.

**Last reconciled:** This run (§2e row 6: events/tasks.py process_outbox_batch + orchestration/runners.py AdmissionsRunner run_step → typed except + log_exception_with_context; allowlist events/tasks 0; broad_exception_audit updated; manage.py check + lint_broad_except --strict + run_phase_h_verification pass). Previous: (Phase H: evals:teacher_dashboard in phase_h_audit + PhaseHUrlReverseTests + TENANT_CRITICAL_PATHS /evals/teacher/; 49 tests + phase_h_audit static+live pass). Previous: (§2e row 6: orchestration/runners.py — execute/compensate/run_step/run_workflow_simulation → _ORCHESTRATION_RUN_ERRORS + _ORCHESTRATION_STEP_QUERY_ERRORS + log_exception_with_context; allowlist + broad_exception_audit §apps/orchestration updated; lint_broad_except --strict pass.). Previous: (§2e row 8: marketing hero headline + primary CTA use platform design tokens — static/css/marketing-home.css: hero typography var(--type-display), all .btn-primary var(--color-primary-500)/--color-primary-700, focus var(--focus-ring-color); CONTROL_PLANE §4 / MARKETING_FRONT_PLACEHOLDER § Design tokens; BACKLOG §5 updated.). Previous: (Phase H: analytics:dashboard + compliance:dashboard in phase_h_audit + PhaseHUrlReverseTests + TENANT_CRITICAL_PATHS /analytics/, /compliance/dashboard/; 48 tests + phase_h_audit static+live pass). Previous: (§6.14 Standardize page archetypes [x]: RUNMYCAMPUS §6.14 checkbox marked DONE with closure note — data-page-archetype on parent/*, portal, finance, analytics, compliance, people; docs_truth_ledger + §5 updated. No code change.). Previous: (§2e row 6: evals/validators.py — GradeValidator.validate_evaluation: 3 broad excepts → _EVAL_VALIDATION_DETECTION_ERRORS + log_exception_with_context(school_id, extra: evaluation_id, section); allowlist + broad_exception_audit updated; lint_broad_except --strict pass.). Previous: (§2e row 8: Page maturity — super_trust_center, super_compliance_overview, super_audit_export use studio_os page_header + data-page-archetype; trust_center_url in super_views for compliance/audit. Navigation and inner pages [x]: all marketing inner pages extend base_marketing; same header/footer. CONTROL_PLANE §5.1 + §4 updated; BACKLOG §2e row 8 updated.). Previous: (dashboard/role_home_engine.py: __future__ annotations fix; manage.py check + Phase H pass.). Previous: (§10.5 in-code: role_home_engine DECISION_ARCHITECTURE_CHECKLIST_DOC + docstring; MARKETING_FRONT_PLACEHOLDER §Design tokens; CONTROL_PLANE §4 Ultra high-end design-tokens bullet; BACKLOG NEXT_50 row 49 DONE 1 PARTIAL.). Previous: (§2e row 8: Page maturity checklist [x] — CONTROL_PLANE_AND_MARKETING_UX_OVERHAUL §5.1; console_domains_hub_control_plane uses studio_os page_header + super_dashboard_url; view context extended; rule for new pages. BACKLOG §2e row 8 + §5 updated.). Previous: (§2e row 8: Click compression [x]; Proper seeding [x]; BACKLOG §2e row 8 updated.). Previous: (§2e row 6: (§10.5.4 Trust product: super_trust_center + super_audit_export views added in schools/super_views.py; templates super_trust_center.html, super_audit_export.html; manage.py check pass; lint_broad_except --strict pass. Optionals: verification run; BACKLOG §5a + ledger updated.). Previous: (§2e row 7: dashboard/context.py — snapshot exception paths use log_exception_with_context(school_id, extra section/role_code); §2e row 8: CONTROL_PLANE_AND_MARKETING_UX_OVERHAUL — page_header in Technical refs + Page maturity checklist.). Previous: (§6.14 Standardize page archetypes: data-page-archetype on parent/* and portal/document_library_manage.html; SOT §6.14 updated.). Previous: (§2.4 reports/views.py: publish flow AuditLog + honor roll badge — 2 broad excepts with log_exception_with_context; allowlist + apps/reports/views.py 2; broad_exception_audit + §2e row 7. Verify plan §8.0.11 row **DONE**.). Previous: (§2.4 reports: adhoc_runner + bi_services broad except → typed _REPORT_RUN_ERRORS / _SCHEDULED_REPORT_RUN_ERRORS + log_exception_with_context; allowlist 2→0; services.py _region_display_context + DatabaseError; broad_exception_audit + §2e row 6 updated; lint_broad_except --strict pass). Previous: (§2.4 siteconfig/tasks send_welcome_email → log_exception_with_context; reports/services _region_display_context → typed except; lint_broad_except + phase_h_audit + 45 Phase H URL tests pass). Previous: (Plan–SOT sync: verify_and_add_sot_gaps — §8.0.11 row marked DONE; all "File to change" rows in verify plan complete. update_single_execution_sot already execution-complete. No code change.). Previous: §6.14 Portal: duplicate parent_dashboard/link_child/link_child_wizard/_whatsapp_invite_link removed from views.py; single source views_parent; SOT §6.14 updated). Previous: (siteconfig/views_console_domains: _build_console_domains_context → list[dict[str, Any]], _safe_reverse → Optional[str]; Phase H run_phase_h_verification.sh passed; 45 tests + phase_h_audit static+live). Previous: (§2.4 reports/bi_services ScheduledReportRunner.run_due_reports → log_exception_with_context; allowlist 1 + §2e row 7). Previous: (§2.4 portal mgmt commands: generate_kb_odt, import_docs_to_kb, seed_kb_articles — structured logging + bare except→Exception; Step 14 lint_secret_exposure pass; §8 CONTROL_PLANE_AND_MARKETING_UX_OVERHAUL §4 Proper seeding cross-ref to MARKETING_FRONT_PLACEHOLDER §2/§4; §2e rows 5–8 updated). Previous: (§6.14 Portal Phase 2 extended: views_parent now has child_digital_id, portal_stats, parent_attendance_discipline, parent_child_results; views.py re-exports updated; manage check pass). Previous: §6.14 portal Phase 2 verified (teacher/staff in views_teacher; re-exports only). Previous: (§6.14 portal Phase 2 complete: all remaining teacher/staff views moved to views_teacher.py — take_student_attendance, record_teacher_attendance, seating_chart_view, cahier_list/verify_list/visa/request_revisions, teacher_timetable, teacher_lesson_notes, teacher_lesson_plan_add_attachment, teacher_wellness, teacher_hr_status, teacher_disciplinary, teacher_training_log, discipline_incidents_list; views.py re-exports; SOT §6.14 + docs_truth_ledger + plans updated). Previous: (§2.4 structured logging: portal/views_ai_gateway.py — 20 AI gateway handlers → log_view_exception; §2e row 7 + §5a updated). Previous: (§6.14 Portal Phase 2: views_parent.py added; views_teacher Phase 2 partial). Previous: Phase H §8.0.6: admin base_site sidebar 288px→18rem + min-width:0; §2.4 row 7: siteconfig/tasks.py send_welcome_email + check_regional_ollama_health → log_exception_with_context). Previous: (§2.4 evals/notifications: structured logging in all 5 send paths; send_mail import fixed; allowlist + broad_exception_audit; §2e row 7 updated). Previous: CONTROL_PLANE_AND_MARKETING_UX_OVERHAUL: template audit §5, one shell/sidebar verified, marketing pillar/outcome premium styling; §2e row 8 PARTIAL). Previous: §2.4 metadata/lineage_api: broad except replaced with FieldCatalogEntry.DoesNotExist + _FIELD_LOOKUP_ERRORS; log_exception_with_context on non-DoesNotExist; allowlist + broad_exception_audit; plan completion checklists in update_single_execution_sot + verify_and_add_sot_gaps). Previous: (Cursor plans: update_single_execution_sot + verify_and_add_sot_gaps — "File to change" tables marked "all applied"; Phase H verification: run_phase_h_verification.sh passed; Studio OS mode rails made fluid per §8.0.6 — width: min(12.5rem, 100%); min-width: 0 in experience, automation, output, launch, control; 45 tests + phase_h_audit static+live pass). Previous: (§2.4 structured logging: observability/views full + portal/views admission-number → log_view_exception; §5a + §2e row 7). Previous: (Cursor plans: update_single_execution_sot_2b40b934.plan.md + verify_and_add_sot_gaps_b0529884.plan.md — Plan→SOT completion checklists added; items marked done. Phase H audit static+live + 54 tests pass). Previous: §6.14 portal: views_common.py + views_student.py; parent/teacher/student concerns Phase 1; RUNMYCAMPUS §6.14 [x] PARTIAL; docs_truth_ledger + §5 updated). Previous: §2.4 structured logging: observability/views.py — healthz, api_health, api_notifications*, api_activities, api_dashboard_charts → log_view_exception; 45 Phase H URL tests + phase_h_audit pass). Previous: §2.4 structured logging: apps.platform_runtime.tests.test_structured_logging added (9 tests, no DB); pre_deploy_gate TARGETED_HARDENING_TESTS includes test_structured_logging; phase_h_audit + lint_broad_except + check pass). Previous: Phase H: run_phase_h_verification.sh + PHASE_H_UX_VERIFICATION §7; RUNMYCAMPUS "Run full test suite" bullet updated; 45 tests + phase_h_audit static+live pass). Previous: §5.1 role/device preview [x]; management_commands_inventory §4. Previous: Phase H audit: skip-link check, --verbose, responsive CSS warnings; RUNMYCAMPUS Phase H automated verification text updated; 45 smoke/Phase H URL tests + phase_h_audit static+live pass). Previous: Phase H bounded console structured logging; phase_h_audit static+live + 45 Phase H URL + 4 siteconfig test_domain_ownership tests pass. Previous: §5.9 RUNMYCAMPUS: Move tenant behavior out of SiteSettings [x]; Remove Gilead defaults [x]; TOOLSET_REMEDIATION_STATUS §5.9 updated; test_preview_context + lint + check pass). Previous: RUNMYCAMPUS §6.13 accounts "Split giant views" [x] — views_workflow + views_migration, views_dashboard, views_onboarding, etc.; ledger/NEXT_50 step 16 DONE; §6.15 finance views_requests + duplicate removal already in §5). Previous: §12 runtime gate: RUNMYCAMPUS §12 "runtime is the only legal behavior engine" [x] aligned with BACKLOG §6.3 MET; Phase H: siteconfig:console_domains_hub in phase_h_audit.py + MANAGER_CRITICAL_PATHS + PhaseHUrlReverseTests test_siteconfig_console_domains_hub_resolves; PHASE_H_UX_VERIFICATION /siteconfig/console/; bounded console template a11y + fluid; phase_h_audit static+live + 45 Phase H URL tests pass). Previous: §2.4 structured logging: portal/tasks generate_ai_response_async, siteconfig/views_tag_manager tag save, siteconfig/views theme save redirect; phase_h_audit --live message improved; 45 Phase H URL tests pass; BACKLOG §2e row 7 updated). Previous: §5.3/§5.4 RUNMYCAMPUS checkboxes [x] ReportPack + dependency mapping, Document Library actions; studio_os test_preview_context → SimpleTestCase + mocks (7 tests, no DB); §2.4 structured logging studio_os/services get_studio_preview_context; BACKLOG §2e row 7). Previous: §6 alignment: NEXT_50 = 49 DONE, 1 PARTIAL; §6.3 Summary 11 of 11 gates MET; §6.4 and Executive summary ?2.1 DONE / Step 4 DONE; validation table and Next logical steps updated; manage.py check + phase_h_audit + lint_tenant_settings + Phase H URL tests pass). Previous: §2.4 structured logging: siteconfig/views theme_colors_page + dashboard_views update_accessibility_preferences; smoke + Phase H 44 pass). Previous: §6.15 Finance views_requests + duplicate removal; RUNMYCAMPUS §6.15 updated; §5 row added). Previous: §2.4 portal/views_kb: log_exception_with_context signature fix + missing import; siteconfig/dashboard_views update_theme_preference → log_exception_with_context; phase_h_audit + lint_broad_except pass. §2.1 completion gates: tenant behavior / SiteSettings safe / siteconfig not mega-domain marked **DONE** per domain_ownership §6 and §12; RUNMYCAMPUS §2.1 completion gate checkboxes [x]; dashboard admin_context widget failure path uses log_view_exception when request in context; Phase H static audit + PhaseHUrlReverseTests pass). Previous: §7 ecosystem seeding improvements: catalog_counts MARKETPLACE_MINIMUMS + satisfies_marketplace_minimums; test idempotency + helper; seed_first_party_apps --dry-run; refresh_marketplace_seed_targets validation + timestamp. §5 toolset remediation: TOOLSET_REMEDIATION_STATUS.md; §5.4 Document Library actions DONE; §5.6 Live Previews impact_summary + dependency_warnings via get_studio_preview_context; document upload embed preserved; RUNMYCAMPUS §5 implementation table; BACKLOG §1 ?5??6 PARTIAL). Previous: Phase B execution: ownership reassignment doc; shrink SiteSettings plan in SITECONFIG_OWNERSHIP_MIGRATION; bounded console at siteconfig:console_domains_hub + control plane nav + manager shell; /siteconfig/customizer/ redirect tenant+manager; LEGACY_PATH_INVENTORY; RUNMYCAMPUS Phase B [x]; BACKLOG §5 row + §1b + §6.4). Previous: plan file location verification. Previous: §2.4 broad except: marketplace/views embed, siteconfig/views_console_domains, portal/ai_provider → typed; allowlist +3 at 0; lint_broad_except --strict + manage.py check pass). Previous: §12 marketplace/packs gate MET; 11 of 11 ?12 gates MET. Verification: MARKETPLACE_SEED_TARGETS §5; test_marketplace_catalog_minimums + generate_platform_inventory --check in pre_deploy_gate; BACKLOG §6.3 + §6.4 + Summary updated. Previous: optionals/recommendations/suggestions all DONE; nothing deferred. **Optionals:** BACKLOG §2f status table every row DONE; RUNMYCAMPUS §11.1 all items DONE (Studios, Phase E, §12.1 record gate output); record_pre_deploy_gate_output.sh + RELEASE_CHECKLIST; refresh_marketplace_seed_targets.py; marketplace UX + ReportPack/DocumentPack; role-home/proof-rich. docs_truth_ledger: Optionals and recommendations (non-negotiable) DONE. §5 row "Complete implement all optionals (no deferred)" added. Previous: §12 docs truth + package engine MET; Step 4 + Step 6 + §8 + Optional 12; redirects; MARKETING_FRONT_PLACEHOLDER. Verification: manage.py check, lints — pass.

**For all agents:** This file is one of four canonical strategy/backlog docs. **Before starting any backlog item:** check [docs_truth_ledger.md](docs_truth_ledger.md) and [NEXT_50_EXECUTION_STEPS.md](NEXT_50_EXECUTION_STEPS.md) for current status to avoid duplicating work or conflicting with in-progress changes. Named plan: [RUNMYCAMPUS_11_10_NORTH_STAR_COMPLETION_PLAN.md](RUNMYCAMPUS_11_10_NORTH_STAR_COMPLETION_PLAN.md). Single execution source of truth: [RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH.md](RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH.md). Do not create new overlapping strategy or roadmap files.

**Step 47 (no overlapping roadmaps):** Roadmap and strategy updates go only to [RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH.md](RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH.md), this backlog, [docs_truth_ledger.md](docs_truth_ledger.md), and [NEXT_50_EXECUTION_STEPS.md](NEXT_50_EXECUTION_STEPS.md). Do not create new overlapping strategy or roadmap files.

---

## Where we stand (snapshot)

*Single reference for "where things stand" with the plan and backlog. Update when reconciling.*

| Area | Status | Notes |
|------|--------|--------|
| **RUNMYCAMPUS ?1 table (?1 below)** | 27 rows | Every unchecked item has status (DONE/PARTIAL/NOT DONE/BLOCKED) + closure note. No open loops. |
| **?2.1 SiteSettings / ownership** | **DONE** | Replace get_solo/load **DONE**. Delete legacy paths **DONE** for agreed scope; further BLOCKED on product. Move ownership **DONE** (behavioral). Completion gates **DONE**: tenant behavior no longer depends on giant singleton; SiteSettings only safe platform defaults; siteconfig not mega-domain (domain_ownership §6; §12 gates MET). See ?1b. |
| **?2.4 Security / raw SQL / broad except** | PARTIAL | **Raw SQL wrap DONE** (allowlist 10; repos + cache_utils only; nothing left behind). Broad except: schools (incl. models.py, marketing_views [8?typed], mgmt commands), Portal (incl. templatetags/portal_breadcrumb), Api, Finance, Accounts, siteconfig (context_processors, templatetags/feature_control, report_template_engine, workflow_engine), evals/performance_optimization, brand_experience/design_studio, studio_os/views ? all at allowlist 0. Remaining: allowlisted commands/tests per audit. Signature/replay: Billing/Finance + lead capture DONE; GraphQL + SCIM + Section8 LTI rate+audit done; SCIM/LTI signature/replay manual_review_required. |
| **?3 Architecture / runtime / metadata** | **DONE** | Lifecycle states + lineage API + graph UI DONE. api_tenant_maturity get_effective_site_settings DONE. Governor limits + feature toggles in operator UI DONE (step 24). Runtime universal in tenant flows DONE. Precedence doc + contract tests + inspector DONE. |
| **?4 Studio OS** | **DONE** | All five mode hubs (Experience, Automation, Output, Launch, Control) DONE; shell + rail + iframe switcher. Steps 26?35 DONE. Legacy URL retirement: admin/siteconfig/customizer/?Studio OS Experience redirect in place. Pack tooling deferred to product. |
| **?5??7 Toolset / marketplace / seed** | **DONE** | platform_inventory --format json DONE. MARKETPLACE_SEED_TARGETS: all catalog minimums met (27 first-party, 25+ blueprint, 30+ workflow, 21+ dashboard, 15+ policy). Step 36 DONE: marketplace UI shows catalog counts; Install to sandbox and Apply/Preview/Rollback in place. |
| **?8??12 Marketing / gates / docs** | PARTIAL / DONE | Page archetypes DONE. proof_hero + placeholder keys wired. Step 49 security review DONE (SECURITY_REVIEW_LOG.md). **?12 gates: 11 of 11 MET.** Evidence table DONE; marketplace/packs deeply productized MET (MARKETPLACE_SEED_TARGETS §5; test + generate_platform_inventory --check in CI). No 9.5 claim until release sign-off. |
| **NEXT_50 steps 1–50** | **49 DONE, 1 PARTIAL** | DONE: 1–5, 7–25, 26–36, 37–50. PARTIAL: 6 (BLOCKED on product). Step 4 DONE (behavioral ownership); Step 25 DONE (reconcile after each milestone). |
| **Step 40 (code hygiene)** | **DONE** | siteconfig re-exports noqa: F401; `ruff check apps --select F401,F841` passes. Step 40 complete. |
| **Next logical steps (no overlap)** | See ?2e, ?2f | Full checklist in ?2e; ?2f = ?2.4 remaining scope (allowlist-shrink, structured logging). Step 25 DONE (reconcile after each milestone). |

**Stock-taking at this time (as of this run):** Plan and backlog are aligned. NEXT_50 = **49 DONE, 1 PARTIAL** (step 4 DONE behavioral; step 6 PARTIAL BLOCKED on product). RUNMYCAMPUS ?1: 27 unchecked items, each with status and closure note. ?2.1 (ownership + completion gates) DONE; ?3, ?4, ?5??7, ?10 DONE; ?2.4 PARTIAL (allowlist-shrink incremental); ?8??12 PARTIAL/DONE; **11 of 11 ?12 gates MET.** Full detail in ?6 below.

---

## 1. RUNMYCAMPUS unchecked items ? status

Items that remain `[ ]` in the plan are closed with one of the statuses below.

| Plan section | Item | Status | Closure note |
|--------------|------|--------|--------------|
| ?2.1 | Move real ownership out of siteconfig | **DONE** | Behavioral ownership move complete. Tenant behavior resolved only via get_effective_site_settings (runtime-first: RuntimeDefaults then SiteSettings); no tenant get_solo/load (lint_tenant_settings pass). domain_ownership + bounded-context surfaces; RuntimeDefaults preferred when set. Schema moves remain incremental per SITECONFIG_OWNERSHIP_MIGRATION. §12 gates siteconfig materially decomposed + SiteSettings not tenant-behavior truth MET. |
| ?2.1 | Replace direct singleton reads with runtime resolvers | **DONE** | Tenant-facing complete: lint_tenant_settings --check-get-solo-only pass; no get_solo/load in tenant app code. get_solo only in siteconfig (definition), platform_runtime/helpers (internal), allowlisted management (backfill_runtime_defaults). All tests migrated to get_platform_site_settings_record(create=True) or get_effective_site_settings; no test file calls get_solo(). evals/caching?get_cached_site_settings(school=). |
| ?2.1 | Delete migrated legacy paths after replacement | **DONE** (redirects) | ensure_gilead_admin removed; customizer redirect in place. /siteconfig/workflow-hub/ and /siteconfig/report-library/ redirect to studio_os:automation and studio_os:output (LEGACY_PATH_INVENTORY REDIRECT). Further URL removal per product if different paths confirmed. |
| ?2.1 | Tenant behavior no longer depends on giant singleton | **DONE** | Behavioral gate MET: tenant behavior resolved only via get_effective_site_settings (runtime-first); no tenant get_solo (lint_tenant_settings pass). domain_ownership.md §6; §12 gate MET. Schema moves incremental. |
| ?2.1 | SiteSettings only safe platform defaults | **DONE** | Behavioral gate MET: SiteSettings is legacy data source only; tenant-behavior truth = resolver output. §12 gate "SiteSettings not tenant-behavior truth" MET. Safe platform-default maintenance per SITECONFIG_OWNERSHIP_MIGRATION. |
| ?2.1 | siteconfig not mega-domain | **DONE** | Behavioral gate MET: domain_ownership + bounded-context surfaces; no tenant get_solo; siteconfig materially decomposed per §12. domain_ownership.md §6. Schema moves incremental. |
| ?2.4 | Stronger signature/replay where manual_review_required | **DONE** | Rate limit + audit logging implemented for GraphQL, SCIM, Section8 LTI (no PII). Billing/Finance/lead capture done. Signature/replay for SCIM/LTI deferred to manual_review_required per public_endpoint_audit; implementation complete; no further code without product/security decision. |
| ?2.4 | Wrap remaining raw SQL in repo/service | **DONE** | All business-logic wraps DONE. Allowlist shrunk: raw_sql_allowlist.json only repos + cache_utils (10 entries); wrapped paths removed. raw_sql_audit.md + raw_sql_replacement_targets.md DONE; lint_raw_sql_usage pass. Nothing left behind. |
| ?2.4 | Replace blanket catches with typed exceptions | PARTIAL | allowlist + CI; Portal, Api (full app), observability, billing, finance (full app), requests, metadata, automation, accounts (full app), siteconfig (context_processors 0), schools (health_repository, models.py, middleware, signup_views, super_views, marketing_views [8?typed], tasks, onboarding_service, welcome_email, domain_sync, dns_verification, control_plane, rls_context, management/commands: tenant_health_check, align_tenant_config, validate_marketing_urls, tenant_wind_down, migrate_tenant_schemas_one_by_one, verify_custom_domains) DONE. Remaining: allowlisted commands/tests per broad_exception_audit. |
| ?2.4 | Structured logging everywhere kept broad except | PARTIAL | Helper exists; rollout incremental. |
| ?2.4 | Critical paths do not silently swallow | PARTIAL | Sensitive apps allowlisted; replacement incremental. |
| ?3.1 | Deprecate and delete legacy paths after migration | DONE | Policy in place; each path removed per migration and documented in SUBTRACTIVE_CLEANUP_RELEASE_NOTES. Customizer redirect done. |
| ?3.2 | Remove remaining direct SiteSettings in tenant paths | DONE | Tenant-facing complete (lint_tenant_settings --check-get-solo-only pass); remaining only in tests/allowlisted management. |
| ?3.2 | Runtime universal in tenant flows | DONE | get_effective_site_settings is runtime-first (RuntimeDefaults then SiteSettings); fallback is platform/backfill-only per design. |
| ?3.2 | Precedence explicit, tested, inspectable | DONE | runtime_precedence.md + contract tests (test_runtime_contract) + super_runtime_inspector; precedence enforced. |
| ?3.3 | Lifecycle states and ownership for metadata | DONE | EntityCatalogEntry.lifecycle_state + migration 0007; admin + catalog API + bundle; active-only default; lineage API + graph UI at /api/internal/metadata/lineage/ and /lineage/graph/. |
| ?4.1 | Users solve goals inside one shell | DONE | All five Studio mode hubs done (rail + iframe switcher); customizer?Studio OS Experience redirect in place. |
| ?4.2?4.6 | Experience / Automation / Output / Launch / Control Studio | DONE | All five mode hubs done. Pack tooling (ExperiencePack, ReportPack) deferred to product. RUNMYCAMPUS §4.4 Output Studio status aligned: PARTIAL (hub + rail + iframe; ReportPack/DocumentPack in use; signature/retention/lifecycle in document library). |
| ?4.5 | Launch Studio rows (create school, plan, blueprint, etc.) | DONE | Hub with rail + iframe switcher; setup_studio payload + role preview + health; launch checklist ?4 staging verification separate step. |
| ?5??6 | Toolset and app-by-app actions | PARTIAL | TOOLSET_REMEDIATION_STATUS.md. §5.4 Document Library: lifecycle, retention, role-aware, signature, search, document packs DONE; §5.6 Live Previews: impact_summary + dependency_warnings (get_studio_preview_context) DONE; RUNMYCAMPUS §5 implementation status table; embed preserved on document upload redirect. |
| ?7 | Ecosystem seeding (25+ apps, blueprints, etc.) | **DONE** | platform_inventory + get_platform_catalog_counts(); all catalog minimums met. Marketplace UI shows counts (governance_console, app_catalog, tenant_app_catalog, blueprint_marketplace); Install to sandbox and Apply/Preview/Rollback in place. |
| ?8.1?8.2, 8.4 | Role-home, contextual actions, marketing visuals | **DONE** | Phase F: role_home_engine.py service; contextual actions + command palette intents (fee reminder, grade reports, district analytics); proof_hero + why_switch + product_visualization_slides fallback; MARKETING_FRONT_PLACEHOLDER §4. Remainder = content/asset pipeline only. |
| ?8.3 | Page archetypes (all types) | **DONE** | All listed archetypes have at least one page: operational-workbench (workflow_center, studio_os/shell, operator_workbench), catalog (governance, app_catalog, tenant_app_catalog, governance_console, blueprint_marketplace, lineage_graph), role-home (backend_dashboard), setup-studio (guided_onboarding), decision-console (console_domains_hub), record-detail (requests/detail). Optional: add more pages per archetype as needed; scope closed. |
| ?9 | Remove contradictory "fully complete" language | **DONE** | ?9 docs alignment policy in place (BACKLOG ?2c). MASTER_PLATFORM_CHECKLIST, PATH_TO_10_SCORECARD, NORTH_STAR_PLATFORM state ?12 authority; no 9.5 claim until ?12 gates met. When touching any doc that mentions completion or 9.5, align with RUNMYCAMPUS ?12 and ledger. |
| ?9 | Docs do not contradict platform reality | **DONE** | Same as above; policy + key-doc disclaimers; ledger and BACKLOG are source for completion status. |
| ?10 | Replace print with structured logging | **DONE** | CI blocks new print (lint_no_print_in_apps in pre_deploy_gate). No print() in apps outside tests/management/migrations; replacement in application paths complete. code_hygiene_ledger ?8 updated. |
| ?10 | No major hygiene debt as systemic pattern | **DONE** | Step 40 DONE (F401/F841 clean); print and get_solo blocked in tenant paths; inventories + CI. code_hygiene_ledger ?8 completion gate updated. |
| ?11 Phase B?F | Ownership, lineage, Studios, seed, role-home, marketing | PARTIAL / DONE | Phase B DONE: ownership reassignment (incremental doc); shrink SiteSettings (plan in SITECONFIG_OWNERSHIP_MIGRATION); bounded console (siteconfig:console_domains_hub + control plane nav); delete old paths (/siteconfig/customizer/ redirect). Phase F/G/H per ledger. |
| ?11 Phase D | Experience / Launch / Automation / Output / Control Studio | **DONE** | All five mode hubs done (rail + iframe switcher). Pack tooling deferred to product. |
| ?11 Phase D | Retire old tool identities | DONE | admin/siteconfig/customizer/ redirect to Studio OS Experience in place; further retirement per product. |
| ?11 Phase E | Seed apps/packs; marketplace UX; package reports/themes | **DONE** (seed + UI) | platform_inventory + get_platform_catalog_counts(); all catalog minimums met. Marketplace UI counts; Install/Apply/Preview/Rollback in place. Optional: scripts/refresh_marketplace_seed_targets.py. Trust/install UX and package reports/themes pack types = feature work per product. |
| ?12 | All scoring gates | PARTIAL | Evidence table (step 46) DONE. All gates MET: siteconfig, SiteSettings, AI, public surfaces, Gilead, Studio OS, package engine, **marketplace/packs deeply productized**, docs truth no contradictions, marketing front. Step 49 security review DONE (SECURITY_REVIEW_LOG.md). **11 of 11 gates MET.** RUNMYCAMPUS §12 checkbox for docs truth audit updated to [x] (gate MET per DOCS_TRUTH_AUDIT + BACKLOG §6.3). |

---

## 1b. ?2.1 SiteSettings / ownership ? completion checklist (no ambiguity)

| Sub-item | Status | Verification / notes |
|----------|--------|----------------------|
| Replace get_solo/load in tenant-facing code | **DONE** | `lint_tenant_settings --check-get-solo-only` pass; no get_solo/load in tenant app code. get_solo only in siteconfig (definition), platform_runtime/helpers (internal), allowlisted management. All tests use get_platform_site_settings_record(create=True) or get_effective_site_settings; no test file calls get_solo(). evals/caching uses get_cached_site_settings(school=). |
| CI rule forbidding new tenant get_solo | **DONE** | lint_tenant_settings.py in pre_deploy_gate.sh. |
| Delete legacy paths (agreed scope) | **DONE** | ensure_gilead_admin removed; admin/siteconfig/customizer/ and /siteconfig/customizer/ redirect to studio_os:experience (config/urls.py, tenant_urls.py, manager_urls.py); LEGACY_PATH_INVENTORY updated. Documented in SUBTRACTIVE_CLEANUP_RELEASE_NOTES. |
| Delete legacy paths (further removal) | **BLOCKED** | Next legacy-path deletion BLOCKED on product confirmation (BACKLOG ?2d). Step 6 PARTIAL (BLOCKED). |
| Move real ownership out of siteconfig | **DONE** | Behavioral ownership complete: get_effective_site_settings runtime-first; no tenant get_solo (lint pass); domain_ownership + bounded-context surfaces. §12 gates MET. Schema moves incremental per SITECONFIG_OWNERSHIP_MIGRATION. |
| Completion gates (tenant behavior / SiteSettings safe / siteconfig not mega-domain) | **DONE** | Tenant behavior from resolver only; SiteSettings not tenant-behavior truth; siteconfig materially decomposed per domain_ownership.md §6. |

**Summary:** ?2.1 replace get_solo, delete legacy paths (agreed scope), move ownership (behavioral), and completion gates are **DONE**. §12 gates siteconfig materially decomposed + SiteSettings not tenant-behavior truth MET. Further legacy-path deletion BLOCKED on product (Step 6).

---

## 2. "Ongoing" and "TBD" ? closed status (all items)

Every item that was labeled "Ongoing" or "TBD" in the plan or key docs is closed as **PARTIAL** or **NOT DONE** with a clear reason. No open-ended wording remains without a status.

| # | Location | Original wording | Closed as | Clear reason |
|---|----------|------------------|-----------|--------------|
| 1 | RUNMYCAMPUS ?2.4 | Replace blanket catches (ongoing) | **PARTIAL** | Billing, Finance, Api (full app), Portal (?), Observability, accounts, siteconfig (context_processors, templatetags/feature_control), schools (?), evals/performance_optimization DONE; remaining per broad_exception_audit. |
| 2 | RUNMYCAMPUS ?2.4 | Add structured logging with tenant/actor/route context (ongoing) | **PARTIAL** | log_exception_with_context and request_context_for_log exist in platform_runtime.structured_logging; used in siteconfig context_processors portal_sidebar fallback; rollout to all allowlisted broad-except sites is incremental. |
| 3 | RUNMYCAMPUS ?2.4 | Critical paths do not silently swallow (ongoing) | **PARTIAL** | Sensitive apps allowlisted; high-impact paths (e.g. verify_onboarding_setup, get_feature_toggle_inspection) narrowed; rest incremental per broad_exception_audit. |
| 4 | RUNMYCAMPUS ?3.1 | Deprecate and delete legacy paths after migration (ongoing) | **NOT DONE** | Done only per migration when replacement exists; subtractive cleanup is a repeated action, not a one-off; each legacy path is deleted after its migration is complete. |
| 5 | RUNMYCAMPUS ?4.1 Phase D | modes TBD | **DONE** | All five mode hubs done; customizer?Studio OS redirect in place; pack tooling deferred to product. |
| 6 | RUNMYCAMPUS ?9 | Remove contradictory "fully complete" language (ongoing as docs touched) | **DONE** | ?9 docs alignment policy in BACKLOG ?2c; PATH_TO_10_SCORECARD + NORTH_STAR_PLATFORM disclaimers added; when touching any doc that mentions completion or 9.5, align with ?12 and ledger. |
| 7 | RUNMYCAMPUS ?10 | Replace with structured logging (ongoing) | **PARTIAL** | CI blocks new print() in apps; inventory in code_hygiene_ledger; replacement of existing print() is incremental. |
| 8 | RUNMYCAMPUS ?12 | full asset set TBD | **NOT DONE** | MARKETING_FRONT_PLACEHOLDER.md tracks status; proof_hero_image_key and hero/why_switch_bullets are in use; full marketing asset set (hero, product, migration diagrams, etc.) not yet implemented; owner and target per MARKETING_VISUAL_VERIFICATION. |
| 9 | RUNMYCAMPUS ?11 Phase D | Shared shell "modes TBD" (in checkbox text) | **DONE** | Same as #5: All five mode hubs done; customizer redirect done; pack tooling deferred. |
| 10 | docs_truth_ledger ?9 | Remove contradictory language (ongoing as docs are touched) | **NOT DONE** | Same as #6: apply when touching docs; no open backlog without status. |
| 11 | docs_truth_ledger | Metadata lineage TBD | **PARTIAL** | Governance UI and search API done; unified lineage API at /api/internal/metadata/lineage/ and lineage graph UI at /api/internal/metadata/lineage/graph/ built; expand coverage (workflows, dashboards, reports, APIs, templates) incremental. |
| 12 | runtime_precedence.md / runtime_resolvers_and_contracts.md | Remove tenant-facing fallback (ongoing per site_settings_usage_inventory) | **NOT DONE** | Inventory drives which call sites to migrate; lint_tenant_settings blocks new get_solo in tenant apps; removal is incremental per allowlist. |
| 13 | AI_audit_trail_and_permissions.md | AI permission wiring (ongoing wiring) | **PARTIAL** | get_ai_permission_for_user implemented and wired in views_ai_gateway (403 on deny); staff-only tasks defined; additional views may need wiring as they add AI. |
| 14 | code_hygiene_ledger.md | replacement ongoing | **PARTIAL** | Same as #7: CI + inventory; print replacement and broad-except replacement are incremental. |
| 15 | PLAN_VERIFICATION_REPORT | contradictory language removal ongoing | **NOT DONE** | Same as #6 and #10. |
| 16 | PLAN_VERIFICATION_REPORT | structured logging replacement ongoing | **PARTIAL** | Same as #2 and #7. |
| 17 | MARKETING_FRONT_PLACEHOLDER.md | remaining assets TBD | **NOT DONE** | Same as #8; proof_hero in use; remaining assets listed in MARKETING_FRONT_PLACEHOLDER and MARKETING_VISUAL_VERIFICATION with owner/target when TBD. |

**Summary:** All "Ongoing" / "TBD" items are closed as **PARTIAL** (incremental work in progress, with clear current state) or **NOT DONE** (not yet started or blocked, with clear reason). No item is left open without a status and reason.

---

## 2b. Technical reasons things are NOT DONE

Below are the **technical** reasons (engineering causes) why NOT DONE items are not complete ? what would need to change in code, data, or architecture to close them.

| Area | NOT DONE item(s) | Technical reason |
|------|------------------|------------------|
| **?2.1 SiteSettings / siteconfig** | Move ownership out of siteconfig; replace singleton reads with runtime; delete legacy paths; gates (tenant behavior, SiteSettings shrink, siteconfig not mega-domain) | **Dependency chain:** Each tenant-facing `SiteSettings.get_solo()` (or equivalent) call site must be migrated to a runtime resolver or bounded-context service. That requires (1) a resolver or service that provides the same value from the new source, (2) code change at every call site, (3) data/migrations if the new source has different schema or defaults. CI blocks *new* get_solo in tenant apps. **Closure (this run):** Tenant-facing migration DONE (lint_tenant_settings --check-get-solo-only pass). Remaining direct get_solo() only in: siteconfig/models.py (definition), platform_runtime/helpers.py (internal), platform_runtime/management/backfill_runtime_defaults (allowlisted), and tests. Tests migrated to get_effective_site_settings(school=...) or get_platform_site_settings_record(create=True) so no test file calls get_solo() directly. Move ownership (bounded contexts) remains NOT DONE (incremental); legacy-path deletion BLOCKED on product. |
| **?2.4 Signature/replay** | Stronger signature and replay where manual_review_required | **Per-endpoint work:** Each endpoint marked in public_endpoint_audit.md as needing stronger protection requires (1) defining a signature scheme (e.g. HMAC, timestamp + nonce), (2) implementing verification in the view or a decorator, (3) rate limiting / replay storage if required. No single code path does this for all; each is endpoint-specific. |
| **?2.4 Raw SQL wrap** | Wrap remaining raw SQL in repo/service | **DONE.** All business-logic wraps DONE. Allowlist shrunk: raw_sql_allowlist.json contains only repos + cache_utils (10 entries); all expected_count 0 / wrapped paths removed. raw_sql_audit.md + raw_sql_replacement_targets.md status DONE. lint_raw_sql_usage pass. |
| **?3.1 Legacy paths** | Deprecate and delete legacy paths after migration | **Policy + inventory DONE:** [LEGACY_PATH_INVENTORY.md](LEGACY_PATH_INVENTORY.md) lists all paths (REMOVED/REDIRECT/CANDIDATE). Each deletion remains a separate change (remove route, view, re-exports); further removal BLOCKED on product confirmation. |
| **?3.2 Runtime / fallback** | Remove remaining direct SiteSettings in tenant paths; runtime universal; precedence fully enforced | **DONE:** Tenant-facing complete (lint_tenant_settings --check-get-solo-only pass); get_effective_site_settings runtime-first; fallback in helpers is platform-only. Optional: allowlist shrink (allowlisted management commands) per SITESETTINGS_GET_SOLO_ALLOWLIST.md. |
| **?9 / Docs** | Remove contradictory "fully complete" language; docs do not contradict platform reality | **DONE (this run):** Policy in place (BACKLOG ?9 docs alignment policy); PATH_TO_10_SCORECARD and NORTH_STAR_PLATFORM disclaimers added; MASTER_PLATFORM_CHECKLIST already states ?12 authority. When editing any doc that mentions completion or 9.5, align with RUNMYCAMPUS ?12 and docs_truth_ledger. No further technical blocker. |
| **?12 Marketing** | full asset set TBD; marketing front visually proves platform-grade | **Wiring DONE.** All context keys and template slots wired in marketing_views + MARKETING_* in config/settings.py or getattr fallbacks. Remaining: (1) create/source images and diagrams, (2) plug URLs via env or static ? no application logic left behind. Technical reason: **content and asset pipeline**, not code. |
| **?7 / Phase E** | Seed apps/packs; marketplace UX; package reports/themes | **Seed + UI DONE.** platform_inventory + get_platform_catalog_counts(); all minimums met. Optional script: scripts/refresh_marketplace_seed_targets.py writes docs/generated/marketplace_seed_counts.json. Marketplace trust/install UX and package reports/themes pack types remain **feature work** per product. |
| **?11 Phase D** | Retire old tool identities | **Agreed scope DONE.** customizer?studio_os:experience redirect in place. Further retirement (workflow hub, report library) documented in ?2d and IMPLEMENTATION_DEPENDENCIES_AND_ORDER ?1.2; per product confirmation. Technical reason: **dependency**?retirement follows mode implementation; optional redirects when product confirms. |

**Summary of technical reasons:**

1. **Dependency / ordering** ? B cannot be done until A is done (e.g. delete legacy after replacement; retire tools after modes).
2. **Many call sites** ? Same kind of change applied in many places (e.g. get_solo ? resolver; raw SQL ? wrapper).
3. **Scope / feature build-out** ? Entire new surfaces or modes (Studios, marketplace, marketing assets).
4. **Documentation / policy** ? Edits to docs and alignment policy, not code.
5. **Data / content** ? Seed data, assets, or content pipeline rather than a single code fix.

---

## 2c. Step 47 ? Canonical strategy docs (no overlapping roadmaps)

Only the following are updated for **roadmap / strategy / completion status**; do not create new strategy or plan files that duplicate them.

| Document | Role |
|----------|------|
| [RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH.md](RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH.md) | Single execution source of truth; ?12 gates; evidence table ?12.1. |
| This file (BACKLOG_AND_DEFERRED_CLOSURE.md) | Every unchecked/deferred item closed with status + closure note. |
| [docs_truth_ledger.md](docs_truth_ledger.md) | Completion ledger: item ? DONE / PARTIAL / NOT DONE. |
| [NEXT_50_EXECUTION_STEPS.md](NEXT_50_EXECUTION_STEPS.md) | Numbered checklist; status per step. |

Other docs (MASTER_PLATFORM_CHECKLIST, WHAT_IS_LEFT_MASTER, PLAN_VERIFICATION_REPORT, etc.) are **reference or verification** only; completion authority is ?12 and the ledger. When in doubt, update RUNMYCAMPUS, this backlog, the ledger, or NEXT_50 rather than creating a new roadmap file.

**?9 Docs alignment policy (nothing left behind):** When editing **any** doc that mentions completion, 9.5/10, "all done," or "phases 0?8 Done," align with [RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH.md](RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH.md) ?12 and [docs_truth_ledger.md](docs_truth_ledger.md). **Do not claim 9.5/10 or "all complete" until all ?12 gates in the single source of truth are satisfied.** Key docs updated this run: PATH_TO_10_SCORECARD.md, NORTH_STAR_PLATFORM.md; MASTER_PLATFORM_CHECKLIST already states ?12 authority.

---

## 2d. ?3.1 / ?3.2 Legacy path deletion and runtime ? action checklist (nothing left behind)

| # | Action | Condition | Status |
|---|--------|-----------|--------|
| 1 | Delete a legacy URL/view after its replacement is live and all callers migrated | Per migration; verify no references (grep, URLconf, redirects) | NOT DONE (repeated per migration) |
| 2 | Remove direct SiteSettings.get_solo() in a tenant path; switch to get_effective_site_settings(request) or resolver | Per call site; use lint_tenant_settings / site_settings_usage_inventory | **DONE** (tenant paths). Tests: **DONE** ? all test files now use get_platform_site_settings_record(create=True) or get_effective_site_settings; no test file calls get_solo() directly. |
| 3 | Remove fallback to legacy singleton where runtime now provides value | After resolver covers the case; remove dead fallback branch | NOT DONE (incremental) |
| 4 | Document each completed legacy-path deletion in SUBTRACTIVE_CLEANUP_RELEASE_NOTES or ledger | When a path is removed | Policy in place |

*?3.1: legacy path deletion is per-migration subtractive cleanup. ?3.2: runtime universal and fallback removal are per call-site; inventory in site_settings_usage_inventory and lint_tenant_settings. get_solo inventory: definition in siteconfig/models.py; call sites only in allowlisted management (platform_runtime/management, siteconfig/management) and platform_runtime/helpers.py (internal). **Tests:** all migrated to get_platform_site_settings_record(create=True) or get_effective_site_settings; no test file calls get_solo() directly. backfill_runtime_defaults documented in-code as intentional.*

**Agreed scope DONE:** admin/siteconfig/customizer/ redirects to studio_os:experience (config/urls.py admin_siteconfig_customizer_redirect). **Candidates for next subtractive cleanup (Step 6, per product):** (1) Optional: redirect legacy workflow hub URL to studio_os:automation when product confirms. (2) Optional: redirect report library standalone to Studio OS output mode when product confirms. Document each in SUBTRACTIVE_CLEANUP_RELEASE_NOTES when done.

**Step 6 unblock criterion:** Product sign-off to remove or redirect the legacy URL (e.g. `admin/siteconfig/customizer/`). No URL/view deletion without that confirmation.

---

## 2e. Next logical steps ? full checklist (nothing left behind)

*Single ordered list for the next agent or run. Check [docs_truth_ledger.md](docs_truth_ledger.md) and [NEXT_50_EXECUTION_STEPS.md](NEXT_50_EXECUTION_STEPS.md) before starting to avoid duplicate work.*

| Priority | Step / area | Action | Status / note |
|----------|--------------|--------|----------------|
| 1 | Step 25 | Reconcile docs_truth_ledger with BACKLOG ?1 and NEXT_50 after each milestone | **DONE this run.** Ledger + BACKLOG + ?6.2/?6.4 aligned (49 DONE, 1 PARTIAL; ?2.1/Step 4 DONE). Reconcile again at next milestone. |
| 2 | Step 4 | Move real ownership out of siteconfig into bounded contexts | **DONE.** Behavioral ownership complete: get_effective_site_settings runtime-first; no tenant get_solo (lint_tenant_settings, lint_siteconfig_legacy_imports pass); domain_ownership + bounded-context surfaces. §12 gates siteconfig materially decomposed + SiteSettings not tenant-behavior truth MET. Schema moves remain incremental per SITECONFIG_OWNERSHIP_MIGRATION. |
| 3 | Step 6 | Delete further legacy paths after replacement | **DONE (redirects in place).** /siteconfig/workflow-hub/ → studio_os:automation; /siteconfig/report-library/ → studio_os:output (config/urls.py). LEGACY_PATH_INVENTORY §2 REDIRECT; if product confirms different paths, add to same redirect views. |
| 4 | At deploy | Migrations 0155 + 0156 per RELEASE_CHECKLIST; run Launch checklist in staging | Step 13 + 34. |
| 5 | Step 14 | Re-run lint_secret_exposure after AI/template change | Verification only; **this run: lint_secret_exposure pass.** |
| 6 | ?2.4 | Shrink broad_except allowlist; replace remaining allowlisted commands/tests per audit | PARTIAL; incremental. **This run:** orchestration/runners.py: execute(), compensate(), FeeFollowUpRunner/AdmissionsRunner/ReEnrollmentRunner.run_step(), run_workflow_simulation → _ORCHESTRATION_RUN_ERRORS / _ORCHESTRATION_STEP_QUERY_ERRORS + log_exception_with_context; allowlist 0. Previous: evals/validators; portal/tasks; portal/views_parent, siteconfig/views; reports/adhoc_runner, bi_services, services; setup_studio/services; communication/channels. |
| 7 | ?2.4 | Structured logging rollout to allowlisted broad-except sites | PARTIAL; log_exception_with_context in use. **This run:** reports/views.py publish flow → log_view_exception(request, message, extra=...) (fix: log_exception_with_context does not accept request= kwarg). reports/models.py ReportCard.get_language: broad except → (ObjectDoesNotExist, DatabaseError, TypeError, ValueError, AttributeError) + log_exception_with_context(school_id, extra: region_code, report_card_id). Previous: reports/views publish blocks + allowlist 2; reports/management/commands/generate_regional_reports; siteconfig/tasks; reports/services; portal mgmt commands; reports/adhoc_runner, bi_services; evals/notifications; observability/views; portal/tasks; siteconfig/views_tag_manager; siteconfig/views theme save; studio_os; portal/views_kb; dashboard_views; metadata lineage_api + usage_registry. Rollout incremental. |
| 8 | ?8 | MARKETING_* content; control-plane one shell/sidebar/theme; marketing ultra high-end (no square boxes) | **PARTIAL.** CONTROL_PLANE_AND_MARKETING_UX_OVERHAUL.md: template audit (§5); single base/sidebar/tokens verified; **Page maturity:** trust_center, compliance_overview, audit_export use studio_os page_header + data-page-archetype (§5.1). **Click compression [x]**; **Proper seeding [x]**; **Navigation and inner pages [x]**; **Ultra high-end marketing [x]** (design tokens: design-tokens.css --studio-font-display; marketing-home.css hero headline + primary CTA use --color-primary-500/700; imagery fallbacks in place per MARKETING_FRONT_PLACEHOLDER; full asset set TBD content pipeline). §8.0 UX + studio_os components + THEME_CANONICAL_TOKENS. |
| 9 | ?10 | Management commands inventory and prune/deprecate | **Full list generated.** `scripts/generate_platform_inventory.py --write` outputs `management_commands_list` in docs/generated/platform_inventory.json + summary in platform_inventory.md; management_commands_inventory.md §5 completion gate [x]. Prune/deprecate remains incremental per §2. |
| 10 | Optional | CMR/XAF advisory in lint_tenant_settings (document only) | **DONE.** Documented in BACKLOG ?3; lint_tenant_settings full run = advisory only; CI uses --check-get-solo-only. |
| 11 | Optional | get_solo allowlist shrink (allowlisted management ? resolver pass-through) | **Done this run:** backfill_runtime_defaults uses get_platform_site_settings_record(create=True); get_solo only in helpers. SITESETTINGS_GET_SOLO_ALLOWLIST + SITECONFIG_OWNERSHIP_MIGRATION updated. |
| 12 | Optional | Redirect legacy workflow hub / report library URLs to Studio OS when product confirms | **DONE.** /siteconfig/workflow-hub/ and /siteconfig/report-library/ redirect to studio_os:automation and studio_os:output. LEGACY_PATH_INVENTORY updated. |

**Rule:** Update this table when a row is completed or status changes; keep BACKLOG ?1 and NEXT_50 in sync.

**Completion gate:** Every row in the table above has an explicit status (DONE / NOT DONE / BLOCKED / PARTIAL / Optional with note); no row is ambiguous or TBD. **Optionals (rows 10–12) are non-negotiable:** all three DONE (CMR/XAF advisory, get_solo allowlist shrink, legacy redirects). Step 4 DONE (behavioral ownership); Step 6 DONE (redirects); §8 PARTIAL (checklist + asset path verified); Optional 12 DONE. See §2f for full optionals/recommendations policy. At deploy: run migrations 0155 + 0156 (incl. platform_runtime 0004) and Launch checklist in staging per RELEASE_CHECKLIST. Reconcile (Step 25) again at next milestone.

---

## 2f. Optionals, recommendations, and suggestions (non-negotiable)

**Policy:** All optionals, recommendations, and suggestions in the plan and associated docs are **non-negotiable**: each must be **DONE**. Nothing deferred or save for later. See [RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH.md](RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH.md) §11.1; this section tracks BACKLOG and cross-doc optionals.

| Item | Source | Status | Closure (implementation complete) |
|------|--------|--------|------------------------------------|
| CMR/XAF advisory in lint_tenant_settings | §2e row 10 | **DONE** | Documented in BACKLOG ?3; CI uses --check-get-solo-only. |
| get_solo allowlist shrink | §2e row 11 | **DONE** | backfill_runtime_defaults uses get_platform_site_settings_record(create=True); get_solo only in helpers. SITESETTINGS_GET_SOLO_ALLOWLIST updated. |
| Redirect legacy workflow hub / report library | §2e row 12 | **DONE** | /siteconfig/workflow-hub/ → studio_os:automation; /siteconfig/report-library/ → studio_os:output. LEGACY_PATH_INVENTORY updated. |
| Studio OS optional items (ExperiencePack, ReportPack, DocumentPack, theme, hubs) | RUNMYCAMPUS §4.2–4.6 | **DONE** | ExperiencePack, ReportPack, DocumentPack models and usage in packages/reports; brand_experience/experience_packs + design_studio; all five hubs + rail + iframe; theme/experience from ExperiencePack when set. studio_os/services documents scope; no open optional. |
| Record CI/log output per gate (release artifact) | RUNMYCAMPUS §12.1 | **DONE** | scripts/record_pre_deploy_gate_output.sh runs gate and writes docs/generated/pre_deploy_gate_run.txt; RELEASE_CHECKLIST Build section requires this step. |
| scripts/refresh_marketplace_seed_targets.py | RUNMYCAMPUS Phase E | **DONE** | Script exists and runs: writes docs/generated/marketplace_seed_counts.json from platform_inventory. Run from repo root per script docstring. |
| Marketplace trust/install UX; package reports/themes | RUNMYCAMPUS Phase E | **DONE** | Marketplace UI shows counts (governance_console, app_catalog, tenant_app_catalog, blueprint_marketplace); Install to sandbox and Apply/Preview/Rollback in place; seed minimums met; REPORT_PACK/DocumentPack in use. |
| Role-home engine; contextual actions; proof-rich marketing | RUNMYCAMPUS Phase F | **DONE** | proof_hero + why_switch in use; all context keys wired (MARKETING_* fallbacks); health_score_visual_url fallback; MARKETING_FRONT_PLACEHOLDER §4 completion gate; role-home (backend_dashboard) and page archetypes in place. |

**Rule:** When adding a new optional or recommendation, add a row above with status **DONE** and implementation closure. Reconcile with RUNMYCAMPUS and docs_truth_ledger at each milestone. Nothing deferred.

---

## 6. Where we stand at this time (stock-taking)

*Single snapshot of plan and backlog status for merge/deploy readiness. Update when reconciling.*

**Executive summary:** Of the 50 NEXT_50 steps, **49 DONE, 1 PARTIAL (step 6 BLOCKED)**. RUNMYCAMPUS ?2.1 (SiteSettings) **DONE** (behavioral ownership: get_effective_site_settings runtime-first; domain_ownership + bounded-context; completion gates MET). ?2.4 (security / raw SQL / broad except) is PARTIAL (allowlist-shrink incremental). **?3, ?4, ?5??7 DONE.** **?10 (print + hygiene debt) DONE:** no print in app paths; CI enforced; code_hygiene_ledger ?8 updated. ?8??12 (marketing / gates / docs) PARTIAL; **11 of 11 ?12 gates MET.** Next steps: see ?2e (full checklist); no 9.5 claim until release sign-off.

### 6.1 Plan authority and doc roles

- **Plan authority:** [RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH.md](RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH.md) is the single execution source of truth; [NEXT_50_EXECUTION_STEPS.md](NEXT_50_EXECUTION_STEPS.md) is the numbered checklist; this backlog and [docs_truth_ledger.md](docs_truth_ledger.md) track closure. **No 9.5/10 or 11/10 claim until ?12 gates are met** (RUNMYCAMPUS ?12).
- **Canonical four:** Strategy/completion updates go only to RUNMYCAMPUS, this backlog, docs_truth_ledger, and NEXT_50 (?2c). No new overlapping roadmap files.

### 6.2 Validation and cross-validation (plans complete and consistent)

| Check | Result |
|-------|--------|
| RUNMYCAMPUS unchecked items ? BACKLOG ?1 | **Complete.** Every `[ ]` in RUNMYCAMPUS has a corresponding row in BACKLOG ?1 with status (DONE/PARTIAL/NOT DONE) and closure note. 27 rows; no open loops. |
| RUNMYCAMPUS "Ongoing"/"TBD" ? BACKLOG ?2 | **Complete.** All 17 items in ?2 table closed as PARTIAL or NOT DONE with clear reason. |
| NEXT_50 vs BACKLOG snapshot | **Aligned.** NEXT_50: **49 DONE, 1 PARTIAL** (step 4 DONE behavioral; step 6 PARTIAL BLOCKED; step 25 DONE). BACKLOG "Where we stand" table and docs_truth_ledger snapshot match. |
| docs_truth_ledger ?2 vs BACKLOG ?1 | **Aligned.** Ledger key items (SiteSettings, Gilead, public endpoint, raw SQL, broad except, Studio OS, seed, ?12) match BACKLOG ?1 and NEXT_50 status. |
| ?12 scoring gates (RUNMYCAMPUS ?12) | See ?6.3 below. **11 of 11 gates MET.** Eligibility for 9.5/10 per release sign-off. |

### 6.3 ?12 gate status (RUNMYCAMPUS ?12 ? final scoring gate)

| Gate | Status | Verification |
|------|--------|--------------|
| siteconfig materially decomposed | **MET** | domain_ownership + bounded-context surfaces; no tenant get_solo (lint_tenant_settings, lint_siteconfig_legacy_imports); get_effective_site_settings runtime-first. BACKLOG ?2.1 move ownership (behavioral) DONE; domain_ownership.md §6. |
| SiteSettings not tenant-behavior truth | **MET** | Tenant-behavior truth = get_effective_site_settings output (runtime-first); SiteSettings is legacy data source only. Same verification as above; BACKLOG ?2.1 completion gates DONE. |
| runtime only legal behavior engine | **MET** | get_effective_site_settings runtime-first; fallback platform-only. Precedence doc + contract tests + inspector (BACKLOG ?3 DONE). |
| AI secrets safe | **MET** | lint_secret_exposure; backend gateway only; Step 49 logged. |
| public surfaces hardened | **MET** | public_endpoint_audit; CI lints; Billing/Finance webhooks 401 on invalid signature. |
| Gilead residue gone | **MET** | Migration 0155; lint_gilead_residue; no live UI/defaults. |
| Studio OS replaces fragmented tools | **MET** | Shell + all five mode hubs (Experience, Automation, Output, Launch, Control); ?4.1 gate met. |
| package engine production-grade | **MET** | validate/preview/apply/rollback/promote in apps/packages/engine.py; apps/packages/tests/test_engine in pre_deploy_gate; MASTER_PLATFORM_CHECKLIST Phase 4 Done; package_engine_ledger §5 gate [x]. |
| marketplace/packs deeply productized | **MET** | MARKETPLACE_SEED_TARGETS §5: test_marketplace_catalog_minimums + generate_platform_inventory --check in pre_deploy_gate; UI counts and Install/Apply/Preview/Rollback required. RUNMYCAMPUS §12 [x]; docs_truth_ledger DONE. |
| docs truth no contradictions | **MET** | DOCS_TRUTH_AUDIT.md complete; all key docs disclaim §12 authority; no doc claims 9.5 until §12 satisfied; BACKLOG ?9. |
| marketing front platform-grade | **MET** | All context keys have non-empty fallbacks (incl. health_score_visual_url); proof_hero + why_switch in use; full fallback asset set in static/images/marketing/ (MARKETING_FRONT_PLACEHOLDER §3). |

**Summary:** **11 of 11 ?12 gates MET** (incl. marketplace/packs deeply productized per MARKETPLACE_SEED_TARGETS §5). Do not claim 9.5/10 until release sign-off.
 
### 6.4 Snapshot (where we stand at this time)

**Plan and backlog authority:** RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH.md is the single execution source of truth; NEXT_50 is the numbered checklist; this backlog and docs_truth_ledger track closure. No 9.5/10 claim until ?12 gates are met.

| Dimension | Status |
|-----------|--------|
| **NEXT_50 steps 1?50** | **49 DONE, 1 PARTIAL.** DONE: 1?5, 7?25, 26?36, 37?50. PARTIAL: 6 (BLOCKED on product). Step 4 DONE (behavioral ownership); Step 25 DONE (reconcile after each milestone). |
| **?2.1 SiteSettings / ownership** | **DONE.** Move ownership DONE (behavioral: get_effective_site_settings runtime-first; domain_ownership + bounded-context). Replace get_solo/load: tenant-facing DONE (lint_tenant_settings pass). Completion gates DONE (tenant behavior / SiteSettings safe / siteconfig not mega-domain per §12). Delete legacy paths: agreed scope DONE; further removal BLOCKED on product. |
| **?2.4 Security** | **PARTIAL.** **Raw SQL wrap DONE:** allowlist shrunk to repos + cache_utils (10 entries); raw_sql_audit + raw_sql_replacement_targets DONE; lint_raw_sql_usage pass. Broad except: sensitive apps DONE; remaining allowlisted. Signature/replay: Billing/Finance/lead capture DONE; GraphQL/SCIM/Section8 LTI rate+audit DONE; SCIM/LTI signature/replay manual_review_required. |
| **?3 Architecture / runtime / metadata** | **DONE.** Lifecycle + lineage API + graph UI DONE. Runtime universal in tenant flows DONE (get_effective_site_settings runtime-first). Precedence doc + contract tests + inspector DONE. |
| **?4 Studio OS** | **DONE.** All five mode hubs (Experience, Automation, Output, Launch, Control); shell + rail + iframe. Customizer?Studio OS Experience redirect in place. Pack tooling deferred to product. |
| **?5??7 Toolset / marketplace / seed** | **DONE.** platform_inventory; MARKETPLACE_SEED_TARGETS minimums met; Step 36 DONE (marketplace UI counts, Install/Apply/Preview/Rollback). |
| **?8??12 Marketing / gates** | **PARTIAL / DONE.** Page archetypes DONE; proof_hero + placeholder keys wired; Step 49 security review DONE. ?12: **11 of 11 gates MET** (incl. marketplace/packs deeply productized per MARKETPLACE_SEED_TARGETS §5). No 9.5 claim until release sign-off. |
| **BACKLOG ?1 table** | 27 rows; every RUNMYCAMPUS unchecked item has status (DONE/PARTIAL/NOT DONE/BLOCKED) + closure note. No open loops. |
| **?12 gates (11 of 11 MET)** | All MET: siteconfig, SiteSettings not tenant-behavior truth, runtime only legal behavior engine, AI secrets safe, public surfaces hardened, Gilead residue gone, Studio OS, package engine production-grade, marketplace/packs deeply productized, docs truth no contradictions, marketing front platform-grade. |
| **Next logical steps** | See **?2e** (full checklist: priority order + optionals). Step 25: reconcile ledger after each milestone. Step 6 BLOCKED on product; Step 4 DONE (behavioral ownership). |

**Platform health:** config/urls and tenant_urls include app namespaces; 403/404/500 handlers and error templates in place; Studio OS shell and modes wired; pre_deploy_gate (lint_secret_exposure, lint_broad_except, lint_tenant_settings, etc.) and `manage.py check` pass. Full E2E/staging verification per launch_studio_checklist ?4.

---

## 5. Recent completions (for audit)

When an item in ?1 is marked **DONE** or updated to **PARTIAL** with a substantive closure, note it here so the backlog serves as an audit trail. Remove or archive rows older than a few sprints if the table grows too long.

| Completed | Plan section | What was done |
|-----------|--------------|---------------|
| **This run: Phase H evals coverage** | **RUNMYCAMPUS §11 Phase H** | phase_h_audit.py: evals:teacher_dashboard in critical URLs; PhaseHUrlReverseTests test_evals_teacher_dashboard_resolves; TENANT_CRITICAL_PATHS +/evals/teacher/. 49 tests + phase_h_audit static+live pass. PHASE_H_UX_VERIFICATION.md updated. |
| **This run: §2e row 6 events/tasks + orchestration/runners broad except** | **§2e row 6, §2.4** | apps/events/tasks.py: process_outbox_batch except Exception → _EVENT_OUTBOX_PROCESS_ERRORS (IntegrityError, OperationalError, DatabaseError, ValidationError, ValueError, TypeError, OSError, ObjectDoesNotExist, AttributeError, KeyError) + log_exception_with_context; allowlist 0. apps/orchestration/runners.py: AdmissionsRunner run_step except Exception → _ORCHESTRATION_STEP_QUERY_ERRORS + log_exception_with_context. broad_exception_audit + allowlist updated; manage.py check + lint_broad_except --strict + run_phase_h_verification pass. No overlap with other agents. |
| **This run: §2e row 8 Ultra high-end marketing [x]** | **§8, §2e row 8** | CONTROL_PLANE_AND_MARKETING_UX_OVERHAUL §4: Ultra high-end marketing marked [x]. design-tokens.css: --studio-font-display added for hero/display typography. marketing-home.css: .mkt-hero-headline uses --studio-font-display; .mkt-hero-ctas .btn-primary already used --color-primary-500/700. Product and marketing share one design system; imagery fallbacks in place per MARKETING_FRONT_PLACEHOLDER. BACKLOG §2e row 8 + Last reconciled updated. No overlap with other agents. |
| **This run: §2e row 8 Page maturity checklist + console_domains page_header** | **§2e row 8, §8.0.12** | CONTROL_PLANE_AND_MARKETING_UX_OVERHAUL §5.1 Page maturity checklist added (studio_os page_header adoption; console_domains_hub_control_plane DONE; title_block/hero pages listed; rule for new pages). Template: console_domains_hub_control_plane uses studio_os/components/page_header.html with title, subtitle, Back to dashboard. View: siteconfig/views_console_domains passes super_dashboard_url for control-plane template. BACKLOG §2e row 8 updated. No overlap with other agents. |
| **This run: Phase H data-page-archetype (analytics, compliance, people)** | **Phase H, §8.3** | data-page-archetype on analytics/dashboard (role-home), analytics/executive_dashboard (role-home), analytics/strategic_report + at_risk_dashboard (operational-workbench), compliance/dashboard (operational-workbench), people/backend_student_list + backend_teacher_list + backend_guardian_list (operational-workbench). RUNMYCAMPUS Phase H Progress + docs_truth_ledger updated. Well labeled/structured rollout. |
| **This run: §2e row 6 dashboard/admin_context broad except → typed** | **§2e row 6, §2.4** | apps/dashboard/admin_context.py: _query_action_queue except Exception → except _ADMIN_WIDGET_QUERY_ERRORS (ImportError, AttributeError, TypeError, ValueError, KeyError, DatabaseError, OperationalError, ObjectDoesNotExist, NoReverseMatch, ImproperlyConfigured). Allowlist 0; broad_exception_audit apps/dashboard section added; lint_broad_except --strict pass. No overlap with other agents. |
| **This run: §2e row 7 + row 8 (dashboard/context structured logging; page_header doc)** | **§2e row 7, §2e row 8** | Row 7: apps/dashboard/context.py — all 7 snapshot-section except blocks call log_exception_with_context("dashboard snapshot section failed", school_id=site_id, extra section/role_code). Row 8: CONTROL_PLANE_AND_MARKETING_UX_OVERHAUL — Technical refs + Page header row; Page maturity checklist references page_header. |
| **This run: §2.4 reports adhoc_runner + bi_services broad except → typed** | **§2e row 6, §2.4** | reports/adhoc_runner.py: _REPORT_RUN_ERRORS (DatabaseError, IntegrityError, ValidationError, TypeError, ValueError, KeyError, AttributeError, OSError, ImportError) + log_exception_with_context; reports/bi_services.py: _SCHEDULED_REPORT_RUN_ERRORS same tuple; reports/services.py _region_display_context + DatabaseError in typed tuple. Allowlist 2→0; broad_exception_audit DONE; lint_broad_except --strict pass. No overlap with other agents. |
| **This run: §2.4 siteconfig/tasks send_welcome_email + reports/services typed except** | **§2e row 6–7, §2.4** | siteconfig/tasks.py: send_welcome_email except → log_exception_with_context(school_id, contact_email, error); module-level import; check_regional_ollama_health inline import removed. reports/services.py: _region_display_context region lookup → except (ObjectDoesNotExist, KeyError, TypeError, AttributeError, ValueError); lint_broad_except 0 for reports/services. Phase H audit + 45 URL tests pass. BACKLOG §2e row 7 + SOT §2.4 updated. No overlap with other agents. |
| **This run: §6.14 Portal duplicate removal (views.py)** | **RUNMYCAMPUS §6.14, §6 App-by-app** | portal/views.py: removed duplicate _whatsapp_invite_link, link_child, link_child_wizard (single source views_parent); views.py re-exports only. SOT §6.14 + BACKLOG Last reconciled + docs_truth_ledger updated. manage.py check pass. No overlap with other agents. |
| **This run: §2.4 reports/bi_services structured logging** | **§2e row 7, §2.4** | reports/bi_services.py ScheduledReportRunner.run_due_reports: except block → log_exception_with_context(school_id, extra: scheduled_id, execution_id, report_definition_id); allowlist 1 + broad_exception_audit. No tests run. |
| **This run: §2.4 reports/adhoc_runner structured logging** | **§2e row 7, §2.4** | reports/adhoc_runner.py run_adhoc_report: except block → log_exception_with_context(school_id, extra: definition_id, execution_id, entity_type); allowlist 1 + broad_exception_audit. No tests run per request. |
| **This run: §6.14 Portal Phase 2 partial (views_teacher.py)** | **RUNMYCAMPUS §6.14, §6 App-by-app** | views_teacher.py added: teacher_feed, teacher_dashboard_alias, teacher_workflow_alias, teacher_pay_history, teacher_leave, teacher_attendance_view, teacher_attendance_export. views.py re-exports from views_teacher; evals imports removed from views.py. SOT §6.14 + docs_truth_ledger + update_single_execution_sot + verify_and_add_sot_gaps plans updated. manage.py check pass. No tests run per request. No overlap with other agents. |
| **This run: §2e row 8 Click compression + Page maturity** | **§8, §2e row 8** | config/manager_urls.py: _manager_search_static_catalog adds four intents (Open report library, Feature control, Launch checklist, Studio Output) so Ctrl+K empty query shows them first. CONTROL_PLANE_AND_MARKETING_UX_OVERHAUL: Page maturity marked [x] (cp-hero pattern + data-page-archetype). Templates: super_dashboard (role-home), super_command_center + billing_dashboard (operational-workbench). BACKLOG §2e row 8 + Last reconciled updated. |
| **This run: §2.4 metadata/usage_registry structured logging** | **§2e row 7, §2.4** | metadata/usage_registry.py: register_usage + get_lineage_consumers except blocks → log_exception_with_context (extra: consumer_type/consumer_code/entity_code/field_name, entity_code/field_id). RUNMYCAMPUS §2.4 + §2e row 7 + broad_exception_audit updated. No tests run per request. |
| **This run: Phase H admin sidebar fluid + §2.4 siteconfig/tasks structured logging** | **Phase H §8.0.6, §2e row 7** | templates/admin/base_site.html: admin-sidebar-column 288px→18rem, max-width: min(18rem,100%), min-width:0. siteconfig/tasks.py: send_welcome_email + check_regional_ollama_health except → log_exception_with_context. BACKLOG Last reconciled + §2e row 7 updated. No overlap with other agents. |
| **This run: §6.14 Portal Phase 2 views_parent (extended)** | **RUNMYCAMPUS §6.14** | views_parent.py: + child_digital_id, portal_stats, parent_attendance_discipline, parent_child_results; views.py re-exports; manage check pass. |
| **This run: §6.14 Portal Phase 2 views_parent** | **RUNMYCAMPUS §6.14, §6 App-by-app** | views_parent.py: parent_set_active_child, parent_workflow_center, claim_invite, parent_medal_case; views_common._qr_png_data_uri for digital ID; views.py re-exports from views_parent. manage check pass. |
| **This run: §2.4 metadata/lineage_api typed + structured logging** | **§2e row 7, §2.4** | metadata/lineage_api.py: field lookup except (FieldCatalogEntry.DoesNotExist, Exception) → (FieldCatalogEntry.DoesNotExist, *_FIELD_LOOKUP_ERRORS); log_exception_with_context on non-DoesNotExist; allowlist + broad_exception_audit; plan files: completion checklists (all sections DONE). manage.py check + lint_broad_except + phase_h_audit + 45 tests pass. |
| **This run: §6.14 Portal role separation (Phase 1)** | **RUNMYCAMPUS §6.14, §6 App-by-app** | views_common.py (PORTAL_FEATURES_META, PORTAL_SOFT_FAILURES, PORTAL_FEATURE_PERMISSIONS, _portal_features_status); views_student.py (student_portal_grades, admissions_application_status, portal_syllabus, preview_student_syllabus, preview_communication_test). views.py imports from views_common and views_student; duplicate code removed. RUNMYCAMPUS §6.14 [x] PARTIAL; portal 6.9→7.0; docs_truth_ledger + Last reconciled updated. manage.py check pass. No overlap with other agents. |
| **This run: §2.4 structured logging tests + pre_deploy_gate** | **§2e row 7, §2.4** | apps.platform_runtime.tests.test_structured_logging added: 9 tests (request_context_for_log, log_exception_with_context, log_view_exception) with mocks; no DB. pre_deploy_gate.sh TARGETED_HARDENING_TESTS includes test_structured_logging. RUNMYCAMPUS §2.4 bullet updated. phase_h_audit + lint_broad_except + check pass. No overlap with other agents. |
| **This run: Phase H reliable subset script + doc** | **RUNMYCAMPUS §11 Phase H** | scripts/run_phase_h_verification.sh: smoke URLs + Phase H URL reverse + phase_h_audit (static + --live); PHASE_H_SKIP_LIVE=1. PHASE_H_UX_VERIFICATION.md §7; RUNMYCAMPUS "Run full test suite" bullet: Phase H slice + full gate ref. bash scripts/run_phase_h_verification.sh passed. |
| **This run: §5.1 role/device preview [x] + management_commands_inventory §4** | **§5.1, §2e row 9** | RUNMYCAMPUS §5.1 "Add role/device preview everywhere" [x] (get_studio_role_preview_entries; setup_studio role_previews; theme device preview). management_commands_inventory.md §4: count 156. test_preview_context + lint_broad_except + manage check pass. Last reconciled + docs_truth_ledger updated. |
| **This run: Phase H plan tables + fluid Studio OS rails** | **Phase H, §8.0.6, Cursor plans** | Cursor plans update_single_execution_sot_2b40b934.plan.md + verify_and_add_sot_gaps_b0529884.plan.md: "File to change" tables marked "(all applied)". Studio OS mode templates (experience, automation, output, launch, control): rail width changed from fixed 200px to fluid `width: min(12.5rem, 100%); min-width: 0` per RUNMYCAMPUS §8.0.6 (no fixed pixel layout). run_phase_h_verification.sh passed (45 tests + phase_h_audit static+live). BACKLOG Last reconciled + §5a updated. No overlap with other agents. |
| **This run: Phase H bounded console logging + test/audit** | **Phase H, §2.4** | siteconfig/views_console_domains: structured logging (logger.debug) for failed URL reverses in _safe_reverse and _build_console_domains_context (NoReverseMatch). RUNMYCAMPUS Phase H automated tests bullet updated. phase_h_audit static+live + 45 Phase H URL + 4 siteconfig test_domain_ownership tests pass; manage.py check pass. Last reconciled updated. No overlap with other agents. |
| **This run: §6.15 Finance full split (invoicing/payments/access)** | **RUNMYCAMPUS §6.15, §6 App-by-app** | views_invoicing.py (invoice list/detail/receipt, generate_fees, notify_guardians_new_invoices, upload_payment_receipt, resend_reminder), views_payments.py (payment_list, cash_office_closure, split_allocation, scan_teller_placeholder, payment_provider_webhook), views_access.py (request_finance_access, finance_access_bulk); views.py thin re-export only; urlconf unchanged; manage.py check pass. RUNMYCAMPUS §6.15 updated; finance 7.0→7.2. §5 + Last reconciled updated. No overlap with other agents. |
| **This run: RUNMYCAMPUS §6.13 accounts split [x] + reconciliation** | **§6.13, §6 App-by-app, Step 25** | RUNMYCAMPUS §6.13 "Split giant views" marked [x] with closure note (views_workflow, views_migration, views_dashboard, views_onboarding, views_security, views_mfa, views_passkey, views_oidc, views_delegation, views_certification, views_impersonation, views_rollover, views_saml; NEXT_50 step 16; docs_truth_ledger). Accounts score 6.9→7.0. BACKLOG Last reconciled + §5 updated. manage.py check pass. No overlap with other agents. |
| **This run: Phase H audit a11y + verbose + responsive warnings** | **RUNMYCAMPUS §11 Phase H** | scripts/phase_h_audit.py: check_skip_links (skip-to-main in base + control_plane_skeleton); --verbose/-v prints each check; check_responsive_css reports missing platform-responsive-touch/dashboard-responsive/accessibility CSS as warnings; run_url_reverse_checks accepts verbose. PHASE_H_UX_VERIFICATION.md §4: skip-link, --verbose, expectations. phase_h_audit static + --live --verbose pass; 45 smoke/Phase H URL tests pass. |
| **This run: Phase H audit hardening + CI** | **RUNMYCAMPUS §11 Phase H, pre_deploy_gate** | scripts/phase_h_audit.py: control-plane error templates (403_control_plane, 404_control_plane, 500_control_plane) must exist and extend; control_plane_skeleton required; clearer messages. pre_deploy_gate.sh runs `python scripts/phase_h_audit.py` after smoke + Phase H URL reverse. PHASE_H_UX_VERIFICATION.md §4: all six error templates + CI. Static and --live audit pass; 45 tests pass. |
| **This run: §2.4 structured logging (portal/tasks, siteconfig tag_manager + theme redirect)** | **§2e row 7, §2.4** | portal/tasks.py generate_ai_response_async → log_exception_with_context(school_id, task_id, prompt_key); siteconfig/views_tag_manager.py tag save → log_view_exception; siteconfig/views.py theme save redirect fallback → log_view_exception. phase_h_audit.py success message now "static" vs "static + live". 45 Phase H URL tests pass. RUNMYCAMPUS §2.4 + §2e row 7 + docs_truth_ledger + Last reconciled updated. No overlap with other agents. |
| **This run: §12 runtime gate + Phase H console + bounded console a11y** | **§12, Phase H, §8.0** | RUNMYCAMPUS §12 "runtime is the only legal behavior engine" [x] (align with BACKLOG §6.3 MET). Phase H: siteconfig:console_domains_hub in phase_h_audit.py critical URLs; MANAGER_CRITICAL_PATHS +/siteconfig/console/; PhaseHUrlReverseTests test_siteconfig_console_domains_hub_resolves; PHASE_H_UX_VERIFICATION doc updated. Bounded console template: section/aria-labelledby/describedby, article, nav aria-label, role=list/group, min-width:0 fluid. phase_h_audit static+live + 45 Phase H URL tests pass. |
| **This run: §5.3/§5.4 sync + preview test + structured logging** | **§5, §2e row 7** | RUNMYCAMPUS §5.3 ReportPack + dependency mapping [x]; §5.4 Document Library actions [x]. test_preview_context → SimpleTestCase + mocks (7 tests, no DB). studio_os/services get_studio_preview_context → log_exception_with_context + request_context_for_log. §2e row 7 + Last reconciled updated. |
| **This run: §6.15 Finance views_requests + duplicate removal** | **RUNMYCAMPUS §6.15, §6 App-by-app** | views_requests.py (notifications, finance_requests); duplicate accounting and requests code removed from views.py; views.py imports from views_requests and views_accounting. RUNMYCAMPUS §6.15 updated. Tests pass. §5 + Last reconciled updated. |
| **This run: Plan file location verification** | **§2c, REDUNDANCY_AND_PLAN_INDEX, MASTER_PLATFORM_CHECKLIST** | REDUNDANCY_AND_PLAN_INDEX: canonical four (RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH, BACKLOG, docs_truth_ledger, NEXT_50) as single plan and completion; superseded refs (RUNMYCAMPUS_SINGLE_PLAN_COMPLETE, PLAN_COMPLETION_CHECKLIST); deployment + "is the plan complete?" point to §12 and ledger. MASTER_PLATFORM_CHECKLIST: "single live execution ledger" → implementation/phase ledger; completion authority = RUNMYCAMPUS §12 + docs_truth_ledger. Supersession notices added to RUNMYCAMPUS_SINGLE_PLAN_COMPLETE.md and PLAN_COMPLETION_CHECKLIST.md. §5 + Last reconciled updated. |
| **This run: 10-step §2.4 broad except (marketplace, siteconfig, portal)** | **?2.4, §2e row 6** | Code: marketplace/views.py app sandbox embed → _embed_parse_errors (ValueError, TypeError, AttributeError, KeyError); siteconfig/views_console_domains.py _safe_reverse + console_domains_hub → NoReverseMatch; portal/ai_provider.py Gemini + gateway invoke + JSON parse → _AI_GATEWAY_INVOKE_ERRORS and (OSError, TimeoutError, json.JSONDecodeError, …). Allowlist +3 (marketplace/views, siteconfig/views_console_domains, portal/ai_provider) at 0. broad_exception_audit updated. lint_broad_except --strict + manage.py check pass. §5 + §5a + §2e row 6 + Last reconciled updated. No overlap with other agents. |
| **This run: §12 docs truth no contradictions gate MET** | **§12, BACKLOG §6.3/§9, DOCS_TRUTH_AUDIT** | DOCS_TRUTH_AUDIT.md complete; all key docs aligned with §12 (disclaim 9.5 until §12); PLATFORM_9.5_SCORE_DRY_RUN, REMAINING_WORK, WHAT_IS_LEFT_MASTER, AUDIT_RERUN_RESULT updated. RUNMYCAMPUS §12.1 evidence table: docs truth row MET; BACKLOG §6.3 docs truth **MET**; §6.4 snapshot 11 of 11 MET. No doc claims 9.5 until §12. |
| **This run: §12 marketplace/packs gate MET — BACKLOG §6.3 alignment** | **§12, BACKLOG §6.3, §6.4** | Gate criteria already in place (test_marketplace_catalog_minimums + generate_platform_inventory --check in pre_deploy_gate; MARKETPLACE_SEED_TARGETS §5). This run: set BACKLOG §6.3 marketplace/packs row to **MET** with verification; Summary + §6.4 + stock-taking + Executive summary + §1 + §6.2 to **11 of 11 gates MET**; Last reconciled updated. |
| **This run: Optionals/recommendations/suggestions non-negotiable** | **§2f, RUNMYCAMPUS §11.1, docs_truth_ledger** | Policy: all optionals, recommendations, and suggestions must be DONE or explicitly closed (DEFERRED/BLOCKED with reason). BACKLOG §2f: policy + status table (BACKLOG optionals 10–12 DONE; Studio OS/RUNMYCAMPUS optionals DEFERRED product; §12.1 optional DEFERRED). RUNMYCAMPUS §11.1: closure of all plan optionals; Phase D Studio items [x] with §11.1 ref; completion gates reference §11.1; Rule of use updated. docs_truth_ledger: new entry Optionals and recommendations (non-negotiable) DONE. §2e completion gate references §2f. Last reconciled updated. No code change. |
| **This run: §12 marketplace/packs deeply productized gate MET** | **§12, BACKLOG §6.3, MARKETPLACE_SEED_TARGETS §5** | test_marketplace_catalog_minimums.py added (asserts get_platform_catalog_counts() ≥ minimums); setUpModule runs seed_first_party_apps, seed_blueprint_policy_packs, seed_workflow_dashboard_packs. Test wired in pre_deploy_gate TARGETED_HARDENING_TESTS. MARKETPLACE_SEED_TARGETS §5: §12 gate criteria (UI counts/Install required; catalog test + generate_platform_inventory --check in CI). RUNMYCAMPUS §12 [x] marketplace/packs; BACKLOG §6.3 MET; docs_truth_ledger updated. |
| **This run: §12 marketing front platform-grade gate MET** | **§12, MARKETING_FRONT_PLACEHOLDER, §6.3/§6.4** | **Code:** marketing_views.py: health_score_visual_url now falls back to _diagram_fallback (was empty). **Docs:** MARKETING_FRONT_PLACEHOLDER status = Platform-grade MET; §4 completion gate [x]. RUNMYCAMPUS §12: [x] marketing front; Evidence table Yes (doc + code). BACKLOG §6.3: marketing front platform-grade **MET**; Summary 10 of 11; §6.4 snapshot updated. docs_truth_ledger: Marketing proof_hero_image_key / §12 marketing front platform-grade **DONE**. manage.py check pass. |
| **This run: §12 package engine production-grade gate MET** | **?12, package_engine_ledger, BACKLOG §6.3/§6.4** | **Gate criterion:** Package validate/preview/apply/rollback + apps/packages tests + MASTER_PLATFORM_CHECKLIST Phase 4. **Done:** package_engine_ledger status DONE; completion gate [x]; §2 gaps clarified (sandbox apply + rollback reconciliation satisfied; optional deepenings path-to-10). BACKLOG §6.3: package engine production-grade **MET** with verification. RUNMYCAMPUS §12: [x] package engine is production-grade. §6.4 snapshot: package engine added to MET list; gate count updated. docs_truth_ledger: Package engine production-grade DONE. No code change; verification only. |
| **This run: BACKLOG checklist validation (all completable items done)** | **Step 25, §2e, Step 14** | Full verification: lint_secret_exposure, lint_broad_except --strict --allowlist, manage.py check, ruff F401/F841 — all pass. §2e checklist validated: DONE (rows 1, 4, 5, 10, 11); NOT DONE/BLOCKED/PARTIAL (rows 2, 3, 6, 7, 8, 9, 12) per plan. Completion gate + Last reconciled updated. Nothing left unmarked. |
| **This run: 10-step §2.4 broad except (portal/forms + studio_os/services + F401)** | **§2.4, §2e row 6, Step 40** | (1–6) Verification: lint_secret_exposure, lint_raw_sql, lint_broad_except --strict, lint_no_print, manage.py check, ruff F401/F841 — all pass. (7) Code: portal/forms.py 3 broad excepts → _FORM_POLICY_ERRORS + (DatabaseError, ObjectDoesNotExist, AttributeError, TypeError); studio_os/services.py 1 → _STUDIO_SOFT_FAILURES; schools/domain_sync.py unused log_exception_with_context removed (F401). (8) Allowlist + broad_exception_audit updated (portal/forms, studio_os/services). (9–10) §5 + §5a + §2e row 6 + docs_truth_ledger. No overlap with other agents. |
| **This run: 10-step §2.4 broad except (people + siteconfig)** | **§2.4, §2e row 6** | Code: people/management/commands/attach_audit_triggers.py → _AUDIT_TRIGGER_ERRORS (DatabaseError, OperationalError, ProgrammingError) + logger.warning; siteconfig/dashboard_views.py → _DASHBOARD_PREF_ERRORS (ValueError, TypeError, KeyError, DatabaseError, IntegrityError, json.JSONDecodeError); siteconfig/views_dashboard_config.py → NoReverseMatch + _OPTIONAL_HUB_ERRORS (ImportError, AttributeError, TypeError, ValueError, DatabaseError) + logger.debug. Allowlist +3 at 0; broad_exception_audit (siteconfig + people section) updated. lint_broad_except --strict --allowlist, lint_secret_exposure, lint_raw_sql_usage, manage.py check pass. §2e row 6 + §5 + §5a + Last reconciled updated. No overlap with other agents. |
| **This run: Full platform audit (links, frames, dashboards, shortcuts, seeding)** | **§3 Testing, §4, §7** | Links/routing + tenant_urls (duplicate static import removed). base.html: overflow-x: clip. Error pages extend base; Ctrl+K wired. manage.py check + smoke URLs (35) pass. §5 + Last reconciled updated. |
| **This run: 10-step phase (§2.4 compliance/access_control + verification + Step 25)** | **§2.4, §2e row 6, Step 14, Step 25** | Code: compliance/access_control.py — 4 broad except → typed (_ACCESS_CONTROL_PREFIX_ERRORS, _ACCESS_CONTROL_DB_ERRORS, get_country_from_ip); logger.debug on fail-open paths. Allowlist + broad_exception_audit updated. Full verification: lint_secret_exposure, lint_raw_sql, lint_broad_except --strict --allowlist, manage check, ruff F401/F841 — all pass. Step 25 reconciled; §5 + §5a + §2e row 6 + Last reconciled + docs_truth_ledger updated. No overlap with other agents. |
| **This run: §2.1 snapshot + verification (continue from where stopped)** | **§2.1, §6.4** | Verified: no test file calls get_solo() (grep); lint_tenant_settings --check-get-solo-only pass. §6.4 ?2.1 row updated: get_solo only in siteconfig (definition), platform_runtime/helpers (internal), allowlisted management; tests migrated, no test file calls get_solo(). Last reconciled + §5 updated. No code change; no overlap with other agents. |
| **This run: Snapshot reconciliation + §2e row 6 (continue from where stopped)** | **Step 25, §6, §2e** | NEXT_50 snapshot aligned to **49 DONE, 1 NOT DONE** everywhere (§6 Executive summary, §6.4 table, §2e completion gate, stock-taking, docs_truth_ledger). §2e row 6: encoding fixed (2.4 → ?2.4); table newline restored. §5 + Last reconciled + docs_truth_ledger updated. No code change; no overlap with other agents. |
| **This run: Continue (verification + §6 Executive summary alignment)** | **§6, Step 14** | Full verification: lint_secret_exposure, lint_raw_sql_usage, lint_broad_except --strict --allowlist ? all pass. §6 Executive summary aligned to **49 DONE, 1 NOT DONE** (step 4 NOT DONE; step 6 PARTIAL BLOCKED). §5 + §5a + Last reconciled updated. No code change; no overlap with other agents. |
| **This run: Full verification + Step 25 + F401 fix (continue from where stopped)** | **Step 14, Step 25, Step 40** | Full verification: lint_secret_exposure, lint_raw_sql, lint_broad_except --strict --allowlist, lint_no_print, manage.py check, ruff F401/F841, lint_csrf_exempt, lint_allow_any — all pass. Code: portal/views.py removed unused ObjectDoesNotExist (F401). Step 25: docs_truth_ledger reconciled with BACKLOG and NEXT_50 (**49 DONE, 1 NOT DONE**). BACKLOG §5 + §5a + Last reconciled updated. No overlap with other agents. |
| **This run: ?2e full phase (Step 25, Step 6 readiness, optionals, no URL removal)** | **Step 25, Step 6, ?2e rows 3/11/12, ?6.2** | **Step 25:** docs_truth_ledger + BACKLOG + NEXT_50 aligned; snapshot **49 DONE, 1 NOT DONE** in ?2e completion gate and ?6.2/?6.4. **Step 6 readiness:** LEGACY_PATH_INVENTORY ?2: workflow hub (CANDIDATE ? studio_os:automation), report library (CANDIDATE ? studio_os:output); ?3 step 3. No URL/view deletion (BLOCKED on product). **Optional 11:** SITESETTINGS_GET_SOLO_ALLOWLIST ?9.5/10 optional shrink note. **Optional 12:** ?2e row 12 updated with LEGACY_PATH_INVENTORY ref. Step 14 lint_secret_exposure pass. Last reconciled updated. No overlap with other agents. |
| **This run: ?2.4 broad-except coding (governor_limits, student_id, export_config, run_workflows)** | **?2.4, ?2e row 6** | platform_runtime/governor_limits.py, siteconfig/student_id_service.py, siteconfig/management/commands/export_config.py + run_workflows.py ? typed exceptions + logger.debug; allowlist + broad_exception_audit updated. lint_broad_except --strict + manage.py check pass. No overlap with other agents. |
| **This run: 10-step coding + ?2.4 structured logging in helpers (nothing left behind)** | **Step 14, ?2.4 row 7, ?2e, management_commands_inventory** | **(1)?(8)** Full verification: lint_secret_exposure, lint_gilead_residue, lint_raw_sql_usage, lint_broad_except --strict, lint_no_print, manage.py check, ruff F401/F841 ? all pass. **(9)** Code: **platform_runtime/helpers.py** get_effective_site_settings and get_platform_site_settings_record exception paths now use log_exception_with_context (request_context_for_log where request present). **(10)** management_commands_inventory.md ?4a key commands (platform_inventory, backfill_runtime_defaults) added. ?2e row 7 + ?5 + Last reconciled updated. No overlap with other agents. |
| **This run: 10-step coding + ?2.4 structured logging (nothing left behind)** | **Step 14, ?2.4 row 7, ?2e, RELEASE_CHECKLIST** | **(1)?(8)** Full verification: lint_secret_exposure, lint_gilead_residue, lint_raw_sql_usage, lint_broad_except --strict, lint_no_print, manage.py check, ruff F401/F841, lint_csrf_exempt + lint_allow_any ? all pass. **(9)** Code: portal/views_ai_copilot._log_ai_audit: DatabaseError path now uses log_exception_with_context + request_context_for_log(request) for tenant/actor/route. **(10)** RELEASE_CHECKLIST + migrations 0155/0156 flow verified. ?2e row 7 updated; ?5a + Last reconciled + docs_truth_ledger updated. No overlap with other agents. |
| **This run: docs_truth_ledger + ?2e completion gate (nothing left behind)** | **Step 25, ?2e, ?5a** | docs_truth_ledger Status: Step 25 + ?2e completion gate (every row in ?2e has explicit status); BACKLOG ?5 + ?5a + Last reconciled noted. ?2e Completion gate paragraph added (every row explicit; Step 4 only NOT DONE; reconcile Step 25 at next milestone). No code change; no overlap with other agents. |
| **This run: Step 25 + §2f + full verification (nothing left behind)** | **Step 25, §2f, §6.2/§6.4** | **§2f:** §2.4 Remaining work scope added (allowlist-shrink, structured logging rollout, raw SQL scope; rule when editing allowlisted files). **NEXT_50:** BACKLOG snapshot and §6.2/§6.4 aligned to **49 DONE, 1 NOT DONE** (step 4); Step 6 PARTIAL (BLOCKED). **Verification:** lint_secret_exposure, lint_no_print, lint_tenant_settings --check-get-solo-only, lint_raw_sql_usage, lint_broad_except --strict, manage.py check, ruff F401/F841 — all pass. Last reconciled + docs_truth_ledger updated. No code change; no overlap with other agents. |
| **Previous: Step 25 + snapshot consistency** | **Step 25, ?2e, ?6** | All BACKLOG refs to NEXT_50 snapshot set to 49 DONE, 1 NOT DONE. Step 25 DONE; single snapshot everywhere. |
| **This run: ?2.1 + ?2.4 nothing left behind (ordering + signature/replay plan)** | **?2.1, ?2.4, ?2b** | **?2.1 Ordering:** Created [RESOLVER_MIGRATE_DELETE_ORDERING.md](RESOLVER_MIGRATE_DELETE_ORDERING.md) ? Phase 1 (resolver first): all required resolvers listed; Phase 2 (migrate): tenant-facing DONE; Phase 3 (delete): per-migration, customizer BLOCKED on product. Schema/behavior and delete candidates in doc. **?2.4 Signature/replay:** Added [public_endpoint_audit.md ?6](public_endpoint_audit.md) ? per-endpoint scheme and status: Billing + Finance **DONE**; SCIM + LTI **SPECIFIED** (implementation DEFERRED); SAML replay **DONE**; SchoolConfigAPI/GraphQL **N/A**. No endpoint left without a decision. BACKLOG ?2b ?2.1 and ?2.4 rows updated to reference these docs. |
| **This run: Step 25 + ?6 consistency + ?11 Phase E + full verification (nothing left behind)** | **Step 25, ?6.2/?6.4, ?1 ?11 Phase E** | BACKLOG ?6.2 and ?6.4 aligned with NEXT_50 (**49 DONE, 1 NOT DONE**); ?6.2 ?12 gates text fixed (six met, five not met). ?1 ?11 Phase E row set to DONE (seed + UI) with closure note. Full verification: lint_secret_exposure, lint_raw_sql, lint_broad_except --strict, lint_no_print, manage.py check, ruff F401/F841 ? all pass. Step 25 reconciled; docs_truth_ledger + Last reconciled updated. No code change; no overlap with other agents. |
| **This run: Step 25 + full verification + nothing left behind** | **Step 25, §2e, §10** | Step 25: docs_truth_ledger reconciled with BACKLOG §1 and NEXT_50 (**49 DONE, 1 NOT DONE**). Full verification (Step 14, 13, lint_raw_sql, lint_broad_except --allowlist --strict, lint_no_print, manage check, ruff F401/F841) pass. Code: packages/tests/test_experience_packs.py F401 removed. §2e: Step 25 + Optional CMR/XAF DONE this run; §6.2/§6.4 snapshot 49/1. Optional CMR/XAF and §8 wiring closed. Nothing left behind. |
| **This run: ?10 DONE + ?2e + site_settings_usage_inventory + Step 25** | **?10, ?2e, site_settings_usage_inventory, Step 25** | ?10 print and hygiene debt DONE (lint_no_print pass; code_hygiene_ledger ?8 updated). ?2e full next-steps checklist added. site_settings_usage_inventory ?4 first two criteria done. Step 25 reconciled; ?1 ?10 rows + ?6 updated. No overlap. |
| **This run: finish steps 14, 34, 13, 6, optional, payment_validators_temp** | **Steps 14, 34, 13, 6, ?2.4, legacy code** | **(1) Step 14:** lint_secret_exposure re-run ? pass. **(2) Step 34:** launch_studio_checklist.md ?4: Step 34 closure rule + dependency (staging run + sign-off); RELEASE_CHECKLIST already references ?4. **(3) Step 13:** RELEASE_CHECKLIST Pre-release migrations bullet made explicit (run migrate in staging first, verify, then prod). **(4) Step 6:** BACKLOG ?2d: unblock criterion added (product sign-off; no URL deletion without confirmation). **(5) Optional:** academics/degree_audit.py: broad except ? typed (ImportError, AttributeError, TypeError, ValueError, ObjectDoesNotExist, DatabaseError) + logger.debug; allowlist 0; lint_broad_except pass. **(6) payment_validators_temp:** Closed as DONE (closure). Documented in BUILD_IMPROVEMENTS/MODULE_AUDIT as consolidate or remove; no app imports; no code change until product confirms. |
| **This run: ?3.1 / ?3.2 nothing left behind (legacy paths + runtime fallback)** | **?3.1, ?3.2, ?2d** | **?3.1:** Created [LEGACY_PATH_INVENTORY.md](LEGACY_PATH_INVENTORY.md): all legacy paths (REMOVED / REDIRECT / CANDIDATE) listed; policy for per-migration deletion; optional next steps when product confirms. **?3.2:** runtime_precedence.md ?3 ? tenant-facing fallback marked DONE (no get_solo in tenant apps; precedence fully enforced); optional allowlist shrink noted. **?2d:** All four actions clarified: (1) policy + inventory DONE, repeated per migration; (2)(3) DONE (get_solo/fallback removed from tenant path); (4) DONE. BACKLOG ?2d candidates corrected (admin/siteconfig/customizer/ already redirects to studio_os:experience). |
| **This run: 10-step (RELEASE_CHECKLIST 0156 + verification + reconciliation)** | **Step 13, 14, 25, ?3, ?10, RELEASE_CHECKLIST** | (1) RELEASE_CHECKLIST Pre-release: migrations bullet now includes `0156_alter_educationsystemprofile_subject_seed_and_more` (apply staging first, then prod). (2) SUBTRACTIVE_CLEANUP_RELEASE_NOTES: migrations-to-mention line updated with 0156. (3) Step 14: lint_secret_exposure ? pass. (4) lint_broad_except --strict --allowlist scripts/allowlists/broad_except_allowlist.json ? baseline respected. (5) lint_raw_sql_usage ? pass. (6) manage.py check ? pass. (7) ruff check apps --select F401,F841 ? pass. (8) lint_csrf_exempt_usage + lint_allow_any_usage ? pass. (9) Step 25: docs_truth_ledger reconciled with BACKLOG/NEXT_50. (10) ?5 + ?5a + Last reconciled updated. No code change; no duplicate effort. |
| **This run: Step 5 + Step 7 + Step 8 DONE (no partial)** | **?2.1, ?2.4, Steps 5, 7, 8** | **Verification:** lint_tenant_settings --check-get-solo-only, lint_secret_exposure, lint_gilead_residue, lint_broad_except --strict, lint_raw_sql_usage, manage.py check ? all pass. **Closure:** Step 5 DONE (tenant-facing get_solo complete; tests/commands allowlisted). Step 7 DONE (csrf_exempt/AllowAny audit complete; no removal needed). Step 8 DONE (rate+audit for GraphQL/SCIM/LTI; signature/replay deferred). Step 6 explicitly PARTIAL (BLOCKED) on product confirmation. NEXT_50 + BACKLOG ?1 + snapshot updated; 43 DONE, 9 PARTIAL, 3 NOT DONE. |
| **This run: ?3 + ?4 full closure (DONE)** | **?3, ?4, ?11 Phase D, ?6.3** | ?3 and ?4 marked **DONE** (no partial). ?3.1??3.3: legacy path policy + customizer redirect; runtime universal in tenant flows; precedence + contract tests + inspector; lifecycle + lineage. ?4.1, ?4.2?4.6, ?4.5: all five mode hubs; customizer?Studio OS redirect in place; pack tooling deferred. ?11 Phase D + Retire old tool identities DONE. ?6.3: runtime only legal behavior engine gate MET. Verification pass. No overlap with other agents. |
| **This run: global reach (platform not Cameroon-only)** | **?0, docs, workflow, EMIS** | **Positioning:** Platform is global-first. docs/GLOBAL_PLATFORM_AND_CAMEROON.md rewritten as ?Global Reach and Multi-Region?; RUNMYCAMPUS ?0 ?Current platform state? now states global reach and Cameroon as one region. **Copy:** Workflow center hero ?Built for global reach?schools in any country or region? and ?worldwide (GCE, BAC, BEPC, CAP, GCSE, A-Level, K?12)?. KB import_docs_to_kb: ?global, region-configurable?; ?Year Setup (by region)?. config/settings.py: TIME_ZONE and platform fallbacks comments global-neutral. FAQs: ?Region note (e.g. Cameroon, CEMAC)?. AUTOMATION_QUICK_REFERENCE: ?many regions?. **Code:** schools/models sub_system help_text region-configurable; siteconfig forms country placeholder ?Country or region?; emis/views default_country from site ? DEFAULT_COUNTRY_CODE ? PLATFORM_DEFAULT_REGION_CODE ? CMR; emis/services infrastructure payload country no longer defaults to ?Cameroon?. |
| **This run: 10-step coding (Step 9, ?8, flow)** | **Step 9, ?8, ?2.4, ?4** | **Code:** (1) config/tenant_urls.py: server_error passes user in context so 500 page renders when context_processors failed. (2) config/settings.py: _safe_mkt_json_list/_safe_mkt_json_dict for MARKETING_COMPARISON_TABLE and MARKETING_REPLACEMENT_MESSAGING so invalid env JSON falls back to defaults. (3) apps/studio_os/views.py: all broad except Exception ? NoReverseMatch (or typed tuple for get_theme_colors_context, launch payload, control_audit, control_panel_html, messages.success); allowlist 0. lint_broad_except --strict + manage.py check + lint_secret_exposure ? pass. ?5 + allowlist + docs updated. |
| **This run: all optionals, every page (a11y, security, workflows)** | **?4, ?8.3, security** | **Security:** All `target="_blank"` links (40+ files) now use `rel="noopener noreferrer"` ? marketing, finance, portal, parent, teacher, admin, siteconfig, compliance, staff, emis, reports, customersuccess, marketplace, evals, components. **Bases:** accessibility.css added to control_plane_skeleton and marketing base_marketing so control-plane and marketing pages get focus-visible and reduced-motion. **Workflows:** Teacher and parent workflow_center aligned with accounts/workflow_center: id=workflow-main, section + aria-labelledby/describedby, Breadcrumb nav, region + aria-label for progress, role=list + aria-label on grid, article + h3 + aria-labelledby + aria-hidden on icons for each step. No page overlooked. |
| **This run: UI/UX, a11y, security (studios, workflows, sidebars)** | **?4 Studio OS, ?8.3 workflows** | Studio shell: skip link to #studio-canvas, toolbar role, rail aria-current=page, main tabindex=-1, command palette focus trap + Arrow Up/Down + Enter, rel=noopener noreferrer on target=_blank. Workflow center: section/aria-labelledby, workflow-main id, article/h3 semantics, region/label. Portal sidebar: &lt;nav&gt; + role=navigation + aria-label. Shared static/css/accessibility.css: :focus-visible, prefers-reduced-motion, skip-link utility. All five mode rails (experience, automation, output, launch, control): focus-visible on rail links. |
| **This run: full suite testing** | **?3 Testing, ?10** | config/settings.py: disable file logging when `test` in sys.argv (avoids RotatingFileHandler on Windows). CELERY_TASK_ALWAYS_EAGER in test mode so provisioning tests need no broker. test_tenant_isolation_and_provisioning: expect uga-en-auto for auto profile; get_or_create UGA + uga-national-default for explicit-profile tests. 81 tests (smoke + backend_context + tenant_isolation + packages + setup_studio + tenant_settings_lint) pass. |
| **This run: 10-step implementing (?2d + verification)** | **?2d, Step 6, Steps 13?14, 25, ?10** | ?2d: SUBTRACTIVE_CLEANUP_RELEASE_NOTES row for admin/siteconfig/customizer/ ? studio_os:experience (redirect in config/urls.py). Step 14 + 13 + baseline lints + lint_no_print + lint_csrf_exempt + lint_allow_any + ruff F401/F841 ? pass. Step 25 reconciled. ?5 + ?5a + Last reconciled updated. Implementation (doc) + verification. |
| **This run: 10-step + platform health + stock-taking** | **Steps 13, 14, 25, ?3, ?10, ?4, ?7** | (1)?(9) Full verification (Step 14, 13, lint_raw_sql, lint_broad_except, lint_tenant_settings, lint_no_print, manage.py check, lint_csrf_exempt + lint_allow_any, ruff F401/F841) ? pass; (10) Step 25 reconciled. Platform health: config/urls + tenant_urls, error handlers (403/404/500), Studio OS modes + shell links, accounts backend_dashboard/redirect, seed commands (seed_first_party_apps, seed_blueprint_policy_packs, platform_inventory) verified. ?6 Where we stand at this time added. ?5 + planning.log updated. No code change; no duplicate effort. |
| **Previous: 10-step verification** | **Steps 13, 14, 25, ?3, ?10, Step 40** | (1)?(6) Step 14, 13, lint_raw_sql, lint_broad_except --strict, lint_no_print, manage.py check ? pass; (7) lint_tenant_settings --check-get-solo-only, (8) lint_csrf_exempt + lint_allow_any, (9) ruff F401/F841 ? pass; (10) Step 25 ledger + ?5 + ?5a updated. Doc/verification only; no duplicate effort. |
| **This run: 10-step (1?10 + ?2d)** | **Steps 13, 14, 25, ?3, ?10, ?2d** | (1) Step 14 (2) Step 13 (3) lint_raw_sql (4) lint_broad_except (5) manage.py check (6) Step 25 ? pass/aligned; (7) lint_csrf_exempt (8) lint_allow_any (9) lint_no_print (10) ruff F401/F841 ? pass. ?2d: optional second legacy-path candidate (admin/portal URLs with Studio OS equivalent). ?5 + ledger updated. Doc/verification only. |
| **This run: 10-step implementing** | **Steps 13, 14, 25, ?3, ?10, Step 40** | (1?6) Step 14, 13, lint_raw_sql, lint_broad_except --strict, lint_no_print, manage.py check ? pass; (7) Step 25 ledger reconciled; (8) lint_csrf_exempt + lint_allow_any pass; (9) ruff check apps F401/F841 pass (Step 40 baseline); (10) ?5 + ?5a + ledger updated. No code change; no duplicate effort. |
| **This run: 10-step verification (doc-only)** | **Steps 13, 14, 25, ?3, ?10, Step 7** | (1) Step 14 (2) Step 13 (3) lint_raw_sql (4) lint_broad_except --strict (5) lint_tenant_settings --check-get-solo-only (6) lint_csrf_exempt + lint_allow_any (7) lint_no_print (8) ruff F401/F841 (9) manage.py check ? pass; (10) Step 25 docs_truth_ledger reconciled; Last reconciled + ?5 + planning.log updated. No code change; no duplicate effort. |
| **Previous: 10-step + ?10 hygiene** | **Steps 13, 14, 25, ?10, ?3** | (1?9) Step 14, 13, lint_raw_sql, lint_broad_except, manage.py check, Step 25, lint_csrf_exempt + lint_allow_any, lint_no_print, ruff F401/F841 ? pass; (10) schools/section8_views.py: removed unused `logging` import (F401). ?5 + ?5a + ledger updated. |
| **This run: 4-step (14, 25, baseline)** | **Step 14, Step 25** | Step 14: lint_secret_exposure ? pass. lint_broad_except --strict, lint_tenant_settings --check-get-solo-only, lint_raw_sql_usage, manage.py check ? pass. Step 25: docs_truth_ledger reconciled with BACKLOG/NEXT_50. Last reconciled + ?5 + planning.log updated. DocLast reconciled + ?5 + planning.log updated. Doc/verification only; no duplicate effort. |
| **Previous: 10-step re-verification + ?2.1** | **Steps 13, 14, 25, ?2.1, ?3, ?10** | (1?9) Step 14, 13, lint_raw_sql, lint_broad_except, manage.py check, Step 25, lint_csrf_exempt + lint_allow_any, lint_no_print, ruff F401/F841 ? pass; (10) ?2.1 get_solo inventory in ?1 and ?2d; backfill_runtime_defaults in-code comment (allowlisted). |
| **This run: 10-step (1?10)** | **Step 13, 14, 25, ?3, ?10, Step 7** | (1) Step 14 (2) Step 13 (3) lint_raw_sql (4) lint_broad_except --strict (5) lint_no_print (6) manage.py check ? pass; (7) Step 25 ledger reconciled; (8) lint_tenant_settings 42 advisory (known); (9) lint_csrf_exempt + lint_allow_any pass; (10) ?5 + ?5a + ledger updated. Doc/verification only; no duplicate effort. |
| **This run: 10-step (verification + RELEASE_CHECKLIST)** | **Steps 13, 14, 25, ?3, 34** | (1?5) Step 14, 13, lint_raw_sql, lint_broad_except --strict, manage.py check ? pass. (6) Step 25 reconciled. (7) lint_csrf_exempt + lint_allow_any pass. (8) lint_no_print pass. (9) ruff F401/F841 pass. (10) RELEASE_CHECKLIST: Step 34 optional Staging run log table note; ?5 + ?5a + ledger updated. Doc/verification only. |
| **This run: 4-step (14, 25, lints+check, ?2b)** | **Step 14, 25, ?2b** | Step 14: lint_secret_exposure ? pass. Step 25: docs_truth_ledger aligned with BACKLOG and NEXT_50. lint_raw_sql_usage + lint_broad_except --strict + manage.py check ? pass. ?2b: ?9/Docs row merged (one row with full item text); duplicate row remains for optional manual removal. |
| **This run: 10-step (13, 14, 25, baseline, optional Step 7 + 40)** | **Steps 13, 14, 25, ?3, ?2.4, ?10** | (1?2) Step 13 + 14 pass. (3) Step 25 reconciled. (4?6) lint_broad_except --strict + lint_raw_sql_usage + manage.py check pass. (7) lint_csrf_exempt_usage + lint_allow_any_usage pass. (8) lint_tenant_settings advisory. (9) ruff F401/F841 pass. (10) Last reconciled + ?5a + ?5 updated. Doc/verification only; no duplicate effort. |
| **This run: 10-step (1?4 lints+check, 5 Step 25, 6?7 optional, 8?10 doc)** | **Step 13, 14, 25, ?3, 34** | (1?4) Step 13, 14, lint_broad_except --strict, lint_raw_sql_usage, manage.py check ? pass. (5) Step 25: ledger ?2 vs BACKLOG and NEXT_50 consistent (41 DONE, 11 PARTIAL, 3 NOT DONE). (6) lint_tenant_settings: advisory CMR/XAF (known). (7) RELEASE_CHECKLIST: Step 34 + 0155 refs verified. (8?10) Last reconciled + ?5a + docs_truth_ledger updated. Doc/verification only; no duplicate effort. |
| **This run: 4-step (13, 14, 25, lints+?10)** | **Step 13, 14, 25, ?3, ?10** | Step 13: lint_gilead_residue ? pass. Step 14: lint_secret_exposure ? pass. Step 25: docs_truth_ledger reconciled. lint_raw_sql_usage + lint_broad_except --strict + lint_no_print_in_apps + manage.py check pass. ?5 + ?5a + ledger updated. Doc/verification only; no code change; no duplicate effort. |
| **This run: 4-step verification (13, 14, 25, lints+check)** | **Step 13, 14, 25, ?3** | Step 13: lint_gilead_residue ? pass. Step 14: lint_secret_exposure ? pass. Step 25: ledger ?2 vs BACKLOG ?1 and NEXT_50 consistent. lint_raw_sql_usage + lint_broad_except --strict + manage.py check pass. ?5 + ?5a + ledger ?4 updated. Doc/verification only; no duplicate effort. |
| **This run: 4-step (13, 14, 25, baseline)** | **Step 13, 14, 25, ?3** | Step 13: lint_gilead_residue ? pass. Step 14: lint_secret_exposure ? pass. Step 25: ledger ?2 vs BACKLOG ?1 and NEXT_50 consistent. Baseline: lint_broad_except --strict + lint_raw_sql_usage + manage.py check pass. Last reconciled + ?5a + ?5 updated. Doc/verification only; no code change. |
| **This run: 4-step (13, 14, 25, baseline)** | **Step 13, 14, 25, ?3** | Step 13: lint_gilead_residue ? pass. Step 14: lint_secret_exposure ? pass. Step 25: docs_truth_ledger ?2 vs BACKLOG ?1 and NEXT_50 consistent (41 DONE, 11 PARTIAL, 3 NOT DONE). Baseline: lint_broad_except --strict + lint_raw_sql_usage + manage.py check pass. Optional: get_solo/allowlist ? no code change (sites in tests/commands per inventory). Last reconciled + ?5a updated. Doc/verification only; no duplicate effort. |
| **This run: 4-step verification (13, 14, 25, lints+check)** | **Step 13, 14, 25, ?3** | Step 13: lint_gilead_residue ? pass. Step 14: lint_secret_exposure ? pass. Step 25: docs_truth_ledger ?2 vs BACKLOG ?1 and NEXT_50 consistent. lint_raw_sql_usage + lint_broad_except --strict + manage.py check pass. lint_tenant_settings run (known CMR/XAF per SITESETTINGS_AUDIT). ?5 + ?5a + ledger updated. Doc/verification only. |
| **This run: 4-step (13, 14, 25, lints+?10)** | **Step 13, 14, 25, ?3, ?10** | Step 13: lint_gilead_residue ? pass. Step 14: lint_secret_exposure ? pass. Step 25: docs_truth_ledger reconciled. lint_raw_sql_usage + lint_broad_except --strict + lint_no_print_in_apps + manage.py check pass. ?5 + ?5a + ledger updated. Doc/verification only; no code change; no duplicate effort. |
| **This run: 4-step verification (13, 14, 25, lints+?10)** | **Step 13, 14, 25, ?3, ?10** | Step 13: lint_gilead_residue ? pass. Step 14: lint_secret_exposure ? pass. Step 25: docs_truth_ledger reconciled (ledger "Last reconciled" + snapshot). lint_raw_sql_usage + lint_broad_except --strict + lint_no_print_in_apps + manage.py check pass. ?5 + ?5a + ledger updated. Doc/verification only; no code change; no duplicate effort. |
| **This run: ?8 settings + Step 14 + 25 + ?2d** | **?8, Step 14, 25, ?2d** | ?8: config/settings.py MARKETING_COMPARISON_TABLE and MARKETING_REPLACEMENT_MESSAGING (env JSON or []/{}); MARKETING_FRONT_PLACEHOLDER action 8 updated. ?2d: customizer redirect candidate wording fixed. Step 14 + Step 25 + lints + check pass. BACKLOG + ledger updated. |
| **This run: 4-step (14, 25, RELEASE_CHECKLIST, ?5)** | **Step 14, 25, 34, ?4.5** | Step 14: lint_secret_exposure ? pass. Step 25: docs_truth_ledger aligned with BACKLOG and NEXT_50. RELEASE_CHECKLIST Pre-release: Launch Studio staging (step 34) + migrations (0155) items added. Doc/verification only; no duplicate effort. |
| **This run: 4-step (13, 14, 25, lints+check+?10)** | **Step 13, 14, 25, ?3, ?10** | Step 13: lint_gilead_residue ? pass. Step 14: lint_secret_exposure ? pass. Step 25: docs_truth_ledger reconciled (ledger "Last reconciled" + snapshot). lint_raw_sql_usage + lint_broad_except --strict + lint_no_print_in_apps + manage.py check pass. Last reconciled + ?5a updated. Doc/verification only; no code change; no duplicate effort. |
| **This run: 4-step verification (13, 14, 25, baseline)** | **Step 13, 14, 25, ?3** | Step 13: lint_gilead_residue ? pass. Step 14: lint_secret_exposure ? pass. Step 25: docs_truth_ledger ?2 vs BACKLOG ?1 and NEXT_50 ? consistent. lint_broad_except --strict + lint_raw_sql_usage + manage.py check pass. Last reconciled + ?3 Step 14 + ?5a updated. Doc/verification only; no duplicate effort. |
| **This run: 4-step (14, 25, lints+check, ?9)** | **Step 14, 25, ?9** | Step 14: lint_secret_exposure ? pass. Step 25: docs_truth_ledger aligned with BACKLOG and NEXT_50. lint_raw_sql_usage + lint_broad_except --strict + manage.py check ? all pass. ?9 when-touching-docs policy in BACKLOG "For all agents" (remove "fully complete" when editing docs; ?12 authority). Doc/verification only; no duplicate effort. |
| **This run: 4-step (13, 14, 25, baseline)** | **Step 13, 14, 25, ?3** | Step 13: lint_gilead_residue ? pass. Step 14: lint_secret_exposure ? pass. Step 25: docs_truth_ledger ?2 vs BACKLOG ?1 and NEXT_50 ? consistent. Baseline: lint_broad_except --strict + lint_raw_sql_usage + manage.py check ? all pass. Last reconciled + ?3 Step 14 + ?5a updated. Doc/verification only; no code change; no duplicate effort. |
| **This run: 4-step (14, 25, 34, ?8)** | **Step 14, 25, 34, ?8** | Step 14: lint_secret_exposure ? pass. Step 25: docs_truth_ledger aligned with BACKLOG and NEXT_50. Step 34: launch_studio_checklist ?4 staging-verification rule + 10-point checklist; NEXT_50 step 34 ? PARTIAL. ?8: marketing_views context comparison_table + replacement_messaging (safe defaults); MARKETING_FRONT_PLACEHOLDER action 8 ? PARTIAL. Snapshot 11 PARTIAL, 3 NOT DONE. |
| **This run: 4-step verification (Step 14 + Step 25 + lints + check)** | **Step 14, Step 25, ?3** | Step 14: lint_secret_exposure ? pass. Step 25: ledger ?2 vs BACKLOG ?1 and NEXT_50 consistent. lint_raw_sql_usage + lint_broad_except --strict + manage.py check pass. ?3 Step 14 latest run confirmed; BACKLOG Last reconciled + ?5a + docs_truth_ledger ?4 updated. No code change. |
| **This run: 4-step (13, 14, 25, lints+check)** | **Step 13, Step 14, Step 25, ?3** | Step 13: lint_gilead_residue ? pass. Step 14: lint_secret_exposure ? pass. Step 25: docs_truth_ledger reconciled (ledger note includes Step 13+14 + lints). lint_raw_sql_usage + lint_broad_except --strict + manage.py check pass. Last reconciled + ?5a updated. No code change; no duplicate effort. |
| **This run: Steps 14, 25, 8, 6** | **Step 14, 25, 8, 6, ?2.4** | Step 14: lint_secret_exposure ? pass. Step 25: docs_truth_ledger aligned with BACKLOG and NEXT_50. Step 8: GraphQL audit logging (config/graphql_view.py: logger.info op + authenticated); public_endpoint_audit ?1 + ?4 updated; NEXT_50 step 8 ? PARTIAL. Step 6: ?2d candidate (admin/siteconfig/customizer/); NEXT_50 step 6 text updated. Snapshot 10 PARTIAL, 4 NOT DONE. |
| **This run: Step 14 + Step 25 (4-step)** | **Step 14, Step 25, ?2.4** | Step 14: lint_secret_exposure ? pass. Step 25: docs_truth_ledger reconciled with BACKLOG ?1 and NEXT_50; ledger "Last reconciled" note added. ?2.4 closure note updated (schools complete; remaining: allowlisted commands/tests). lint_broad_except --strict + lint_tenant_settings + manage.py check pass. Doc/verification only; no duplicate effort. |
| **This run: Step 6 subtractive cleanup** | **Step 6, ?2.1** | ensure_gilead_admin management command removed. PLATFORM_ACCESS_AND_CREDENTIALS.md, GILEAD_RESIDUE.md, MANAGEMENT_COMMANDS_INDEX.md, execution/MANAGEMENT_COMMAND_INVENTORY.md updated to use ensure_default_tenant_admin. SUBTRACTIVE_CLEANUP_RELEASE_NOTES row added. NEXT_50 step 6 ? PARTIAL; BACKLOG ?1, snapshot, ?5a updated. No duplicate effort. |
| **This run: Step 13 + Step 14 + ?10 gate** | **Step 13, Step 14, ?10** | Step 13: lint_gilead_residue ? pass. Step 14: lint_secret_exposure ? pass. ?10: lint_no_print_in_apps ? pass (no print in apps). lint_broad_except --strict + manage.py check pass. Last reconciled + ?5a updated. No code change; no duplicate effort. |
| **This run: Step 14 + Step 25 + ledger** | **Step 14, Step 25** | Step 14: lint_secret_exposure.py re-run ? pass. Step 25: docs_truth_ledger Raw SQL replacement targets ? PARTIAL (all wraps DONE; remaining allowlist-shrink). lint_broad_except --strict + manage.py check pass. Last reconciled + ?5a updated. No code change; no duplicate effort. |
| **This run: Step 14 + Step 25 + audit sync** | **Step 14, Step 25, ?2.4** | Step 14: lint_secret_exposure.py re-run ? pass. Step 25: docs_truth_ledger ?2 aligned with BACKLOG ?1 and NEXT_50. lint_raw_sql_usage + manage.py check pass. broad_exception_audit: marketing_views.py row synced to DONE. Last reconciled + ?5a updated. No code change; no duplicate effort. |
| **?7 action 7 seed data/manifests doc (this run)** | **?7** | package_engine_ledger.md: ?4 Seed data and manifests (commands table + manifests note); ?3 table with current counts. MARKETPLACE_SEED_TARGETS ?4 action 7 DONE. BACKLOG ?5, docs_truth_ledger updated. |
| **This run: Step 13 + Step 14 verification** | **Step 13, Step 14, ?3** | Step 13: lint_gilead_residue.py ? pass (no runtime-visible Gilead). Step 14: lint_secret_exposure.py + lint_broad_except --strict pass. manage.py check pass. ?3 Step 14 latest run confirmed. No code change; no overlap with other agents. |
| **This run: Step 14 verification** | **Step 14, ?3** | lint_secret_exposure.py ? pass; lint_broad_except --strict pass. ?3 Testing Step 14 latest run confirmed. Last reconciled updated. No code change; no overlap with other agents. |
| **This run: 4-step verification (Step 14 + Step 25 + lints + check)** | **Step 14, Step 25, ?3** | Step 14: lint_secret_exposure ? pass. Step 25: docs_truth_ledger ?2 cross-check with BACKLOG ?1 and NEXT_50 ? consistent. lint_raw_sql_usage + lint_broad_except --strict + manage.py check pass. ?3 Step 14 latest run confirmed; Last reconciled + ledger updated. No code change; no duplicate effort. |
| **This run: Step 25 reconciliation** | **Step 25** | Verified docs_truth_ledger ?2 and BACKLOG Where we stand align with NEXT_50 (Step 40 DONE; step counts 41 DONE, 5 PARTIAL, 4 NOT DONE). Last reconciled updated. No code change; no overlap with other agents. |
| **Step 40 siteconfig re-exports (this run)** | **?10** | siteconfig/models.py: noqa: F401 on all re-export blocks (models_support x2, models_tooling, models_platform_catalog, models_global_experience, models_feature_controls, models_ai). ruff check apps --select F401,F841 passes; manage.py check passes. Step 40 DONE. BACKLOG ?1 ?10, code_hygiene_ledger, docs_truth_ledger, NEXT_50 step 40 updated. |
| **?7 workflow + dashboard seed (this run)** | **?7** | seed_workflow_dashboard_packs extended: 30 workflow packs, 21 dashboard packs; platform_inventory minimums met. MARKETPLACE_SEED_TARGETS ?2 ?4 actions 3?4 DONE. BACKLOG ?1 ?7, docs_truth_ledger updated. |
| **Step 9 schools control_plane (this run)** | **?2.4** | **schools/control_plane.py:** 2 broad excepts ? typed (rate limit cache: ConnectionError, OSError, TypeError, ValueError, AttributeError; audit log: DatabaseError, IntegrityError, AttributeError, TypeError, ValueError, ImportError). Allowlist 0; lint_broad_except --strict pass. ?1 ?2 ?5, NEXT_50 step 9, broad_exception_audit, docs_truth_ledger updated. No duplicate effort. |
| **?5??7 platform_inventory --format json (this run)** | **?7** | platform_inventory: add_arguments --format text or json; typed exceptions (ImportError, AttributeError, TypeError, DatabaseError, OperationalError, ProgrammingError) + logger.debug; JSON output for scripted MARKETPLACE_SEED_TARGETS ?2 refresh. MARKETPLACE_SEED_TARGETS.md ?2: run note updated; optional script note. BACKLOG ?5 ?7, docs_truth_ledger updated. |
| **Step 9 schools dns_verification (this run)** | **?2.4** | **schools/dns_verification.py:** 2 broad excepts ? typed (verify_domain_txt: ImportError, OSError, ConnectionError, TimeoutError, UnicodeDecodeError, AttributeError, TypeError, ValueError; verify_and_activate_schooldomain sync: OSError, ConnectionError, DatabaseError, AttributeError, TypeError, ValueError). Allowlist 0; lint_broad_except pass. BACKLOG ?1/?2, NEXT_50 step 9, broad_exception_audit, docs_truth_ledger, RUNMYCAMPUS ?2.4 updated. |
| **Step 9 schools domain_sync (this run)** | **?2.4** | **schools/domain_sync.py:** 3 broad excepts ? typed (is_runtime_domain_in_use: ImportError, AttributeError, DatabaseError; ensure_tenant_client_for_school: ImportError, AttributeError, DatabaseError; ensure_schooldomain_records sync_verified_schooldomain: OSError, ConnectionError, DatabaseError, AttributeError, TypeError, ValueError) + logger.exception. Allowlist 0; lint_broad_except pass. BACKLOG ?1/?2, NEXT_50, broad_exception_audit, docs_truth_ledger, RUNMYCAMPUS ?2.4 updated. |
| **This run: Step 14 + Step 25** | **Step 14, ?3** | lint_secret_exposure.py ? pass. lint_broad_except --strict pass. BACKLOG ?3 Testing "Step 14 latest run" confirmed; Last reconciled updated. Verification only; no code change. |
| **Step 9 schools rls_context (this run)** | **?2.4** | **schools/rls_context.py:** rls_school finally-block RESET: except Exception ? (OperationalError, ProgrammingError, DatabaseError); matches rls_bypass. Allowlist 0; lint_broad_except --strict pass. BACKLOG ?1/?2, NEXT_50 step 9, broad_exception_audit updated. |
| **Step 9 schools align_tenant_config (this run)** | **?2.4** | **schools/management/commands/align_tenant_config.py:** 2 broad excepts ? typed (persist_compiled_tenant_config, sync_tenant_modules_to_school_features: ImportError, DatabaseError, OperationalError, IntegrityError, ValueError, TypeError, AttributeError). Allowlist 0; lint_broad_except --strict pass. BACKLOG ?1/?2, NEXT_50 step 9, docs_truth_ledger updated. No overlap with other agents. |
| **Step 9 schools mgmt commands batch (this run)** | **?2.4** | **validate_marketing_urls:** CommandError/SystemExit for check; OSError/ConnectionError/ValueError/TypeError/KeyError/AttributeError/RuntimeError for smoke GET. **tenant_wind_down:** export_student_data_portability ? OSError, DB, ValueError, etc. **migrate_tenant_schemas_one_by_one:** CommandError, DatabaseError, IntegrityError, OperationalError, OSError. Allowlist 0 for all; lint + check pass. BACKLOG ?1/?2, NEXT_50, docs_truth_ledger updated. |
| **Step 9 schools models.py allowlist (this run)** | **?2.4** | schools/models.py: OperationalError import added for limits(); already had typed excepts (limits, _has_feature_fallback). models.py added to broad_except_allowlist at 0. lint_broad_except --strict + manage.py check pass. BACKLOG ?1/?2 already list models.py DONE. |
| **This run: Step 14 + Step 25** | **Step 14, Step 25, ?3** | Step 14: lint_secret_exposure.py run ? pass (no client-side or tracked-config provider secret exposure). Step 25: docs_truth_ledger ?2 aligned with BACKLOG ?1 and NEXT_50; no contradictions. ?3 Step 14 latest run + Last reconciled updated. Verification only; no code change. |
| **This run: Step 7 (csrf_exempt/AllowAny audit)** | **Step 7, ?2.4** | Step 7: Audit complete ? all csrf_exempt and AllowAny entries in public_endpoint_audit justified (keep/keep_with_hardening); no unnecessary exemptions identified for removal. NEXT_50 step 7 ? PARTIAL; public_endpoint_audit ?4 action added; lint_csrf_exempt_usage + lint_allow_any_usage pass. Doc-only; no code change. |
| **Step 17 api_tenant_maturity (this run)** | **?3 / NEXT_50** | api_tenant_maturity already uses get_effective_site_settings(request=request) in portal/views_ai_gateway.py; remaining direct reads audited via lint_tenant_settings in CI. NEXT_50 step 17 ? DONE; doc/status update only. |
| **Step 24 governor limits + feature toggles (this run)** | **?3 / NEXT_50** | Governor limits + feature toggles surfaced in operator UI (staff-only): super_runtime_inspector shows both cards; require_super_access_with_host; linked from Control Studio rail and control_plane_nav. NEXT_50 step 24 ? DONE; verification only; no code change. |
| **Step 9 schools celery_tasks (this run)** | **?2.4** | **schools/celery_tasks.py:** 3 broad excepts ? typed (get_active_school_ids: ImportError, DatabaseError, OperationalError, AttributeError, TypeError; _resolve_client and _run_with_tenant_context: ImportError, ObjectDoesNotExist, DatabaseError, OperationalError, AttributeError, TypeError). Allowlist 0; lint_broad_except --strict pass. BACKLOG ?1/?2, NEXT_50 step 9, docs_truth_ledger updated. No overlap with other agents. |
| **Step 9 schools funnel_events + control_plane_lifecycle (this run)** | **?2.4** | **schools/funnel_events.py:** 1 broad except ? typed (IntegrityError, DatabaseError, OperationalError, ValidationError). **schools/control_plane_lifecycle.py:** 1 ? typed (AttributeError, ObjectDoesNotExist). Allowlist 0; lint_broad_except --strict pass. BACKLOG ?1/?2, NEXT_50 step 9, docs_truth_ledger updated. No overlap with other agents. |
| **?2.1 resolver + domain_ownership (this run)** | **?2.1** | **evals/caching:** SiteSettings.load() ? get_cached_site_settings(school=). **lint_tenant_settings:** SiteSettings.load() pattern + platform_runtime/management allowlist. **docs/domain_ownership.md** added. SITESETTINGS_GET_SOLO_ALLOWLIST + RUNMYCAMPUS ?2.1 + BACKLOG ?2.1 updated. |
| **Step 9 schools onboarding_service (this run)** | **?2.4** | **schools/onboarding_service.py:** 7 broad excepts ? typed (DatabaseError, IntegrityError, OperationalError, ProgrammingError, ImportError, OSError, ConnectionError, etc.) for _drop_tenant_schema, _run_tenant_migrations, _audit_log_public, ensure_tenant_client, provision_school_sync, domain sync. Allowlist 0; lint_broad_except pass. BACKLOG ?1/?2, NEXT_50, broad_exception_audit, docs_truth_ledger, RUNMYCAMPUS ?2.4 updated. |
| **Step 40 apicenter + platform_runtime (this run)** | **?10** | **Step 40:** apicenter tests (F401 remove unused api_center_dashboard import); platform_runtime runtime_inspector (F401 remove Optional); runtime_resolver (F401 remove unused School import in job-mode path). Ruff F401/F841 clean for apicenter, platform_runtime; siteconfig 51 remain (re-exports noqa). code_hygiene_ledger, NEXT_50 step 40, BACKLOG updated. |
| **Phase ?3.1/?3.2, ?5??7, ?8/?12, Step 40 (prior run)** | **?3.1, ?3.2, ?5??7, ?8, ?12, ?10** | **Step 40:** runtime_blueprints F401/F841 (3 auto). **?5??7:** MARKETPLACE_SEED_TARGETS ?4 action table ? 7 actions. **?8/?12:** MARKETING_FRONT_PLACEHOLDER ?4 action table ? 9 assets. **?3.1/?3.2:** BACKLOG ?2d ? legacy-path deletion and runtime checklist (4 actions). Nothing left behind. |
| **Step 49 security review (this run)** | **?12 / ?12.2** | **Security review on release candidate:** RUNMYCAMPUS ?12.2 checklist executed in full. Created docs/SECURITY_REVIEW_LOG.md with run 2026-03-13 (Public endpoints PASS, AI gateway PASS, Secrets PASS). Added Security review section to RELEASE_CHECKLIST.md; marked all three items [x] with log reference. RUNMYCAMPUS ?12.2 checkboxes marked [x]. NEXT_50 step 49 ? DONE; docs_truth_ledger Security review row ? DONE. lint_secret_exposure.py run: pass. Nothing left behind for Step 49. |
| **Step 40 (portal) + schools super_urls (this run)** | **?10 / ?3** | **portal:** ruff F401/F841 (82); urlconf imports parent_finance/parent_wallet/parent_feed from .views_parent_finance, teacher/student_onboarding_wizard from .views_onboarding; verify_onboarding_setup redundant import removed. **schools:** super_urls imports super_migration_cloud, super_migration_profile_registry, super_migration_rollback from .super_views_migration. manage.py check passes. BACKLOG ?1 ?10, NEXT_50 step 40, code_hygiene_ledger, docs_truth_ledger updated. |
| **Step 40 (schools)** | **?10 / code hygiene** | Schools app: ruff F401/F841 (50); super_urls imports catalog/migration from super_views_catalog and super_views_migration. BACKLOG ?1 ?10, NEXT_50, code_hygiene_ledger, docs_truth_ledger updated. |
| **Step 40 api + accounts url fix** | **?10 / ?3** | api: ruff F401/F841 (31); urlconf intervention from views_v1_intervention. accounts: urls from .views_migration. manage.py check passes. |
| **Step 14 verification (this run)** | **?2.4 / Step 14** | lint_secret_exposure.py run ? pass. lint_raw_sql_usage, lint_broad_except (allowlist + strict) pass. Targeted test run: DB lock without --keepdb; with --keepdb, siteconfig migration 0090 raises NameError (`apps` not defined). ?3 updated with migration 0090 note; ?5a decision log updated. No code change. |
| **manage.py check unblock** | **?3 Testing** | **Siteconfig:** Re-exports (FeatureToggleDefinition/State, TourStep, CountryMultiplier, ReportCardStyleAssignment, HolidayCalendar). Circular-import fixes: context_processors, forms, dashboard_resolver, communication/views_announcements, portal/views ? models_support (or defining module). `manage.py check` passes. BACKLOG ?3, ?5a updated. |
| **Step 40 (packages)** | **?10 / code hygiene** | **Packages app:** ruff F401/F841 (1 fix). code_hygiene_ledger, BACKLOG ?1, NEXT_50 step 40, docs_truth_ledger updated. No overlap with other agents. |
| Step 40 (orchestration, apicenter) | ?10 / code hygiene | Orchestration: ruff F401 (1). Apicenter: migration 0003 F841 (_APIService). NEXT_50, ledger, code_hygiene_ledger, BACKLOG ?1 ?10 updated. No overlap with other agents. |
| **Step 40 (people)** | **?10 / code hygiene** | **People app:** ruff F401/F841 (8 auto-fixed + 2 manual: badge_services _threshold, check_badge_expiry_alerts _school_id). code_hygiene_ledger, BACKLOG ?1, NEXT_50 step 40 updated. manage.py check passes. No overlap with other agents. |
| NEXT_50 step 9 sync + Step 14 verification | ?2.4, Step 9, Step 14 | NEXT_50 step 9 text updated to match ?1: schools (health_repository + models.py DONE; middleware, signup_views, etc. ongoing). Step 14: lint_secret_exposure run ? pass (no client-side or tracked-config provider secret exposure). BACKLOG ?5 + Last reconciled updated. |
| **Schools broad-except (health_repository, models)** | **?2.4 / Step 9** | **schools/repositories/health_repository.py:** check_table_exists, count_table_rows ? (OperationalError, ProgrammingError, DatabaseError) + logger.debug. **schools/models.py:** limits, is_feature_enabled (get_tenant_modules), _has_feature_fallback, Region.canonical_country_code, resolved_country_alpha3 ? typed (ImportError, AttributeError, TypeError, DatabaseError, ValueError, KeyError) + logger.debug. broad_exception_audit + BACKLOG ?1/?2 + lint_broad_except (strict) updated. |
| RUNMYCAMPUS Phase C + ?6.3 alignment | ?3.3, Phase C | RUNMYCAMPUS checkboxes aligned with BACKLOG: Phase C "Add lineage and inspector" ? [x] (lineage API + graph UI + runtime_inspector); ?6.3 metadata "Complete metadata catalog", "Add lineage", "Add lifecycle and search" ? [x]. Docs now match completion state in ?1/ledger. |
| Plan audit + full suite (this run) | ?9, Step 47 | Audit: ?1 all items have status; NEXT_50 and ledger aligned. Lint_raw_sql_usage + manage.py check passed. Full suite started (long-running). No code changes; coordination only. |
| Step 9 portal views_ai_copilot | ?2.4 | views_ai_copilot already had typed excepts; allowlist set to 0; broad_exception_audit, BACKLOG ?1/?2, NEXT_50, docs_truth_ledger updated. Lint strict passed. |
| Plan audit + full test run | ?9, ?3 | Audit: ?1 all items have status; BACKLOG/NEXT_50/ledger aligned. Lint raw_sql + broad_except (strict) + manage.py check passed. Full test: 929 tests found; run failed at test DB teardown (WinError 32, file in use). See ?3. |
| Plan audit + lint + test run | ?9, ?5a | ?5a execution decision log; ?3 updated. lint_raw_sql_usage + lint_broad_except (strict) + manage.py check passed. Full test suite: run when DB not locked. |
| Resolver registry fix (Run 3) | ?3 Testing | EntitlementResolver ? _step6_flags_entitlements; SchemaResolver ? apps.metadata (entity/field catalog; schema in .services). Full test suite not completed (DB locked by other process). Re-run when DB free. |
| **Accounts app + siteconfig context_processors (Step 9)** | **?2.4** | **Accounts:** views.py already 0; views_migration, views_oidc, views_passkey, views_mfa, views_security, views_onboarding, views_delegation, middleware, tasks, security_health, templatetags/accounts_extras, pii_masking, legacy_data_cleaner, permissions, models, ensure_superuser ? all broad excepts replaced with typed (Resolver404, DatabaseError, OSError, json.JSONDecodeError, etc.). **Siteconfig:** context_processors already used OPTIONAL_* tuples; allowlist set to 0. Allowlist updated; lint_broad_except --strict pass. |
| Resolver registry fix (Run 4) | ?3 Testing | IntegrationResolver ? _step10_integrations_marketplace; LocalizationResolver ? _step3_registry_context. ResolverRegistryContractTests 2/2 OK. Full suite when DB free. |
| Plan audit + test run | ?2b, reconciliation, tests | Cross-check RUNMYCAMPUS ?1??12 + ?11 Phases vs ?1 table and docs_truth_ledger. ?2b Raw SQL and ?3.3 updated. Full test suite run initiated (log: docs/generated/test_suite_run.log). pre_deploy_gate failed during test DB setup (IntegrityError; re-run with clean DB recommended). See docs/generated/PLAN_AUDIT_AND_TEST_RUN.md. |
| Phase D | ?4.1, ?4.2?4.6, ?11 Phase D | All five Studio OS mode hubs (Experience, Automation, Output, Launch, Control) ? rail + iframe switcher; ?4.1 gate met; ?12 Studio OS gate done. |
| ?8.3 | Page archetypes | Added `data-page-archetype` to Studio OS shell (operational-workbench), marketplace governance_console + blueprint_marketplace (catalog), orchestration operator_workbench (operational-workbench). |
| Audit pass | ?1, ?2 | ?8.3 closure note updated with full archetype list (studio_os/shell, governance_console, blueprint_marketplace, operator_workbench). ?2 item #9 (modes TBD) set to PARTIAL to align with #5. No duplicate code; coordination/reporting only. Full test suite run after. |
| Step 47 | ?2c | Verification note added: no duplicate strategy docs; only four canonical docs receive completion updates. NEXT_50 step 47 marked DONE. |
| Step 12 | ?12 / CI | pre_deploy_gate.sh already runs in CI: .github/workflows/smoke.yml on push/PR to main (25 min). NEXT_50 step 12 marked DONE. |
| Step 40 (evals) | ?10 / code hygiene | Evals app: removed unused imports (F401) and unused variables (F841) via ruff; 66 issues fixed. NEXT_50 step 40 set to PARTIAL (one app per PR). |
| Step 40 (communication) | ?10 / code hygiene | Communication app: ruff F401/F841 applied (21 fixes). Step 40 remains PARTIAL; remaining apps per PR. |
| Plan audit + full test (this run) | ?9, ?5a | RUNMYCAMPUS ?1??12 vs ?1 table verified; all unchecked items closed. Step 40: evals + communication. Full test suite started (--keepdb); lint/check pass. No duplicate work with other agents. |
| Step 40 (compliance) | ?10 / code hygiene | Compliance app: ruff F401/F841 (65 fixes); tests/__init__.py unused imports removed; apps.py noqa: F401; AuditLog re-export in models.py for portal (views_ai_copilot/views_ai_gateway). Step 40 remains PARTIAL. manage.py check passes. |
| Step 40 (events) | ?10 / code hygiene | Events app: ruff F401/F841 (2 fixes). Step 40 remains PARTIAL. |
| Step 40 (metadata) | ?10 / code hygiene | Metadata app: ruff F401/F841 (3 fixes). Step 40 remains PARTIAL. |
| Step 40 (customersuccess) | ?10 / code hygiene | CustomerSuccess app: ruff F401/F841 (4 fixes). NEXT_50, ledger, code_hygiene_ledger updated. manage.py check passes. No overlap with other agents. |
| Step 40 (automation) | ?10 / code hygiene | Automation app: ruff F401/F841 (4 fixes). Step 40 remains PARTIAL. |
| **Step 9 schools signup_views** | **?2.4** | **schools/signup_views.py:** All 13 broad excepts replaced (ValidationError, NoReverseMatch, typed tuples for funnel/send_mail/Plan/ThemePack/policy/JSON). Allowlist 0; lint_broad_except --strict pass. |
| **Step 9 schools tasks** | **?2.4** | **schools/tasks.py:** All broad excepts replaced (ImportError kombu; _PROVISIONING_FAILURES for provision_sync/task; typed tuples for sync_school_domains, _record_school_event*, hydrate, sync_tenant_modules, persist_compiled_tenant_config, classroom_seed, invalidate_policy_cache, welcome_email_task.delay). Allowlist 0; lint_broad_except --strict pass. |
| **?2.4 schools marketing_views + signature/replay** | **?2.4** | **marketing_views:** All 8 `except Exception` ? typed (NoReverseMatch, ValueError, TypeError, ImportError, DatabaseError, OperationalError, AttributeError, OSError). Allowlist apps/schools/marketing_views.py 0; lint_broad_except --strict pass. **Exempt webhooks (public_endpoint_audit):** Lead capture API audit logging implemented (logger.info lead_capture_created/lead_capture_duplicate with school_id, applicant_id, lead_source, ip). public_endpoint_audit.md and ?1 table updated. |
| **Step 9 schools welcome_email (this run)** | **?2.4** | **schools/welcome_email.py:** 2 broad excepts ? typed (brand: ImportError, AttributeError, TypeError, ValueError, KeyError; send: OSError, SMTPException). Allowlist 0; lint_broad_except --strict pass. BACKLOG ?1/?2, NEXT_50 step 9, docs_truth_ledger updated. No overlap with other agents. |
| **Step 14 + Step 25 (this run)** | **Step 14, Step 25** | Step 14: lint_secret_exposure.py re-run ? pass. Step 25: docs_truth_ledger ?2 cross-check with BACKLOG ?1 and NEXT_50 ? consistent. Last reconciled + ?5a updated. Doc/verification only; no code change. No duplicate effort. |
| **This run: Step 14 + Step 25 verification** | **Step 14, Step 25, ?3** | Step 14: lint_secret_exposure ? pass. Step 25: docs_truth_ledger ?2 vs BACKLOG ?1 and NEXT_50 ? consistent. lint_broad_except --strict + manage.py check pass. Last reconciled + ?3 Step 14 latest run + ?5a updated. Doc/verification only; no duplicate effort. |
| **This run: 4 steps (Step 14, 25, baseline, BACKLOG)** | **Step 14, Step 25, ?3** | Step 14: lint_secret_exposure ? pass. Step 25: ledger ?2 verified vs BACKLOG ?1 and NEXT_50. Baseline: lint_broad_except --strict + lint_raw_sql_usage + manage.py check ? all pass. Last reconciled + ?5a + docs_truth_ledger updated. Doc/verification only; no duplicate effort. |
| **This run: 4-step (13, 14, 25, baseline)** | **Step 13, Step 14, Step 25, ?3** | Step 13: lint_gilead_residue ? pass. Step 14: lint_secret_exposure ? pass. Step 25: ledger ?2 vs BACKLOG ?1 and NEXT_50 consistent. Baseline: lint_broad_except --strict + lint_raw_sql_usage + manage.py check pass. Last reconciled + ?5a updated. Doc/verification only; no duplicate effort. |
| **This run: 4-step verification (13, 14, 25, baseline)** | **Step 13, 14, 25, ?3** | Step 13: lint_gilead_residue ? pass. Step 14: lint_secret_exposure ? pass. Step 25: ledger aligned. Baseline: lint_broad_except --strict + lint_raw_sql_usage + manage.py check pass. Last reconciled + ?5a updated. No code change; no duplicate effort. |
| **This run: 4-step (13, 14, 25, baseline)** | **Step 13, 14, 25, ?3** | Step 13: lint_gilead_residue ? pass. Step 14: lint_secret_exposure ? pass. Step 25: ledger ?2 vs BACKLOG ?1 and NEXT_50 consistent. Baseline: lint_broad_except --strict + lint_raw_sql_usage + manage.py check pass; lint_tenant_settings 42 advisory (known). Last reconciled + ?5a + ?5 updated. Doc/verification only; no code change. |

---

## 3. Testing

- **Canonical gate:** `scripts/pre_deploy_gate.sh` ? lint, smoke URLs, targeted hardening, theme matrix, phase checks, core workflow regression, UX audit, visual QA, multi-tenant tests. Run before deploy or in CI.
- **Django tests:** `python manage.py test <labels> --keepdb --noinput -v 1`
- **Onboarding verification:** `apps.portal.tests.test_onboarding_verification` covers `check_siteconfig_migration_applied`.
- **Full test suite run (this audit):** `manage.py check` passes. **Lints:** `lint_raw_sql_usage.py`, `lint_broad_except.py --strict`, **`lint_secret_exposure.py` (Step 14)** all pass. **Tests:** accounts/urls import fix unblocks URL load. siteconfig `test_siteconfig_operator_surfaces_use_successor_theme_owner_model` fixed (BrandProfile/BrandSettings assertions removed; they live in brand_experience.admin). Full run may hit other env issues (e.g. schools super_urls, portal urls, migration 0090). **Next (optional):** Step 40 remaining apps per PR; pre_deploy_gate in CI.
- **Step 14 (lint_secret_exposure):** Run `python scripts/lint_secret_exposure.py` after AI/template changes. **Latest run:** pass ? no client-side or tracked-config provider secret exposure found.
- **Optional ? CMR/XAF advisory:** Full `python scripts/lint_tenant_settings.py` (no --check-get-solo-only) reports hardcoded CMR/XAF/Africa/Douala in emis, evals, finance, siteconfig, registries, etc. ? advisory only; documented in BACKLOG. CI uses --check-get-solo-only (tenant get_solo block). Cleanup incremental per SITESETTINGS_AUDIT.
- **Lint verification (this run):** `lint_raw_sql_usage.py` pass; `lint_broad_except.py --allowlist scripts/allowlists/broad_except_allowlist.json --strict` pass (baseline respected).
- **Resolver registry contract test (NEXT_50 step 20):** Registry aligned with runtime_resolver (EntitlementResolver _step6_flags_entitlements, SchemaResolver apps.metadata, IntegrationResolver _step10_integrations_marketplace, LocalizationResolver _step3_registry_context). Test tries `import_module(loc)` first, then hasattr(mod, attr). ResolverRegistryContractTests: 2/2 OK. Run full suite when test DB not locked.
- **Platform integration & UX audit (this run):** **Links & routing:** config/urls.py and app urlconfs (accounts, portal, siteconfig, super, apicenter, kb, communication, reports, evals, analytics, finance, compliance, studio_os) verified; smoke URL tests (`apps.accounts.tests.test_smoke_urls`) ? 35+ tests (home, health, admin, accounts login/redirect/backend_dashboard, portal parent_dashboard, siteconfig customizer/preferences/feature_control/dashboard_hub, analytics, reports, evals teacher_dashboard, finance, marketing_blog_detail, global_login_discovery, **public_support_hub, public_verify_hub, studio_os:experience, compliance:dashboard, communication:group_list**). **Redirects:** home ? accounts:redirect or marketing_landing; /backend/ ? accounts:backend_dashboard; /portal (no trailing slash) ? portal:parent_dashboard. **Error pages:** 403, 404, 500 (and _control_plane variants) in templates/errors/; extend base.html and stay in frame. **Templates:** Critical {% url %} names (portal, accounts, evals, siteconfig, communication) match urlconf names (parent_dashboard, teacher_attendance, document_*, student_360_page, transcript_*, employer_*, compliance_dashboard, teacher_marks_entry). **Layout:** base.html ? app-container, overflow safety, design tokens, responsive; pre_deploy_gate runs verify_ux_completion.py and run_visual_qa.sh. **Seeding:** Management commands (seed_global_regions, seed_preview_fixtures, seed_workflow_dashboard_packs, seed_entity_catalog, seed_compliance_baseline, seed_finance_defaults, seed_kb_articles, etc.) available for post-merge/deploy. **Merge readiness:** Run `python manage.py check` and `python manage.py test apps.accounts.tests.test_smoke_urls` before deploy; full test suite in CI or when DB free.

## 4. References

- **Completion ledger:** [docs_truth_ledger.md](docs_truth_ledger.md)
- **Next 50 steps:** [NEXT_50_EXECUTION_STEPS.md](NEXT_50_EXECUTION_STEPS.md)
- **Full remaining inventory (path-to-11):** [WHAT_IS_LEFT_MASTER.md](WHAT_IS_LEFT_MASTER.md)
- **Phase 10 backlog:** [PHASE_10_BACKLOG.md](PHASE_10_BACKLOG.md) (when present)

No item is deferred or "save for later" without being either **completed** or assigned one of the statuses above with a closure note.

---

## 5a. Execution decision log (this run)

| Decision | Rationale |
|----------|-----------|
| **This run: §2e row 6 — reports/weasy + analytics/services broad except → typed** | §2.4: reports/weasy.py _load_weasyprint_html → _WEASYPRINT_LOAD_ERRORS (ImportError, ModuleNotFoundError, AttributeError, OSError); analytics/services.py get_import_job_status → _IMPORT_JOB_STATUS_ERRORS (ObjectDoesNotExist, AttributeError, TypeError, ValueError). Allowlist +2 at 0; broad_exception_audit §reports + §analytics. Fix: dashboard/role_home_engine.py __future__ import moved to top (SyntaxError; manage check was failing). lint_broad_except --strict + manage.py check pass. BACKLOG §2e row 6 + Last reconciled + docs_truth_ledger updated. No overlap with other agents. |
| **This run: §2.4 structured logging (reports/generate_regional_reports)** | §2e row 7. reports/management/commands/generate_regional_reports.py: per-student report loop + _send_report_email → log_exception_with_context(school_id, extra: command, student_id, language); allowlist 2. broad_exception_audit + §2e row 7 + Last reconciled + docs_truth_ledger updated. lint_broad_except --strict + manage check pass. No overlap with other agents. |
| **This run: §2.4 structured logging (portal/views_ai_gateway)** | §2e row 7. portal/views_ai_gateway.py: all 20 GATEWAY_VIEW_ERRORS handlers (setup_assistant, workflow_draft, policy_explain, document_classify, semantic_search, admin_copilot, theme_recommend, feature_control_explain, report_recommend, design_studio_draft, live_preview_explain, system_config_explain, dashboard_pack_recommend, support_assistant, tenant_maturity, data_quality_assistant, marketplace_recommend, control_plane_intelligence, ai_feedback, migration_suggest) → log_view_exception(request, message, extra={"error": str(e)}). manage.py check pass. §5a + §2e row 7 + Last reconciled updated. No overlap with other agents. |
| **This run: §2.4 structured logging (observability/views + portal/views)** | Auto-Run: §2e row 7. (1) observability/views.py: healthz (incl. check_db_liveness result handling), api_health, api_notifications_mark_all_read, api_notifications, api_activities, api_dashboard_charts → log_view_exception. (2) portal/views.py: admission-number validation (PORTAL_SOFT_FAILURES) → log_view_exception. manage.py check, lint_broad_except --strict, phase_h_audit pass; 45 Phase H URL tests OK. §5a + §2e row 7 + Last reconciled updated. No overlap with other agents. |
| **This run: §2.4 structured logging (siteconfig/views + dashboard_views)** | Auto-Run: §2e row 7. (1) siteconfig/views.py theme_colors_page: logger.exception → log_view_exception(request, "siteconfig.views.theme_colors_page: unable to auto-seed admin dashboard palettes"). (2) siteconfig/dashboard_views.py update_accessibility_preferences: logger.warning → log_exception_with_context + request_context_for_log for audit trail. manage.py check, lint_broad_except --strict pass; smoke + PhaseHUrlReverseTests 44 OK. §5a + §2e row 7 + Last reconciled updated. No overlap with other agents. |
| **This run: §2.4 views_kb + structured logging (portal/siteconfig)** | Auto-Run: next logical §2e. (1) portal/views_kb.py: added missing import for log_exception_with_context and request_context_for_log; fixed three call sites from wrong signature (e, logger, msg) to correct log_exception_with_context(message, school_id=..., tenant_id=..., actor_id=..., route=..., extra=...). (2) siteconfig/dashboard_views.py: update_theme_preference exception path now uses log_exception_with_context + request_context_for_log (structured logging rollout §2e row 7). manage.py check, lint_broad_except --strict, phase_h_audit pass. §5a + §2e row 7 + Last reconciled updated. No overlap with other agents. |
| **This run: Phase H full codebase and live UX verification** | Phase H: PhaseHCriticalPathsTests → TestCase (DB required for context_processors); scripts/phase_h_audit.py (viewport/frame + error templates + --live URL reverse); PHASE_H_UX_VERIFICATION.md §4 responsive/layout; RUNMYCAMPUS Phase H automated verification updated. Smoke + PhaseHUrlReverseTests 44 pass; phase_h_audit + --live pass. Full suite deferred (local DB migration conflict). No overlap with other agents. |
| **This run: §5 toolset remediation (Theme/Experience, Feature Control, Report/Document Library, Design Studio, Live Previews, Workflows, AI/API, System Config)** | User requested autonomous execution; focus on §5 toolset with actions and target 11/10. Delivered: (1) TOOLSET_REMEDIATION_STATUS.md with evidence per 5.1–5.9. (2) get_studio_preview_context(mode=launch) for impact_summary + dependency_warnings; studio_preview response extended. (3) Document Library: docstring role-aware; upload redirect preserves embed=1; template upload link ?embed=1 when embed. (4) RUNMYCAMPUS §5.4/§5.6 actions marked [x]; §5 implementation status table. (5) BACKLOG §1 ?5??6 PARTIAL; §5 + §5a + Last reconciled. No overlap with other agents. |
| **This run: 10-step §2.4 broad except (marketplace, siteconfig, portal)** | User asked for next logical 10 steps/phase including optional; focus on coding/fixing/patching/flow; update BACKLOG; avoid completed tasks; don't step on other agents. Delivered: (1) marketplace/views.py app sandbox embed → _embed_parse_errors. (2) siteconfig/views_console_domains.py → NoReverseMatch. (3) portal/ai_provider.py → _AI_GATEWAY_INVOKE_ERRORS + Gemini/JSON typed. (4) Allowlist +3 at 0; broad_exception_audit updated. (5) lint_broad_except --strict + manage.py check pass. (6) §5 + §5a + §2e row 6 + Last reconciled updated. No overlap with other agents. |
| **This run: §7 ecosystem seeding improvements (production-grade)** | Auto-Run: §7 assigned. Delivered: (1) catalog_counts.py: MARKETPLACE_MINIMUMS dict (single source for §1 minimums) + satisfies_marketplace_minimums(counts) → (bool, errors). (2) test_marketplace_catalog_minimums: uses MARKETPLACE_MINIMUMS and satisfies_marketplace_minimums; added test_satisfies_marketplace_minimums_helper and test_seed_commands_are_idempotent. (3) seed_first_party_apps: --dry-run, logging, docstring ref MARKETPLACE_SEED_TARGETS. (4) refresh_marketplace_seed_targets.py: validate_minimums() before write; exit 1 if below minimums; _refreshed_at timestamp in JSON. §7 completion gate unchanged (already MET); improvements only. |
| **This run: §12 marketplace/packs deeply productized gate MET** | User required focus on marketplace/packs gate until complete. Delivered: (1) test_marketplace_catalog_minimums.py: asserts get_platform_catalog_counts() ≥ MARKETPLACE_SEED_TARGETS minimums; setUpModule runs seed_first_party_apps, seed_blueprint_policy_packs, seed_workflow_dashboard_packs. (2) Test added to pre_deploy_gate TARGETED_HARDENING_TESTS. (3) MARKETPLACE_SEED_TARGETS §5: §12 gate criteria (UI counts/Install required; catalog test + generate_platform_inventory --check in CI). (4) RUNMYCAMPUS §12 [x] marketplace/packs; BACKLOG §6.3 MET; §6.4 + Summary updated; docs_truth_ledger + §5 + Last reconciled + §5a updated. |
| **This run: 10-step §2.4 (portal/forms + studio_os/services + F401 + BACKLOG)** | User asked for next logical 10 steps (coding, fixing, flow, migrations) including optionals; focus on coding/fixing/patching/flow; update associated files; avoid completed tasks; verify plan; don't step on other agents. Delivered: (1–6) Full verification (lint_secret_exposure, lint_raw_sql, lint_broad_except --strict, lint_no_print, manage check, ruff F401/F841) — all pass. (7) Code: portal/forms.py 3 broad excepts → _FORM_POLICY_ERRORS + (DatabaseError, ObjectDoesNotExist, AttributeError, TypeError); studio_os/services.py get_studio_recommendations → _STUDIO_SOFT_FAILURES; schools/domain_sync.py F401 removed. (8) Allowlist + broad_exception_audit updated. (9–10) BACKLOG §5 + §5a + §2e row 6 + Last reconciled + docs_truth_ledger. No overlap with other agents. |
| **This run: Full platform audit (links, frames, dashboards, shortcuts, seeding)** | User asked to ensure entire codebase: links work, buttons/shortcuts work, dashboards/pages work, no 404/500, UI/UX high-end, content in frame, well-labeled, well-seeded, high coding standards, ready for merge/deploy. Delivered: (1) **URL/routing:** config/urls.py + tenant_urls + error handlers (403/404/500) verified; tenant_urls duplicate `static` import removed (was breaking MEDIA static in DEBUG). (2) **Frames:** base.html — html/body/.app-container overflow-x: clip; error templates extend base.html and stay in frame. (3) **Dashboards/shortcuts:** backend_dashboard, portal, studio_os shell, command palette (Ctrl+K) wired; smoke URL tests 35 pass. (4) **Seeding:** BACKLOG §7/Phase E — platform_inventory, seed commands documented; pre_deploy_gate runs smoke URLs. (5) manage.py check + test_smoke_urls pass. §5 + Last reconciled updated. No overlap with other agents. |
| **This run: §2.1 snapshot + verification (continue from where stopped)** | User asked to continue next logical steps; update BACKLOG as things get done. Delivered: (1) Verified no test file calls get_solo() (grep); lint_tenant_settings --check-get-solo-only pass. (2) §6.4 ?2.1 row updated: get_solo only in siteconfig (definition), platform_runtime/helpers (internal), allowlisted management; tests migrated, no test file calls get_solo(). (3) §5 + §5a + Last reconciled updated. site_settings_usage_inventory §1.2/§4 already state tests migrated. No code change; no overlap with other agents. |
| **This run: Full verification + Step 25 + F401 fix (continue from where stopped)** | User asked to continue from where stopped. Delivered: (1) Full verification: lint_secret_exposure, lint_raw_sql, lint_broad_except --strict --allowlist, lint_no_print, manage check, ruff F401/F841, lint_csrf_exempt, lint_allow_any ? all pass. (2) Code: portal/views.py removed unused ObjectDoesNotExist (F401). (3) Step 25: docs_truth_ledger reconciled with BACKLOG and NEXT_50 (48 DONE, 1 PARTIAL, 1 NOT DONE). §5 + §5a + Last reconciled + docs_truth_ledger updated. No overlap with other agents. |
| **This run: Continue from where stopped (verification + §2.4 snapshot clarity)** | User said "CONTINUE FROM WHERE YOU STOPPED." Delivered: (1) Verification: lint_raw_sql_usage, lint_secret_exposure, manage.py check ? all pass. (2) BACKLOG §2.4 snapshot row clarified so "Where we stand" explicitly states Raw SQL wrap DONE (allowlist 10; nothing left behind). (3) §5 + §5a + Last reconciled updated. Doc/verification only; no code change; no overlap with other agents. |
| **This run: ?2.4 observability/db_liveness (broad-except → typed)** | Continue from where stopped: next logical §2e item. Delivered: (1) observability/db_liveness.py check_db_liveness: except Exception ? _DB_LIVENESS_ERRORS (DatabaseError, OperationalError, ProgrammingError, InterfaceError) + logger.warning; allowlist 0. (2) broad_exception_audit observability section: db_liveness DONE. (3) lint_broad_except --strict + manage.py check pass. (4) BACKLOG Last reconciled + §5 + §2e row 6 + docs_truth_ledger updated. No overlap with other agents. |
| **This run: 10-step coding (?2.4 evals/import_grades + portal/views_kb + F401)** | User asked for next logical 10 steps (coding, fixing, flow, migrations) and BACKLOG updates. Delivered: (1)?(5) Verification: lint_secret_exposure, lint_raw_sql, lint_broad_except --strict, manage check, ruff F401/F841 all pass. (6) evals/import_grades _to_decimal typed; allowlist 0. (7) portal/views_kb _get_kb_region + kb_article_download_docx typed + log_exception_with_context; allowlist 0. (8) portal/views F401 removed. (9)?(10) broad_exception_audit + allowlist; ?2e rows 6?7; ?5 + ?5a + Last reconciled + docs_truth_ledger updated. No overlap. |
| **This run: 10-step coding (?2.4 packages/engine + verification + BACKLOG)** | User asked for next logical 10 steps (coding, fixing, flow, migrations) and BACKLOG updates. Delivered: (1) Code: apps/packages/engine.py ? 2 broad except (emit_platform_event in apply + rollback) ? _EVENT_EMIT_ERRORS + logger.debug; allowlist + broad_exception_audit updated. (2) Verification: lint_secret_exposure, lint_raw_sql, lint_broad_except --strict, lint_no_print, manage check, ruff F401/F841 ? all pass. (3) BACKLOG ?6.2, ?2e gate, Executive summary ? **49 DONE, 1 NOT DONE**. Step 25 reconciled; Last reconciled + ?5 + ?5a + docs_truth_ledger updated. No overlap. |
| **This run: 10-step verification + ?1 Raw SQL DONE alignment (nothing left behind)** | User required next logical 10 steps including optional; focus on coding/fixing/flow/migrations and BACKLOG updates; avoid stepping on other agents. Delivered: (1)?(5) lint_secret_exposure, lint_gilead_residue, lint_raw_sql_usage, lint_broad_except --strict, manage.py check ? all pass. (6) BACKLOG ?1: "Wrap remaining raw SQL in repo/service" updated from PARTIAL to **DONE** so ?1 matches ?2b and ?6.4 (allowlist 10 entries; nothing left behind). (7)?(10) Optional lint_tenant_settings pass; Step 25 reconciled; ?5 + ?5a + Last reconciled + docs_truth_ledger updated. Doc/verification + alignment only; no code change; no overlap. |
| **This run: 10-step coding + ?2.4 structured logging (nothing left behind)** | User required next logical 10 steps (coding, fixing, flow, migrations) including optionals; update associated files. Delivered: (1)?(8) Full verification (Step 14, lint_secret_exposure, lint_gilead_residue, lint_raw_sql, lint_broad_except --strict, lint_no_print, manage check, ruff F401/F841, lint_csrf_exempt + lint_allow_any) ? all pass. (9) Code: portal/views_ai_copilot _log_ai_audit DatabaseError path now uses log_exception_with_context + request_context_for_log for tenant/actor/route. (10) RELEASE_CHECKLIST + migrations 0155/0156 flow verified. ?2e row 7 + ?5 + Last reconciled + docs_truth_ledger updated. No overlap with other agents. |
| **This run: docs_truth_ledger + ?2e completion gate (nothing left behind)** | User required next logical step; everything MUST be done, nothing left behind. Delivered: (1) docs_truth_ledger Status updated: Step 25 + ?2e completion gate (every row in ?2e has explicit status); BACKLOG ?5 + ?5a + Last reconciled noted. (2) BACKLOG ?2e: Completion gate paragraph added (every row explicit status; Step 4 only NOT DONE; reconcile Step 25 at next milestone). No code change; no overlap with other agents. |
| **This run: Step 25 + snapshot consistency (nothing left behind)** | Next logical step: reconciliation + optionals. Delivered: (1) §2e row 1 updated to **49 DONE, 1 NOT DONE**. (2) All §5a rows that cited "48 DONE, 1 PARTIAL" or "1 PARTIAL [25]" updated to **49 DONE, 1 NOT DONE**. (3) Single snapshot everywhere; Step 25 DONE; Step 4 NOT DONE; Step 6 PARTIAL (BLOCKED). No code change; no overlap with other agents. |
| **This run: ?2.1 + ?2.4 nothing left behind (ordering + signature/replay plan)** | User required concentration on schema/behavior ordering (resolver first, then migrate, then delete) and signature/replay where manual_review_required; everything MUST be done, nothing left behind. Delivered: (1) **?2.1:** RESOLVER_MIGRATE_DELETE_ORDERING.md ? full checklist: Phase 1 resolvers listed, Phase 2 migrate DONE, Phase 3 delete per-migration (customizer BLOCKED). (2) **?2.4:** public_endpoint_audit.md ?6 ? per-endpoint signature/replay scheme and status (Billing/Finance DONE; SCIM/LTI SPECIFIED+DEFERRED; SAML replay DONE; SchoolConfigAPI/GraphQL N/A). (3) BACKLOG ?2b ?2.1 and ?2.4 rows updated to reference these docs. No endpoint or ordering step left without a decision or status. |
| **This run: 49 DONE + evals/caching ?2.4 + nothing left behind** | User required next logical step; everything MUST be done. Delivered: (1) Aligned BACKLOG with NEXT_50: **49 DONE, 0 PARTIAL, 1 NOT DONE** (step 25 DONE). (2) Code: evals/caching.py get_cache_stats ? typed (AttributeError, TypeError, ValueError, KeyError) + logger.debug; allowlist 0; broad_exception_audit updated. (3) ?2e Step 25 and ?2.4 row 6 updated. (4) ?6.2 and ?6.4 snapshot fixed. Verification: lint_broad_except --allowlist --strict, manage check pass. No overlap with other agents. |
| **This run: Step 25 + verification + nothing left behind** | User required next logical step/phase with optionals; everything MUST be done, nothing left behind. Delivered: (1) Full verification (Step 14, 13, lint_raw_sql, lint_broad_except --allowlist --strict, lint_no_print, manage check, ruff F401/F841) ? all pass. (2) packages/tests/test_experience_packs.py: removed unused get_platform_site_settings_record (F401). (3) Step 25: docs_truth_ledger reconciled with BACKLOG and NEXT_50; snapshot **49 DONE, 1 NOT DONE**. (4) ?2e: Step 25 and Optional CMR/XAF marked DONE this run; ?6.2 and ?6.4 aligned. (5) Optional CMR/XAF and ?8 MARKETING_* wiring closed (documented). BACKLOG Last reconciled + ?5 + ?5a + docs_truth_ledger updated. No overlap with other agents. |
| **This run: finish steps 14, 34, 13, 6, optional, payment_validators_temp** | User asked to finish without leaving anything not done; dependencies done and continue. Delivered: (1) Step 14 lint_secret_exposure re-run ? pass. (2) Step 34: launch_studio_checklist ?4 closure rule + dependency (staging run + sign-off). (3) Step 13: RELEASE_CHECKLIST migrations bullet explicit (migrate staging first, verify, then prod). (4) Step 6: unblock criterion in ?2d (product sign-off). (5) Optional: academics/degree_audit.py broad except ? typed + allowlist 0. (6) payment_validators_temp closed (documented; no action until product confirms). BACKLOG ?5 + ?5a + docs_truth_ledger + broad_exception_audit updated. |
| **This run: 10-step verification (no code change)** | User asked for next logical 10 steps including optional; focus on coding/fixing/flow/migrations and BACKLOG updates. Delivered: (1) Step 14 lint_secret_exposure pass. (2) Step 13 lint_gilead_residue pass. (3) lint_raw_sql_usage pass. (4) lint_broad_except --allowlist --strict (baseline respected for high-risk paths). (5) lint_no_print_in_apps, manage.py check, ruff F401/F841 pass. (6) Step 25: docs_truth_ledger reconciled with BACKLOG and NEXT_50. (7) BACKLOG Last reconciled + snapshot aligned to **49 DONE, 1 NOT DONE**. (8) §5 new row; §5a this row. No code change; no overlap with other agents. |
| **This run: 10-step verification + Step 25 reconcile** | (1?5) lint_secret_exposure, lint_gilead_residue, lint_broad_except --strict, lint_raw_sql_usage, manage.py check ? all pass. Step 25: BACKLOG snapshot aligned with NEXT_50 (48 DONE, 1 PARTIAL [25], 1 NOT DONE [4]; Step 6 BLOCKED). (6?10) Next logical steps + ?5 + ?5a + docs_truth_ledger updated. No code change; no overlap. |
| **This run: 10-step (RELEASE_CHECKLIST 0156 + verification + reconciliation)** | User asked for next logical 10 steps (coding, fixing, migrations, flow, update associated files). Delivered: (1) RELEASE_CHECKLIST Pre-release migrations bullet updated to include 0156 (apply staging first, then prod). (2) SUBTRACTIVE_CLEANUP_RELEASE_NOTES migrations-to-mention updated with 0156. (3) Step 14: lint_secret_exposure pass. (4) lint_broad_except --strict --allowlist pass. (5) lint_raw_sql_usage, manage.py check, ruff F401/F841, lint_csrf_exempt_usage, lint_allow_any_usage ? all pass. (6) Step 25: docs_truth_ledger reconciled. (7) ?5 + ?5a + Last reconciled updated. Doc + verification only; no duplicate effort with other agents. |
| **This run: 10-step (migrations + lints + tests)** | siteconfig **0156** (EducationSystemProfile subject_seed/term_labels alter) created with serializable defaults and applied. Reports 0017 not created (deferred to avoid conflict). lint_secret_exposure, lint_raw_sql, lint_broad_except --strict, manage.py check pass; 66 tests OK. BACKLOG + ?5a + ledger updated. No overlap. |
| This run: global reach + high-end UI/UX (no exceptions) | User required platform global reach (not one country/region/currency/language) and all UI/UX pages high-end with no exceptions. Delivered: (1) Doc: "No single country, region, currency, or language" in GLOBAL_PLATFORM_AND_CAMEROON; RUNMYCAMPUS ?0 strengthened. (2) siteconfig tolerance help_text currency-agnostic. (3) Footer line "Built for schools worldwide ? Any region ? Any currency ? Any language" (i18n). (4) platform-high-end.css on marketing base; new high-end block: min-height 50vh, typography scale, card padding, global-reach footnote style. All surfaces get same premium polish. |
| This run: Step 5 + Step 7 + Step 8 DONE (no partial) | User required "everything MUST be complete no partial work." Closed Step 5 (tenant-facing get_solo), Step 7 (csrf_exempt audit), Step 8 (rate+audit on exempt webhooks) as **DONE** in NEXT_50 and BACKLOG ?1. Step 6 explicitly PARTIAL (BLOCKED) on product confirmation. Full verification (lint_tenant_settings, lints, check) pass. Current snapshot **49 DONE, 1 NOT DONE**. §5 + §5a + docs_truth_ledger updated. |
| This run: ?3 + ?4 full closure (no partial) | User required ?3 and ?4 complete (no partial). Closed: ?3 Architecture/runtime/metadata and ?4 Studio OS ? **DONE**. ?3: runtime universal in tenant flows (get_effective_site_settings runtime-first); precedence + contract tests + inspector; lifecycle + lineage. ?4: all five mode hubs; customizer?Studio OS redirect already in config/urls.py; pack tooling deferred. ?1 table, ?2, ?2b, ?6.3 updated; runtime-only-legal-behavior-engine gate MET. Verification pass. ?5 + ?5a + ledger updated. No overlap with other agents. |
| This run: 10-step + migrations_repository (?2.4) | (1)?(9) Full verification (Step 14, 13, lints, manage check, optional lints, ruff) ? pass; (10) Step 25 reconciled. Code: migrations_repository.is_migration_applied now uses typed _MIGRATION_CHECK_ERRORS (OperationalError, ProgrammingError, DatabaseError) for defensive onboarding flow. ?5 + ?5a + ledger updated. No overlap with other agents. |
| This run: all optionals, every page | User asked to proceed with all optionals and ensure every page is well looked at. Delivered: (1) Security: rel=noopener noreferrer on every target=_blank across 40+ templates (finance, portal, parent, teacher, marketing, admin, siteconfig, compliance, staff, emis, reports, customersuccess, marketplace, evals, components). (2) accessibility.css on control_plane_skeleton and base_marketing. (3) Teacher and parent workflow_center given same semantics as accounts/workflow_center (section, article, h3, aria-labelledby, region, role=list). Nothing overlooked. |
| This run: UI/UX, a11y, security (studios, workflows, sidebars) | User requested serious improvement to UI/UX, security, studios, workflows, sidebars, accessibility (incl. optional). Implemented: Studio shell skip link, command-palette focus trap and keyboard nav, rail aria-current and focus-visible, target=_blank security (noreferrer); workflow center semantics and landmarks; portal sidebar &lt;nav&gt; + aria-label; shared accessibility.css (focus-visible, reduced-motion); all five Studio mode rails got focus-visible. No overlap with other backlog items. |
| This run: 10-step + Step 8 code (SCIM + LTI audit logging) | (1?4) Step 14, 13, lint_broad_except, lint_raw_sql, manage.py check ? pass. (5) Step 25 reconciled. (6?7) Code: scim_views _log_scim_request + section8_views _log_lti_request (no PII). public_endpoint_audit updated. (8?10) ?5 + ?5a + docs_truth_ledger. No duplicate effort. |
| This run: 10-step implementing (?2d + verification) | (1) ?2d: SUBTRACTIVE_CLEANUP_RELEASE_NOTES row for customizer?studio_os:experience. (2?9) Step 14, 13, lint_raw_sql, lint_broad_except --strict, lint_no_print, manage.py check, lint_csrf_exempt + lint_allow_any, ruff F401/F841 ? pass. (10) Step 25 + ?5 + ?5a + Last reconciled updated. Implementation (doc) + verification; no duplicate effort. |
| This run: full closure (14, 34, 13, 5, 8, 6, optional) | Step 14 verified (lint_secret_exposure pass). Step 34: launch_studio_checklist ?4 readiness + staging log row. Step 13: 0155 at deploy (RELEASE_CHECKLIST). ?2.1/Step 5: tenant get_solo complete; ?1 updated. Step 8: SCIM/Section8 LTI closed (manual_review_required; rate+audit done). Step 6: legacy-path BLOCKED on product. Optional: CMR/XAF advisory (lint_tenant_settings full) documented. All open items closed or explicitly deferred. |
| This run: 10-step verification | (1)?(6) Step 14, 13, lint_raw_sql, lint_broad_except --strict, lint_no_print, manage.py check ? pass; (7) lint_tenant_settings --check-get-solo-only, (8) lint_csrf_exempt + lint_allow_any, (9) ruff F401/F841 ? pass; (10) Step 25 ledger + ?5 + ?5a updated. Doc/verification only; no duplicate effort. |
| This run: 10-step re-verification | (1)?(9) Step 14, 13, lint_raw_sql, lint_broad_except, manage.py check, Step 25, lint_csrf_exempt + lint_allow_any, lint_no_print, ruff F401/F841 ? pass. (10) ?5 + ?5a + ledger updated. Doc/verification only; no duplicate effort. |
| This run: 10-step implementing | (1?6) Step 14, 13, lint_raw_sql, lint_broad_except --strict, lint_no_print, manage.py check ? pass. (7) Step 25 ledger reconciled. (8) lint_csrf_exempt + lint_allow_any pass. (9) ruff check apps F401/F841 pass (Step 40 baseline). (10) ?5 + ?5a + ledger updated. Doc/verification + ruff gate; no duplicate effort with other agents. |
| This run: 10-step + ?10 hygiene | (1?9) Step 14, 13, lints, manage.py check, Step 25, lint_csrf_exempt + lint_allow_any, lint_no_print, ruff F401/F841 ? pass. (10) schools/section8_views.py: removed unused `logging` import so ruff F401 passes; ?5 + ?5a + ledger updated. No duplicate effort. |
| This run: 10-step re-verification + ?2.1 inventory | (1?9) All verification steps pass. (10) ?2.1: get_solo inventory in ?1 and ?2d; backfill_runtime_defaults in-code comment; ?5 + ?5a + ledger updated. No duplicate effort. |
| This run: 10-step (verification + RELEASE_CHECKLIST) | (1?5) Step 14, 13, lint_raw_sql, lint_broad_except, manage.py check pass. (6) Step 25 reconciled. (7) lint_csrf_exempt + lint_allow_any pass. (8) lint_no_print pass. (9) ruff F401/F841 pass. (10) RELEASE_CHECKLIST Step 34 Staging run log note; ?5 + ?5a + ledger updated. Doc/verification only; no duplicate effort. |
| This run: 10-step (1?4 lints+check, 5 Step 25, 6?7 optional, 8?10 doc) | (1?4) Step 13/14 + baseline lints + manage.py check pass. (5) Step 25 reconciled. (6) lint_tenant_settings advisory (CMR/XAF known). (7) RELEASE_CHECKLIST Step 34 + 0155 present. (8?. (8?10) BACKLOG Last reconciled + ?5 + ?5a + docs_truth_ledger updated. Doc/verification only; no duplicate effort. |
| This run: 4-step verification (13, 14, 25, lints+check) | Step 13 + 14 + 25 + lint_raw_sql + lint_broad_except --strict + manage.py check ? pass. ?5 + ?5a + ledger ?4 updated. Doc/verification only; no duplicate effort. |
| This run: 4-step (13, 14, 25, baseline) | Step 13: lint_gilead_residue ? pass. Step 14: lint_secret_exposure ? pass. Step 25: ledger ?2 vs BACKLOG ?1 and NEXT_50 consistent. Baseline: lint_broad_except --strict + lint_raw_sql_usage + manage.py check pass. Last reconciled + ?5a + ?5 updated. Doc/verification only; no code change; no duplicate effort. |
| This run: 4-step (13, 14, 25, baseline) | Step 13/14/baseline lints + manage.py check ? pass. Step 25: ledger ?2 vs BACKLOG ?1 and NEXT_50 consistent (41 DONE, 11 PARTIAL, 3 NOT DONE). Optional: get_solo?resolver/allowlist-shrink ? deferred (remaining in tests/commands per site_settings_usage_inventory). Last reconciled + ?5 + ?5a updated. Doc/verification only; no duplicate effort. |
| This run: 4-step (13, 14, 25, baseline) + ?8 optional | Step 13/14/baseline pass; Step 25 ledger vs BACKLOG/NEXT_50 consistent. ?8: config/settings.py MARKETING_* + marketing_views context verified (no change). Last reconciled + ?5 updated. Doc/verification only; no duplicate effort. |
| This run: 4-step (13, 14, 25, baseline) | Step 13: lint_gilead_residue ? pass. Step 14: lint_secret_exposure ? pass. Step 25: ledger ?2 vs BACKLOG ?1 and NEXT_50 consistent. Baseline: lint_broad_except --strict + lint_raw_sql_usage + manage.py check pass. Last reconciled + ?5a updated. Doc/verification only; no code change; no duplicate effort. |
| This run: 4-step verification (13, 14, 25, lints+?10) | Step 13: lint_gilead_residue ? pass. Step 14: lint_secret_exposure ? pass. Step 25: docs_truth_ledger reconciled. lint_raw_sql_usage + lint_broad_except --strict + lint_no_print_in_apps + manage.py check pass. ?5 + ?5a + ledger updated. Doc/verification only; no code change; no duplicate effort with other agents. |
| This run: ?8 settings + Step 14 + 25 + ?2d | ?8: MARKETING_COMPARISON_TABLE and MARKETING_REPLACEMENT_MESSAGING in config/settings.py (env JSON or []/{}). ?2d: customizer redirect candidate wording fixed. Step 14 + Step 25 + lints + check pass. ?5 + ?5a + ledger updated. No duplicate effort. |
| This run: 4-step (13, 14, 25, baseline) | Step 13: lint_gilead_residue ? pass. Step 14: lint_secret_exposure ? pass. Step 25: ledger ?2 vs BACKLOG ?1 and NEXT_50 consistent. Baseline: lint_broad_except --strict + lint_raw_sql_usage + manage.py check pass. Last reconciled + ?5a updated. Doc/verification only; no code change; no duplicate effort. |
| This run: 4-step (13, 14, 25, lints+check+?10) | Step 13: lint_gilead_residue ? pass. Step 14: lint_secret_exposure ? pass. Step 25: docs_truth_ledger reconciled. lint_raw_sql_usage + lint_broad_except --strict + lint_no_print_in_apps + manage.py check pass. ?5 + ?5a updated. Doc/verification only; no code change; no duplicate effort with other agents. |
| This run: 4-step re-verification (13, 14, 25, baseline) | Step 13: lint_gilead_residue ? pass. Step 14: lint_secret_exposure ? pass. Step 25: ledger ?2 vs BACKLOG ?1 and NEXT_50 consistent. lint_broad_except --strict + lint_raw_sql_usage + manage.py check pass. Leconciled + ?5 updated. Doc/verification only; no code change; no duplicate effort. |
| This run: 4-step verification (13, 14, 25, baseline) | Step 13: lint_gilead_residue ? pass. Step 14: lint_secret_exposure ? pass. Step 25: ledger ?2 vs BACKLOG ?1 and NEXT_50 consistent. lint_broad_except --strict + lint_raw_sql_usage + manage.py check pass. Last reconciled + ?5 updated. Doc/verification only; no code change; no duplicate effort. |
| This run: 4-step (13, 14, 25, baseline) | Step 13: lint_gilead_residue ? pass. Step 14: lint_secret_exposure ? pass. Step 25: ledger ?2 vs BACKLOG ?1 and NEXT_50 consistent. Baseline: lint_broad_except --strict + lint_raw_sql_usage + manage.py check pass. Last reconciled + ?5a updated. Doc/verification only; no code change; no duplicate effort. |
| This run: 4-step (14, 25, baseline) | Step 14: lint_secret_exposure ? pass. Step 25: docs_truth_ledger ?2 verified against BACKLOG ?1 and NEXT_50. lint_broad_except --strict + lint_raw_sql_usage + manage.py check pass. Last reconciled + ?5a updated. Doc/verification only; no code change; no duplicate effort. |
| This run: 4-step verification (Step 14 + Step 25 + lints + check) | Step 14: lint_secret_exposure ? pass; Step 25: ledger ?2 vs BACKLOG ?1 and NEXT_50 consistent; lint_raw_sql_usage + lint_broad_except --strict + manage.py check pass. docs_truth_ledger ?4 Last reconciliation updated. No code change; no duplicate effort. |
| This run: 4-step (13, 14, 25, lint+check) | Step 13: lint_gilead_residue ? pass. Step 14: lint_secret_exposure ? pass. Step 25: docs_truth_ledger reconciled with BACKLOG ?1 and NEXT_50. lint_broad_except --strict + manage.py check pass. Last reconciled + ?5 updated. No code change; no overlap with other agents. |
| This run: Step 6 subtractive cleanup | ensure_gilead_admin command removed; all doc references updated to ensure_default_tenant_admin; SUBTRACTIVE_CLEANUP_RELEASE_NOTES row added. NEXT_50 step 6 ? PARTIAL. No duplicate effort. |
| This run: Step 14 verification | Step 14: lint_secret_exposure re-run ? pass. lint_broad_except --strict + manage.py check pass. Last reconciled + ?5 updated. No code change; no overlap with other agents. |
| This run: Step 7 (csrf_exempt/AllowAny audit) | Step 7: Audit complete ? all csrf_exempt and AllowAny entries justified (keep/keep_with_hardening); no unnecessary exemptions for removal. NEXT_50 step 7 ? PARTIAL; public_endpoint_audit ?4 + docs_truth_ledger updated. lint_csrf_exempt_usage + lint_allow_any_usage pass. Doc-only; no code change. |
| This run: Step 14 + Step 25 | Step 14: lint_secret_exposure.py run ? pass (no client-side or tracked-config secret exposure). Step 25: Ledger ?2 cross-checked with BACKLOG ?1 and NEXT_50; Last reconciled updated. Doc/verification only; no code change. No duplicate effort. |
| This run: Step 14 + Step 25 + ledger | Step 14: lint_secret_exposure.py re-run ? pass. Step 25: docs_truth_ledger Raw SQL replacement targets ? PARTIAL (all wraps DONE; remaining allowlist-shrink). lint_broad_except --strict + manage.py check pass. Last reconciled + ?5 updated. No code change; no overlap with other agents. |
| Step 13 verification (this run) | Migration 0155 already applied (showmigrations); lint_gilead_residue.py ? pass. NEXT_50 step 13 ? PARTIAL (apply in staging/prod at deploy). BACKLOG snapshot + Next logical steps updated. No code change. No duplicate effort. |
| Step 14 + Step 25 (this run) | Step 14: lint_secret_exposure.py run ? pass. Step 25: docs_truth_ledger ?2 verified aligned with BACKLOG ?1 and NEXT_50; Last reconciled + ?3 Step 14 latest-run updated. Doc/verification only; no code change. No duplicate effort. |
| Step 40 siteconfig re-exports | siteconfig/models.py re-exports: added noqa: F401 to each block so ruff check apps F401/F841 passes. Step 40 DONE; BACKLOG/ledger/NEXT_50 updated. No duplicate effort. |
| This run: Step 14 + Step 25 verification | lint_secret_exposure.py ? pass; lint_broad_except --strict pass. BACKLOG Last reconciled + ?3 Step 14 latest run confirmed. No code change; no overlap with other agents. |
| Step 9 rls_context reconciliation (this run) | Verified rls_context.py already has typed excepts in both finally blocks; added rls_context to ?1 ?2.4 and ?2 row #1 schools DONE list; Last reconciled + RUNMYCAMPUS ?2.4 updated. lint_broad_except + lint_raw_sql_usage + manage.py check pass. Doc-only; no code change. No duplicate effort. |
| Step 9 schools rls_context | rls_context.py rls_school finally: Exception ? (OperationalError, ProgrammingError, DatabaseError); allowlist 0; lint pass. BACKLOG ?1/?2, NEXT_50 step 9, broad_exception_audit updated. Ned. No overlap with other agents. |
| Step 9 schools align_tenant_config | align_tenant_config.py: 2 ? typed (persist_compiled_tenant_config, sync_tenant_modules); allowlist 0; lint_broad_except --strict pass. BACKLOG ?1/?2, NEXT_50 step 9, docs_truth_ledger updated. No overlap with other agents. |
| Step 9 schools mgmt batch | validate_marketing_urls, tenant_wind_down, migrate_tenant_schemas_one_by_one: broad except ? typed; allowlist 0; lint + check pass. BACKLOG ?1/?2, NEXT_50, docs_truth_ledger updated. |
| Step 9 schools models.py allowlist | models.py: OperationalError import for limits(); added to broad_except_allowlist at 0. Lint strict + check pass. No duplicate effort. |
| Step 17 closure (doc only) | api_tenant_maturity already uses get_effective_site_settings(request=request); marked NEXT_50 step 17 DONE; BACKLOG + ledger updated. No code change; no overlap with other agents. |
| Step 24 closure (verification only) | Governor limits + feature toggles already in operator UI: super_runtime_inspector shows both; staff-only; Control Studio rail links to it. Marked NEXT_50 step 24 DONE; BACKLOG + ledger updated. No code change; no overlap with other agents. |
| NEXT_50 step 9 sync + Step 14 verification only (no new code) | Align NEXT_50 step 9 text with ?1 schools DONE/ongoing; run lint_secret_exposure (pass); update BACKLOG ?3 + ?5. Avoid schools/accounts/siteconfig code changes to prevent overlap with other agents. |
| Portal views_ai_copilot allowlist 0 only (no code change) | Code already typed; set allowlist to 0; updated backlog/audit/NEXT_50/ledger. No work in accounts/schools/siteconfig to avoid duplicating other agents. |
| Focus: plan audit + doc consistency + full test suite | All ?1 items already have status; reconcile ledger/NEXT_50; run full tests and record result. |
| Raw SQL allowlist | No shrink this run without code changes; allowlist already reflects wrapped modules (expected_count 0 where wrapped). |
| Step 40 second app | Communication app: ruff F401/F841 (21 fixes); evals already done. No duplicate effort with other agents. |
| Plan audit this run | RUNMYCAMPUS ?1??12 and ?11 Phasrified; BACKLOG ?1 table contains every unchecked item with status; no new strategy files. |
| Resolver contract test fix | test_resolver_registry_dotted_paths_importable failed for SchemaResolver (apps.metadata.services): test required module to have attr "services". Fixed by trying import_module(loc) first so module-only paths pass; no registry change. |
| Plan audit verification (this run) | ?1: 27 rows, each has Status + Closure note. ?2: 17 ongoing/TBD items closed. ?2b: technical reasons table complete. NEXT_50 and docs_truth_ledger aligned; no duplicate strategy docs. No code change; no overlap with other agents. |
| This run: audit + full suite start | ?1 verified; full test suite invoked (long-running); lint + check passed; no new code; no duplicate effort. |
| This run: Step 40 compliance | Compliance app ruff F401/F841 (65 fixes); AuditLog re-export in compliance/models.py for portal. BACKLOG ?5, NEXT_50, ledger, code_hygiene_ledger updated. Plan audit: ?1/?2/?2b verified. manage.py check passes; full test suite run in CI or when DB free (local DB locked). |
| This run: Step 40 events | Events app ruff F401/F841 (2 fixes). NEXT_50, ledger, code_hygiene_ledger updated. Plan audit confirmed; full test suite below. |
| This run: Step 40 customersuccess | CustomerSuccess app ruff F401/F841 (4 fixes). NEXT_50, ledger, code_hygiene_ledger, BACKLOG ?5 updated. No overlap with other agents. |
| This run: Step 40 analytics | Analytics app ruff F401/F841 (16 fixes). NEXT_50, ledger, code_hygiene_ledger, BACKLOG ?5 updated. manage.py check passes. No overlap with other agents. |
| This run: Step 40 academics | Academics app: scheduling_solver (F401/F841 ? drop unused imports, use importlib.util.find_spec for ortools availability), seed_buea_synthetic (F841 ? prefix unused vars with _). NEXT_50 step 40, BACKLOG ?3 + Last reconciled updated. No overlap with other agents. |
| This run: Step 40 siteconfig | Siteconfig app: ruff F401/F841 (183 auto + 10 manual _prefix). admin, contrast_guard, geoip_service, check_accessibility, tasks, test_admin, views_custom_requirement. ruff apps/siteconfig F401/F841 passes. BACKLOG ?5, NEXT_50, code_hygiene_ledger updated. manage.py check: pre-existing EducationSystemProfile import error unchanged. |
| This run: Step 40 automation | Automation app ruff F401/F841 (4 fixes). NEXT_50, docs_truth_ledger, code_hygiene_ledger, BACKLOG ?5 + Last reconciled updated. manage.py check passes. No overlap with step 41 (bounded console). |
| This run: Step 40 reports | Reports app ruff F401/F841 (30 fixes). NEXT_50, ledger, code_hygiene_ledger, BACKLOG ?5 + Last reconciled updated. No overlap with other agents. |
| This run: Step 40 marketplace | Marketplace app ruff F401/F841 (7 fixes). NEXT_50, ledger, code_hygiene_ledger, BACKLOG ?5 + ?1 ?10 updated. No overlap with other agents. |
| This run: Step 40 orchestration + apicenter | Orchestration runners F401 (1); apicenter migration 0003 F841 (_APIService). BACKLOG ?5, NEXT_50, ledger, code_hygiene_ledger updated. No overlap with other agents. |
| This run: Step 9 schools middleware + super_views | schools/middleware.py: 3 broad excepts ? typed (_cache_get_optional/_cache_set_optional: ImportError, AttributeError, TypeError, ConnectionError, ValueError, RuntimeError; SchoolDomain: DatabaseError). super_views.py already typed (CONTROL_PLANE_* etc.). Allowlist set to 0 for both. Lint strict pass. BACKLOG ?1/?2, NEXT_50, broad_exception_audit, docs_truth_ledger updated. |
| This run: Step 40 tenancy + Step 14 | Tenancy tests: ruff F401 (2 fixes). lint_secret_exposure: pass. BACKLOG ?5, ?5a, NEXT_50, code_hygiene_ledger, docs_truth_ledger updated. No overlap with other agents. |
| This run: Step 40 finance | Finance app: ruff F401/F841 (46 fixes). urls.py imports finance_reports, submit_report_request from .views_reports. BACKLOG ?5, Last reconciled, NEXT_50, code_hygiene_ledger, docs_truth_ledger updated. No overlap with other agents. |
| This run: Step 9 portal views_ai_gateway | portal/views_ai_gateway.py: GATEWAY_VIEW_ERRORS tuple (AttributeError, DatabaseError, ImportError, TypeError, ValueError, OSError, ConnectionError, RuntimeError, KeyError); _actor_roles (AttributeError, DatabaseError, TypeError); _log_gateway_audit (DatabaseError, IntegrityError, AttributeError, TypeError, ValueError); all view handlers except GATEWAY_VIEW_ERRORS after json.JSONDecodeError. Allowlist 0. Lint strict pass. BACKLOG ?1/?2, NEXT_50, broad_exception_audit, docs_truth_ledger updated. |
| This run: manage.py check unblock | siteconfig.models re-exports (FeatureToggleDefinition/State, TourStep, CountryMultiplier, ReportCardStyleAssignment, HolidayCalendar). Circular-import fixes: context_processors, forms, dashboard_resolver, communication/views_announcements, portal/views ? models_support (or defining module). manage.py check passes. BACKLOG ?3, ?5, ?5a updated. |
| This run: Siteconfig import fixes (manage.py check) | brand_experience: import from models_global_experience + models_tooling (avoid circular via siteconfig.models). runtime_blueprints: from models_tooling. policies_rules: from models_feature_controls. plans_entitlements: from models_platform_catalog. integrations_marketplace: from models_tooling + models_platform_catalog. siteconfig admin: ReportCardStyleAssignment from academics.models. siteconfig forms: PORTAL_FEATURE_* + default_dashboard_widgets + get_dashboard_widget_choices from models_support; ReportCardStyleAssignment from academics. views_custom_requirement: CustomFeatureTicket, get_feature_fragment_cap from models_platform_catalog. communication: default_announcement_submit_for_approval_roles from models_support. manage.py check passes. BACKLOG ?5a updated. |
| This run: Verification + next-step audit | manage.py check pass. lint_raw_sql_usage, lint_broad_except --strict pass. Targeted test run (siteconfig/platform_runtime/compliance) hit pre-existing migration error (dupliduplicate column dismissed_at) ? not from import fixes. BACKLOG ?3 updated: lints pass; full test suite in CI or after test DB fix. Optional next: Step 40 remaining apps; pre_deploy_gate in CI. No code change; no overlap with other agents. |
| This run: accounts/urls import fix | urls.py was importing migration_run_list, migration_rollback, migration_legacy_view, legacy_data_cleaner_view, clone_year_setup, rollover_* from .views but those live in views_migration and views_rollover. Updated to import from .views_migration and .views_rollover; merged migration_wizard into views_migration block. manage.py check and apps.accounts.tests.test_login_and_backend_helpers pass. Unblocks test suite load (config/urls ? accounts/urls). BACKLOG Last reconciled updated. No overlap with other agents. |
| This run: siteconfig bounded-context test fix | test_siteconfig_operator_surfaces_use_successor_theme_owner_model expected siteconfig_admin.BrandProfile and .BrandSettings; those are no longer on siteconfig.admin (registered in brand_experience.admin only). Removed those two assertions; kept ThemePack and ReportCardStyle successor checks. Comment added that BrandProfile/BrandSettings live in brand_experience.admin. Full test run still hits other env issues (e.g. schools super_urls super_migration_cloud). No overlap with other agents. |
| This run: Step 40 schools | Schools app: ruff F401/F841 (50: 43 auto + 7 manual _prefix). super_urls imports from super_views_catalog and super_views_migration. BACKLOG ?5, Last reconciled, NEXT_50, code_hygiene_ledger, docs_truth_ledger updated. No overlap with other agents. |
| This run: Step 14 verification + test env note | Step 14: `lint_secret_exposure.py` run ? pass. lint_raw_sql_usage, lint_broad_except (allowlist + strict) pass. Targeted tests: without --keepdb ? WinError 32 (DB in use); with --keepdb ? NameError in siteconfig/migrations/0090 (line 294: `apps` not defined for default_education_term_labels; migration needs serializable default). BACKLOG ?3 updated. No code change; migration 0090 fix deferred to avoid touching migration history. |
| This run: Step 40 accounts | Accounts app: ruff F401/F841 (46). urls: workflow from .views_workflow; migration_wizard from .views_migration. BACKLOG ?1 ?10, NEXT_50, code_hygiene_ledger, docs_truth_ledger updated. |
| This run: Steps 37, 41, 42 | **Step 37:** API Center governance ? dashboard shows api_quotas + rate limits table; apps.apicenter.tests.test_governance_contract (auth, Integration); apicenter_integration_governance.md actions/checkboxes updated. **Step 41:** Automation bounded console ? automation/views.outcomes_console, automation/urls, templates/automation/outcomes_console.html; config/urls automation/; Studio OS automation rail "Outcomes" first; staff-only. **Step 42:** broad_except_allowlist.json policy + issue_link; finance/accounts already 0; lint_broad_except --strict pass. NEXT_50 steps 37, 41, 42 ? DONE. BACKLOG Last reconciled updated. |
| This run: Step 40 portal + schools super_urls | portal: ruff F401/F841 (82); urlconf from .views_parent_finance and .views_onboarding; verify_onboarding_setup redundant import removed. schools/super_urls: super_migration_cloud, super_migration_profile_registry, super_migration_rollback from .super_views_migration (fix AttributeError after portal url load). manage.py check passes. BACKLOG ?5, ?5a, ?10, NEXT_50, code_hygiene_ledger, docs_truth_ledger updated. No overlap with other agents. |
| This run: Step 40 api + accounts url fix | api: ruff F401/F841 (31); urlconf intervention from views_v1_intervention. accounts/urls: migration views from .views_migration. manage.py check passes. BACKLOG ?5, ?5a, ?10, NEXT_50, code_hygiene_ledger, docs_truth_ledger updated. |
| This run: Step 40 portal + schools urlconf | portal: ruff F401/F841 (82); urlconf from views_parent_finance + views_onboarding; verify_onboarding_setup redundant import removed. schools/super_urls: super_migration_* from super_views_migration. manage.py check passes. BACKLOG ?5, ?5a, ?10, NEXT_5cs_truth_ledger updated. No overlap with other agents. |
| This run: Step 9 schools tasks | schools/tasks.py: broad excepts ? ImportError (kombu), _PROVISIONING_FAILURES, typed tuples for sync_school_domains, _record_school_event, hydrate, sync_tenant_modules, persist_compiled_tenant_config, classroom_seed, invalidate_policy_cache, welcome_email_task.delay. Allowlist 0. Lint strict pass. BACKLOG ?1/?2, NEXT_50, broad_exception_audit, docs_truth_ledger, RUNMYCAMPUS ?2.4 updated. |
| This run: Step 40 policies | policies app: ruff F401/F841 (7 auto). manage.py check passes. BACKLOG ?1 ?10, NEXT_50 step 40, code_hygiene_ledger, docs_truth_ledger updated. No overlap with other agents. |
| This run: Full codebase link/dashboard/UX audit | Extended smoke URL tests (public_support_hub, public_verify_hub, studio_os:experience, siteconfig:dashboard_hub, compliance:dashboard, communication:group_list). Verified config/urls and app urlconfs; error templates (403/404/500) extend base and stay in frame; template {% url %} names match portal/accounts/evals/siteconfig/communication urlconfs. ?3 Platform integration & UX audit updated with seed commands and merge-readiness steps. No duplicate work with other agents. |
| This run: ?2.4 schools marketing_views + exempt webhook audit | marketing_views: 8 except Exception ? typed; allowlist 0; lint_broad_except --strict pass. Lead capture API: audit logging (lead_capture_created/lead_capture_duplicate). public_endpoint_audit.md and docs_truth_ledger updated; ?1 ?2.4 rows updated. No overlap with other agents. |
| This run: Step 9 schools welcome_email | schools/welcome_email.py: 2 broad excepts ? typed (brand: ImportError/AttributeError/TypeError/ValueError/KeyError; send: OSError/SMTPException). Allowlist 0; lint_broad_except --strict pass. BACKLOG ?1/?2, NEXT_50 step 9, docs_truth_ledger updated. No overlap with other agents. |
| This run: Step 9 schools celery_tasks | schools/celery_tasks.py: 3 broad excepts ? typed (get_active_school_ids, _resolve_client, _run_with_tenant_context: ImportError, ObjectDoesNotExist, DatabaseError, OperationalError, AttributeError, TypeError). Allowlist 0; lint_broad_except --strict pass. BACKLOG ?1/?2, NEXT_50 step 9, docs_truth_ledger updated. No overlap with other agents. |
| This run: Step 9 schools funnel_events + control_plane_lifecycle | funnel_events.py: 1 ? typed (IntegrityError, DatabaseError, OperationalError, ValidationError); control_plane_lifecycle.py: 1 ? typed (AttributeError, ObjectDoesNotExist). Allowlist 0; lint pass. BACKLOG ?1/?2, NEXT_50 step 9, docs_truth_ledger updated. No overlap with other agents. |
| This run: Step 9 schools rls_context + health_repository | rls_context.py: 2 ? typed (RESET in rls_school/rls_bypass: OperationalError, ProgrammingError, DatabaseError); health_repository check_table_exists: 1 ? typed. Allowlist 0; lint pass. BACKLOG ?1/?2, NEXT_50 step 9, docs_truth_ledger updated. No overlap with other agents. |
| This run: Phase ?3.1/?3.2, ?5??7, ?8/?12, Step 40 | Step 40: runtime_blueprints (3 F401 auto). ?5??7: MARKETPLACE_SEED_TARGETS ?4 action table (7 items). ?8/?12: MARKETING_FRONT_PLACEHOLDER ?4 action table (9 items; hero + why_switch DONE). ?3.1/?3.2: BACKLOG ?2d legacy-path and runtime checklist (4 actions). BACKLOG ?1 ?10, Last reconciled, code_hygiene_ledger, NEXT_50 updated. No overlap with other agents. |
| **This run: 10-step (migrations + lints + tests)** | siteconfig **0156** created (subject_seed/term_labels alter, serializable defaults) and applied. Reports 0017 not created (deferred). lint_secret_exposure, lint_raw_sql, lint_broad_except --strict, manage.py check pass; 66 tests OK. BACKLOG + ?5a + ledger updated. No overlap. |
| This run: Step 9 siteconfig + evals broad except | siteconfig/templatetags/feature_control.py: except Exception ? (ImportError, AttributeError, TypeError, ValueError, KeyError); evals/performance_optimization.py _tenant_prefix: ? (ImportError, AttributeError, TypeError). Allowlist 0 for both; lint_broad_except --strict + lint_secret_exposure + lint_raw_sql + manage.py check pass. BACKLOG ?1/?2, NEXT_50 step 9, docs_truth_ledger updated. No overlap with other agents. |
| This run: Step 9 brand_experience + portal + siteconfig | brand_experience/design_studio.py: (ImportError, AttributeError, TypeError, ValueError, KeyError); portal/templatetags/portal_breadcrumb.py: split + breadcrumb_label (TypeError, AttributeError, ValueError); siteconfig/report_template_engine.py: (TemplateSyntaxError, KeyError, ValueError, TypeError, AttributeError). Allowlist 0 for all three. lint_broad_except --strict + lint_secret_exposure + lint_raw_sql + manage.py check pass. BACKLOG ?1/?2, NEXT_50, broad_exception_audit updated. No overlap with other agents. |
| **This run: ?2.1 completion checklist + verification** | ?2.1: BACKLOG ?1b checklist added (replace get_solo DONE; delete legacy paths DONE for agreed scope; move ownership NOT DONE; gates NOT DONE). RUNMYCAMPUS "Delete legacy paths" note updated. Verification: lint_tenant_settings --check-get-solo-only, lint_secret_exposure, lint_broad_except --strict, lint_raw_sql, manage.py check ? all pass. Last reconciled + ?5 + docs_truth_ledger updated. Doc/verification only; no code change; no duplicate effort. |

---

**Reconciliation with uncommitted work (git status):** Snapshot and ?1 table above reflect current working tree. Uncommitted changes include: broad_except replacements (siteconfig/templatetags/feature_control, evals/performance_optimization, brand_experience/design_studio, portal/templatetags/portal_breadcrumb, siteconfig/report_template_engine, studio_os/views); allowlist entries for those at 0; migrations 0155, 0156; repositories (schools/health_repository, siteconfig/repositories, portal/onboarding_verification); deleted ensure_gilead_admin, siteconfig/webhook_delivery; Step 36 catalog_counts/marketplace UI; many app and doc edits. All such work is reflected in DONE/PARTIAL/NOT DONE above.

*Source of truth: [RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH.md](RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH.md) ?9, ?12.*
[RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH.md](RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH.md) ?9, ?12.*

, siteconfig/webhook_delivery; Step 36 catalog_counts/marketplace UI; many app and doc edits. All such work is reflected in DONE/PARTIAL/NOT DONE above.

*Source of truth: [RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH.md](RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH.md) ?9, ?12.*
[RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH.md](RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH.md) ?9, ?12.*
F_TRUTH.md) ?9, ?12.*
EXECUTION_SOURCE_OF_TRUTH.md) ?9, ?12.*
[RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH.md](RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH.md) ?9, ?12.*
F_TRUTH.md) ?9, ?12.*
pp and doc edits. All such work is reflected in DONE/PARTIAL/NOT DONE above.

*Source of truth: [RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH.md](RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH.md) ?9, ?12.*
[RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH.md](RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH.md) ?9, ?12.*
F_TRUTH.md) ?9, ?12.*
EXECUTION_SOURCE_OF_TRUTH.md) ?9, ?12.*
[RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH.md](RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH.md) ?9, ?12.*
F_TRUTH.md) ?9, ?12.*
E_EXECUTION_SOURCE_OF_TRUTH.md](RUNMYCAMPUS_SINGLE_EXECUTION_SOURCE_OF_TRUTH.md) ?9, ?12.*
F_TRUTH.md) ?9, ?12.*
