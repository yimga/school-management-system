# CLAUDE.md — RunMyCampus engineering guide for Claude Code

This file is read by Claude Code at session start. Keep it short, durable, and load-bearing.

## What this codebase is

RunMyCampus is a multi-tenant SaaS school management platform. **Active code lives in `beta/school-management-system/`** — the `Live Code/` folder at the workspace root is empty and not in use. "The platform" includes:

- `runmycampus.com` — public marketing surface (cream / editorial / Source Serif 4)
- `manager.runmycampus.com` — control plane for super-admin / operator workflows (dark)
- Tenant subdomains — portal / backend / teacher / parent / student / studio_os shells (indigo + emerald + Inter, recessive so tenant brand wins)

Nothing about the platform is "off the table" — marketing, control plane, and tenant surfaces are all in scope. Each has its own design language but all must be world-class.

## Engineering best-practices for "big jobs"

Heavy workflows (large refactors, multi-template polish waves, platform-wide sweeps, end-to-end feature builds) are highly dependent on how the Claude Code session is set up. Protect them by following these three practices:

### 1. Clear session pollution before massive refactors

If a session is already long or carrying unresolved errors, run `/clear` *before* starting a heavy run. This wipes stale conversational history and maximizes the remaining token context available for raw code generation. Don't `/clear` mid-task — but at the start of a fresh big job in a polluted context, clearing first is the right move.

### 2. Isolate and scope explicitly

Don't point Claude Code at the entire monorepo when the task is bounded. Use explicit directory targets so the model's focus stays narrow:

- **Routing work:** `apps/<app>/urls.py` + `config/urls.py` — not the whole tree.
- **Theme / token work:** `static/css/design-tokens.css` first, then the surface shell.
- **Marketing surface:** `templates/marketing/` + `static/marketing/` + `apps/schools/marketing_views*.py`.
- **Cascade work (tenant config):** `apps/platform_runtime/models.py` + first-class resolver + `apps/siteconfig/`.
- **Exploration:** use `Grep` / `Glob` with `path=` set to the narrow subtree; reserve open-ended scans for the Explore subagent with breadth specified.

### 3. Pre-seed configurations before initiating the run

Confirm the precondition state before a heavy structural change:

- `CLAUDE.md` (this file) is up to date.
- `static/css/design-tokens.css` is the canonical foundation — semantic surface tokens (`--surface-bg/canvas/elevated/popover`, `--text-*`, `--hairline`, `--elev-1/2/3`, `--material-blur`) are defined there, **never duplicated** in per-shell stylesheets.
- For tenant-configurable values, the **cascade lands first**: `RuntimeDefaults` typed column → migration → `RUNTIME_DEFAULTS_FIRST_CLASS_FIELD_NAMES` tuple → `EXACT_FIELD_OWNERS` map → `siteconfig.models.SiteSettings` brand_payload tuple → context processor (`apps/siteconfig/context_processors.py`) → meta-tag bridge (`templates/partials/rmc_theme_meta.html`) → CSS custom property on `<html>` (set by `static/js/theme-preference-bootstrap.js`) → CSS rule that consumes it.
- Template adoption work happens **after** the underlying primitives are settled — never alongside.

## Non-negotiable directives

These rules are durable user instructions. Don't relitigate them per-task.

### No hardcoding

Nothing on the platform may be hardcoded. Every value routes through the 7-layer configurability contract (tenant `SiteSettings` → env var → user prefs → i18n → feature flag → DB fixture → platform constant). When in doubt, push the value up the cascade. Inline hex literals, magic numbers, and role strings are all in scope for the no-hardcoding rule.

### Apple-tier polish, world-class delivery

Nothing is assumed. Luxury / premium aesthetic is the bar. The semantic token system + the `.rmc-*` component grammar (data-table, empty, skeleton, cmdk, section-nav, segmented, sheet, kbd-cheatsheet, metric-ticker, page-progress, bottom-sheet, etc.) is the canonical foundation — extend it, don't fork it.

### Clean up after yourself

Removed code does not leave behind retired `// removed` comments, dead `_unused` vars, unused exports, or orphan CSS rules. Find dead callers via grep before deleting; if zero references confirmed, delete cleanly and add one retirement note in `docs/CSS_RETIREMENT_DOCKET.md` (the SOT for that work).

### Surface scope violations early

If a request would silently widen scope (e.g. "sweep dashboards" naturally implicates 100+ templates), name the breadth and the strategic subset *before* sweeping. The user values progress over exhaustive coverage, but only if the trade-off is visible.

## Sources of truth

- **Platform parity sweep + adoption + 6-agent completion push (v3.57.0 → v3.57.11, 2026-05-21 → 2026-05-22):** [`docs/CSS_RETIREMENT_DOCKET.md § v3.57.11`](docs/CSS_RETIREMENT_DOCKET.md#2026-05-22--v35711-six-agent-parallel-completion-push) and [`docs/DEFERRED_v3_57_EXTERNAL.md`](docs/DEFERRED_v3_57_EXTERNAL.md) — v3.5x wave progression closing the platform parity surface area in 7 monotonic patches. v3.57.0 (in-repo continuation after Anthropic quota wall mid-fan-out): 2 cockpit helpers (front_office_200x 10 keys + tenant_v3_extended 10 keys, disjoint from v3.56) wired into `cockpit_context.py`, 3 observability service helpers (sparkline/slo_clocks/ai_copilot stub), 3 CSS bundles (rmc-pagination-grammar / rmc-print-v2 / rmc-email-civic), 2 zero-tolerance scanners (`scan_color_contrast.py` WCAG AA baseline 0 + `scan_horizontal_overflow_risk.py` baseline 55 drift detector); deferred agent-only items catalogued in `docs/DEFERRED_v3_57_EXTERNAL.md`. v3.57.1 (same-day adoption wave): 3 v3.57.0 CSS bundles wired into 4 shells, admin form +20 enable toggles via `_FIELD_TO_KEY` round-trip pattern, 4 NEW zero-tolerance scanners baseline 0 (`scan_email_plaintext_twin` / `scan_sms_template_length` / `scan_pdf_brand_cascade` / `scan_pwa_install_prompt_coverage`), 47-site horizontal-overflow burndown 55→0 via right-to-left 2-pass codemod (lesson durably captured). v3.57.2: cockpit design previews shipped to operators via staff-gated index at `/siteconfig/super/configure/cockpit/previews/` (2 CBVs + slug-map path-traversal-safe serve). v3.57.8: shell parity — civic footer -10% vertical density + help drawer cross-shell close-on-outside-click fix + sidebar 200x token retrofit (4 files). v3.57.9: preview parity wave — `platform_pulse` wired in manager skeleton + 10 v3 extended sections wired in `portal_base` via new block + multi-line `{# #}` regression re-fixed (3 files). v3.57.10: landing-only cockpit + strip floating chrome — cockpit sections moved out of skeleton + base into landings only, 10 tenant-v3-extended sections moved into role-specific landings, 4 floating UI elements stripped from all 4 shells (10 files). v3.57.11 (today, 6-agent parallel completion push, 28 files): PDF print-v2 adoption in 3 templates + email-civic adoption in 6 templates + pager retirement layering on Django admin + ~8 bespoke sites (3 originally-listed forks `.portal-page-pager` / `.bk-dash-pager` / DRF Redoc confirmed absent from tree by Agent C — likely mass-purged earlier; closed item) + cockpit per-section rich editor for 4 sections (`lod_*`/`asb_*`/`tsc_*`/`ues_*`) + header/ticker chrome parity (NPS / revenue ticker moved from universal header to manager landing only) + token cascade v8 preview parity (12 `cp-chrome-*` + warning / danger / success extended). All 8 zero-tolerance scanners green; 0 migrations across the entire v3.57.x progression; SW monotonically bumped each patch.
- **Migration Cloud platform trust wave (v3.39.0, 2026-05-19):** [`docs/CSS_RETIREMENT_DOCKET.md`](docs/CSS_RETIREMENT_DOCKET.md#2026-05-19--v3390-migration-cloud-platform-trust-wave-5-agent-parallel-fan-out-non-css-wave) — 5-agent parallel fan-out (9th consecutive MC wave: v3.26→v3.28→v3.31→v3.32→v3.33→v3.34→v3.37→v3.38→v3.39) addresses v3.38.0's actionable deferrals while skipping items blocked on counsel signoff or 90-day timers. SW `sms-v3.39.0-migration-cloud-platform-trust-2026-05-19`. (1) **Agent 1 — Weekly audit-chain verifier beat + counsel-pending retention purge:** `verify_audit_chain` mgmt command gained `--all-tenants` + `--email-on-broken=<addr>` (4-tuple walker return populating broken-chain/broken-sig/unsigned-legacy/events-checked); new `apps/migration_cloud/tasks_audit.py::verify_audit_chain_weekly_task` `@shared_task` w/ Celery beat `accounts-verify-audit-chain` `crontab(hour=2, minute=0, day_of_week="mon")` lazy-guarded; new `purge_audit_events_pre_approved.py` mgmt command refuses to run when `MIGRATION_CLOUD_AUDIT_PURGE_APPROVAL_TOKEN` env empty (constant-time-compared via `hmac.compare_digest`; counsel-pending message + exit 1), dry-run default, `--apply` uses raw SQL `DELETE` bypassing the model's append-only `delete()` guard, then emits meta-audit-event `audit.retention_purge_applied` w/ `payload_summary={tenant_prefix, rows_purged, cutoff_iso, approval_fingerprint=sha256[:12]}` (keys deliberately avoid `_sanitize_payload`'s rejection list); new TextChoice `AUDIT_RETENTION_PURGE_APPLIED` in `models_audit.py`; PII-free email body (12-hex tenant prefix + first-broken UUID + counts — never raw bytes); 14 Django tests. (2) **Agent 2 — Audit completeness + per-event `root_key_signature` HMAC-SHA512 w/ HSM-pluggable backend:** migration `0021_audit_event_root_key_signature.py` pure AddField, nullable CharField(128); `apps/migration_cloud/services/audit_root_signing.py::compute_root_signature/verify_root_signature` HMAC-SHA512 over **same canonical-JSON pre-image `integrity_hash` covers** (different algorithm prevents single-key compromise breaking both); backend selector `MIGRATION_CLOUD_AUDIT_SIGNING_BACKEND` defaults to `local-env-key`; 4 reserved HSM values `aws-kms`/`azure-keyvault`/`hashicorp-vault`/`gcp-kms` raise `NotImplementedError` w/ "configure HSM bridge first" message; key decoder accepts base64/hex/raw; `models_audit.py` wires signing into `save()` AFTER `integrity_hash` is computed but BEFORE `super().save()` in the same atomic block (`NotImplementedError` propagates — refuses silent HSM downgrade; all other failures land unsigned + WARNING); legacy events `None`. New `MigrationCloudWebhookDeactivateView` at `/super/migration/operator/webhooks/<sub_id>/deactivate/` (`# rbac-allow: super-staff-webhook-subscription-deactivate`) emits `webhook.subscription.deleted` via `_safe_audit`; REST `WebhookSubscriptionViewSet.destroy()` ALSO emits with `via=api` discriminator (best-effort, never breaks API path). `apps/accounts/auth_backends_legacy.py::LegacyHashUpgradeBackend.authenticate` success branch emits `legacy_hash.decrypt` w/ `payload_summary={"vendor": legacy_algo}` (username SHA-256-hashed inside manager — raw username never lands in DB); try/except w/ `legacy_hash_upgrade_audit_failed` WARNING — auth never breaks on audit failure. `verify_audit_chain` extended w/ `--check-root-signature` flag; **exit-code split**: chain broken=1, chain ok + signature mismatch=**2** (backup-restore tamper signal), chain ok + signatures NULL=0 with `unsigned_legacy=<n>` note. `MigrationCloudAuditExportView` extended w/ `?verify_root_signature=1` adding tri-valued `_root_signature_verified: true|false|null` per JSONL line (null = unsigned legacy). 2 new env settings via `os.environ.get(...)` only; `docs/MIGRATION_CLOUD_AUDIT_LOG.md` +110 lines "Root-key signature (v3.39.0)" section; `docs/SECURITY_KEYS.md` +80 lines "Audit-event root-key signature" section; 17 Django tests. (3) **Agent 3 — Canonical-headers CI hash-lock scanner + companion-extension icons:** new `scripts/scan_companion_canonical_headers_drift.py` stdlib-only (`ast`/`json`/`pathlib`/`argparse`/`sys`), AST-parses `DOMAIN_CANONICAL_HEADERS` from Django SOT, compares both companion JSON mirrors order-sensitive; drift classes `identical`/`missing-in-mirror`/`extra-in-mirror`/`order-mismatch`/`column-set-mismatch`; honors per-domain `_canonical-headers-drift-allow` JSON-native block OR `// canonical-headers-drift-allow: <domain> -- <reason>` leading comment; flags `--strict`/`--json`/`--update-baseline`; exits 0 on clean tree. New baseline `var/security-audit-baseline-canonical-headers-drift.json` ({finding_count: 0, generated_at: 2026-05-19}). `.github/workflows/architectural-boundaries.yml` gains 7 paths + new job `canonical-headers-drift` after `pii-logging-smell`. New `scripts/generate_companion_extension_icons.py` stdlib zlib+struct PNG encoder writes solid RMC indigo `#4F46E5` blocks at 16/48/128; PNGs materialized at `companion-extension/icons/icon-{16,48,128}.png` (79/117/259 bytes, valid PNG magic `b"\x89PNG\r\n\x1a\n"`); new `companion-extension/icons/README.md` placeholder note. 6 stdlib unittest + 2 Django = 8 tests green. **All 23 SOT domains verified identical across both mirrors at first scan.** (4) **Agent 4 — `apps/observability/metrics.py` Prometheus/StatsD pluggable bridge:** new module (~395 lines) exports `emit_counter`/`emit_gauge`/`emit_histogram` + `_sanitize_labels` (drops sensitive VALUES regex-matched against `password`/`secret`/`token`/`signature_text`/`private_key`/`email`/`slug`; normalizes keys to `[a-z_][a-z0-9_]*`; truncates values to 64 chars); 4 backends (`noop` default / `structured-log` formalized as a real backend / `prometheus-client` lazy-imported via `importlib.util.find_spec`, auto-fallback to structured-log + one-time WARNING when lib missing / `statsd` lazy-imported, same fallback semantics); idempotent cached `_get_backend()` selection. Pluggable via `OBSERVABILITY_METRICS_BACKEND` env. New `services/observability.py` thin re-export shim satisfies v3.38.0 Agent 4's introspection contract. **Rewritten** `apps/observability/__init__.py` re-exports symbols. New `PrometheusMetricsView` at `/metrics/` anonymous-readable (`# rbac-allow: prometheus-scrape-anonymous-firewall-protected`), returns `text/plain; version=0.0.4` from `generate_latest()`; 404 when `prometheus_client` missing; **lazy URL include** at module load — `/metrics/` ONLY when backend == `prometheus-client`. 4 new env settings (`OBSERVABILITY_METRICS_BACKEND` default `"noop"`, `_STATSD_HOST`, `_STATSD_PORT` 8125, `OBSERVABILITY_PROMETHEUS_NAMESPACE` `"runmycampus"`); new `docs/OBSERVABILITY_METRICS.md` (~140 lines backend matrix + label sanitization rules + deploy snippets). Backwards-compat: v3.38 introspection of both module paths resolves; legacy `tags=` kwarg accepted alongside canonical `labels=`. Prometheus backend caches Counter/Gauge/Histogram instances per `(name, label_keys_tuple)` so repeated emissions don't trip "Duplicated timeseries". 34 Django tests (12 sanitize + 2 noop + 5 structured-log + 2 auto-fallback + 4 prometheus + 1 statsd + 2 view + 1 idempotent + 3 v3.38-backcompat + 1 leak + 1 unknown-fallback) all green. (5) **Agent 5 — Tauri+Docker signed-appliance release workflows:** 3 new tag-only GitHub Actions workflows, all with `workflow_dispatch` `confirm="publish"` gate + tag glob narrow + version-anchor verification step. `release-companion-tauri-macos.yml` (tag `companion-tauri-v*`): universal `.dmg` via `cargo tauri build --target universal-apple-darwin`, Developer ID signing via Keychain import, notarization via `xcrun notarytool submit --wait` + `stapler staple`, GitHub Release attachment; secrets `APPLE_ID`/`APPLE_TEAM_ID`/`APPLE_APP_SPECIFIC_PASSWORD`/`MACOS_CERT_P12_BASE64`/`MACOS_CERT_PASSWORD`. `release-companion-tauri-windows.yml` (same tag glob): Authenticode sign + `signtool verify /pa` per artifact; secrets `WIN_CERT_PFX_BASE64`/`WIN_CERT_PASSWORD`/`WIN_SIGN_TIMESTAMP_URL` default DigiCert. `release-companion-docker.yml` (tag `companion-docker-v*`): `docker buildx build --platform linux/amd64,linux/arm64`, push to `ghcr.io/runmycampus/companion-docker:<tag>`, Cosign keyless sign via `sigstore/cosign-installer` + `cosign sign --yes` (`id-token: write` OIDC, NO PFX secrets) + `provenance: true` + `sbom: true`. New operator bash verifier scripts `companion-tauri/scripts/verify_signed_build.sh` (uname-dispatched `spctl --assess --type install`/`signtool verify /pa`) + `companion-docker/scripts/verify_signed_image.sh` (`cosign verify --certificate-identity-regexp "^https://github.com/runmycampus/.*" --certificate-oidc-issuer "https://token.actions.githubusercontent.com"`; env-overridable identity regex + OIDC issuer for forks). New `scripts/preflight_signed_release.py` stdlib-only CLI validates tag/version alignment + CHANGELOG entry + workflow files present & YAML-parseable + GitHub API secret-name check when token present. Version anchors aligned to `3.39.0` in `companion-tauri/src-tauri/Cargo.toml::version` + `companion-docker/app/__init__.py::__version__`. New `companion-tauri/CHANGELOG.md` + `companion-docker/CHANGELOG.md` (v3.39.0 entries). New `docs/COMPANION_SIBLINGS_SIGNED_RELEASE.md` (~280 lines: cert procurement [Apple Developer Program + EV/OV CA reseller + Sigstore keyless], secret provisioning, tag procedure, verifier-script usage, supply-chain trust model, explicit "what a signature does NOT prove" — signing proves binary was built by RMC infra, NOT bug-free). `docs/COMPANION_SIBLINGS.md` appended signed-release section. Live verification: `python scripts/preflight_signed_release.py companion-tauri-v3.39.0` → "OK: pre-flight clean"; `companion-docker-v3.39.0` same. 12 Python preflight tests + 1 bash harness = 13 green. **Cross-agent verification:** 5/5 modules import OK; `py_compile`+`ast.parse` clean on all touched Python; `AUTHENTICATION_BACKENDS[0]==LegacyHashUpgradeBackend` preserved; `MIGRATION_CLOUD_MAA_DEFAULT_VERSION=="v1.0"` (flip awaits counsel signoff); sole new migration `0021_audit_event_root_key_signature`; `makemigrations --dry-run --check` → "No changes detected"; **9 zero-tolerance gates clean** — adds `scan_companion_canonical_headers_drift 0` (Agent 3 new, 23 SOT domains verified identical day 1) to the prior 8; 1 new Celery beat (`accounts-verify-audit-chain` Mondays 02:00 UTC); 8 new env settings all via `os.environ.get(...)` only; 3 new tag-only release workflows all `confirm="publish"` gated. Test totals: **14 Django (audit verifier+purge) + 17 Django (root-sig+emit-sites) + 6 stdlib unittest + 2 Django (canonical-headers) + 34 Django (observability bridge) + 12 Python + 1 bash (signed-release preflight) = 86 new tests.** **Honest deferred v3.40+:** counsel signoff PDF + MAA v2.0 flip (externally blocked); FACTS/Skyward write-path counsel signoff (externally blocked); SDK 1.0.0 graduation (time-blocked 90 days from 2026-05-19); HSM bridge implementation for at least one of the 4 reserved values; reproducible Tauri builds + in-toto attestations + AzureSignTool for Windows EV; Chrome Web Store / Edge Add-ons / AMO publish for `companion-extension/`; per-metric custom Prometheus histogram buckets + `/metrics/` Bearer-token auth + v3.38 byte_size shift gauge→histogram; real PNG brand icons replacing v3.39 placeholders.
- **Migration Cloud operational maturity (v3.38.0, 2026-05-19):** [`docs/CSS_RETIREMENT_DOCKET.md`](docs/CSS_RETIREMENT_DOCKET.md#2026-05-19--v3380-migration-cloud-operational-maturity-wave-5-agent-parallel-fan-out-non-css-wave) — 5-agent parallel fan-out (8th consecutive MC wave: v3.26→v3.28→v3.31→v3.32→v3.33→v3.34→v3.37→v3.38) closes v3.37.0 honest-deferred items AND adds 3 operational maturity layers (metrics + health dashboard + tamper-evident audit log). SW `sms-v3.38.0-migration-cloud-operational-maturity-2026-05-19`. (1) **Agent 1 — `companion-extension/` MV3 scaffold reconstruction:** new `package.json` (vite 5 / vitest 1.6 / typescript 5.4 / libsodium-wrappers 0.7.13), `manifest.json` (MV3, 6 SIS host_permissions for legitimate content-script DOM reading in operator's own authenticated tab — boundary preserved), `tsconfig.json` (strict, ES2022, paths for `~/lib/*` etc.), hand-rolled multi-entry `vite.config.ts` (3 entries: popup/background/content — no @crxjs dependency for vite-major stability), `vitest.config.ts` (jsdom + chrome.storage mock at `tests/setup.ts`), `popup.html` (7 element IDs matching `wireTenantSwitcher` contract), eslint/prettier defaults, `README.md` operator dev-guide. Bare honest-stub `src/background/service_worker.ts` + `src/content/content_script.ts`. Pre-existing `tenant_switcher.ts`/`popup.ts` parse cleanly under new tsconfig with zero edits. 4 vitest scaffold-health + 16 preserved = 20 total. **Honest deferral:** `icons/` PNG assets not checked in (manifest references them; operator drops in or comments out icons block). (2) **Agent 2 — Per-vendor CSV pre-processors (architectural boundary preserved):** REAL transforms replacing v3.37.0 `// honest-stub:` markers in `companion-tauri/src-tauri/src/extractors/` (Rust) + `companion-docker/app/extractors/` (Python). PowerSchool: `MM/DD/YYYY`→ISO + enrollment-status A/I→active/inactive + multi-word header normalization. Blackbaud: dot-nested header flatten + relationship-row guardian mapping + ISO timestamp date-only. Veracross: role-partitioned columns (StudentEmail vs FacultyEmail) + single-digit slash-date. Alma: JSON-in-CSV-cell parsing one-level flatten + null handling. FACTS: `\t` separator + `\r\n` lines + `MM/DD/YY` 1950-2049 window + write-path fields routed under `read_only_*` prefix preserving counsel docket. Skyward: `\r`-only line endings + ALL_CAPS_UNDERSCORE columns + `YYYYMMDD`→ISO + write-path counsel-blocked. New shared `detect_vendor()` heuristic in both langs (lowest-overlap-threshold heuristic, case-insensitive normalized headers). Wired into `canonical_csv.rs::parse_and_preprocess()` + `canonical_csv.py::parse_and_preprocess()`. `docs/COMPANION_SIBLINGS_HANDSHAKE_AND_CSV_INGEST.md` § 8 added with 6-vendor rule table. **Architectural boundary verified by import-scan test asserting zero `reqwest/httpx/requests/urllib3/aiohttp` imports in any `extractors/` module.** Determinism asserted for PowerSchool + Alma. 51 Rust inline `#[test]` + 56 Python pytest = 107 new. (3) **Agent 3 — SDK 1.0.0-rc.1 stabilization prep:** versions bumped in `pyproject.toml` + `__init__.py::__version__` (Python) and `package.json` + `index.ts::VERSION` (JS) to `1.0.0-rc.1` (NOT yet 1.0.0 — 90-day field-test window). Three new docs per package: `CHANGELOG.md` (Keep-a-Changelog, 3 entries from 0.1.0→0.2.0→1.0.0-rc.1), `STABILITY.md` (frozen public-API surface — 23 Python names, 23 JS values + 6 types; semver mapping table; 90-day deprecation policy; 3-tier stability — `verify()` stable / `verify_signature()` deprecated alias / `_`-prefixed unstable), `MIGRATION_TO_1_0.md` (zero changes for most users; `accept_legacy` default flip from `True`→`False` scheduled for 2.0). Tag-only release workflows `release-webhook-verifier-{py,js}.yml` amended with `workflow_dispatch` `confirm="publish"` gate + tag glob narrowed `*`→`v*`. Python `verify_signature()` emits `warnings.warn(..., DeprecationWarning, stacklevel=2)`; JS `verifySignature()` carries `@deprecated` JSDoc. **`LEGACY_HEADER_DEPRECATION_DATE` aligned to `2026-08-18` everywhere** — 5 occurrences updated in Python SDK source, 6 in JS SDK source, 3 in `WEBHOOK_VERIFICATION.md`; dispatcher/settings/runbook/SECURITY_KEYS/READMEs were already on 2026-08-18. New tests: `test_stability_v1_0_rc1_v3_38.py` (Python, 6 green) + `stability.v1_0_rc1.v3_38.test.ts` (JS, 6 green) + `test_legacy_deprecation_date_alignment_v3_38.py` (Django, 2 green; full suites 50/50 Py + 47/47 JS). (4) **Agent 4 — Migration Cloud metrics + health dashboard:** new `apps/migration_cloud/metrics.py` exports `_hash_tenant_id(slug) -> sha256[:12]` + 6 typed `record_*()` helpers (companion_upload / maa_sign / key_rotation / webhook_delivery / token_mint / legacy_hash_decryption). Introspects `services.observability` / `apps.observability.metrics` for `emit_counter`/`emit_gauge` callables; falls back to structured-JSON `logging.getLogger("migration_cloud.metrics").info(...)`. **Tenant slug always hashed before emission (never plaintext in metric labels).** All helpers wrapped in try/except — metric failure logs WARNING and NEVER propagates. 6 emission sites wired (smallest-diff additive): `companion_receiver.py::{MASignView,CompanionUploadView}`, `services/companion_keypair.py::rotate_active_keypair`, `api/webhook_dispatch.py::_deliver_one`, `views_token_admin.py::MigrationCloudTokenMintView`, `apps/accounts/auth_backends_legacy.py::LegacyHashUpgradeBackend.authenticate`. New `views_health.py::MigrationCloudHealthView` at `/super/migration/health/` (staff-only via `@method_decorator(staff_member_required, name="dispatch")`, `# rbac-allow: super-staff-migration-cloud-health-status`); 6 panels (webhooks last-24h by status, MAA signs last-7d by version, companion uploads last-24h tenant-hashed, active keypairs per tenant, pending legacy hash sunsets, 8 zero-tolerance scanner baselines read defensively from `var/security-audit-baseline-*.json` returning "unknown" on missing/malformed). Template `templates/migration_cloud/super/health.html` 3-col grid w/ 60s `<meta http-equiv="refresh">` auto-refresh; never renders raw tenant slugs (hash only). 18 Django tests (16 green; 2 DB-backed skipped on local stale-DB collision unrelated to wave). (5) **Agent 5 — Tamper-evident audit log:** new `MigrationCloudAuditEvent` model in `apps/migration_cloud/models_audit.py` (re-exported via `models.py`): UUIDv4 PK, `tenant_id_hash` 12-hex of `sha256(slug)`, `event_type` 10-value TextChoices (`companion.upload`/`maa.sign`/`maa.sign_attempt_draft`/`key.rotate`/`webhook.subscription.created`/`webhook.subscription.deleted`/`webhook.delivery.replay`/`token.mint`/`token.revoke`/`legacy_hash.decrypt`), `actor_id` + `event_subject_hash` (SHA-256 hex prefixes, never raw email/slug), `payload_summary` JSONField walked by `_sanitize_payload()` that rejects 14 sensitive keys (`password`/`passwd`/`pwd`/`hash`/`secret`/`token`/`ssn`/`dob`/`api_key`/`apikey`/`private_key`/`signature_text`/`email`/`slug`), `created_at_iso` populated in `save()` override, per-tenant `integrity_hash`/`prev_event_hash` chain (canonical-JSON SHA256 + `"genesis"` sentinel + `hmac.compare_digest` compares). **Append-only enforcement is real**: `save()` raises `MigrationCloudAuditEventReadOnlyError` if `self.pk is not None`; `delete()` always raises; `AuditEventManager.record()` is canonical write path wrapping `transaction.atomic()`; inherits `AppendOnlyManager` blocking `.delete()` on querysets. Migration `0020_migration_cloud_audit_event.py` pure CreateModel + 2 indexes (`(tenant_id_hash, created_at)`, `(event_type, created_at)`). 8 emission sites wired with `_safe_audit()` try/except helper (audit-log failure logs ERROR but NEVER breaks request path): companion MAA sign + draft attempt, companion upload, key rotation, webhook subscription create + delivery replay, token mint + revoke. `MigrationCloudAuditView` at `/super/migration/audit/` + `MigrationCloudAuditExportView` at `/super/migration/audit/export/` (streams `application/x-ndjson` 100-row pages; honors `?tenant=<hash>&from=<iso>&to=<iso>&verify_chain=1` — the verify flag adds `_chain_verified: true/false` per line without modifying stored hash). Management command `python manage.py verify_audit_chain --tenant=<slug> [--repair-genesis]` walks chain in `created_at` order; exit 0 clean / 1 broken; `--repair-genesis` raw-SQL-patches missing first-event sentinel ONLY. Template `audit_dashboard.html` truncates hashes to 12 chars + re-sanitizes payload before render (defense-in-depth). New `docs/MIGRATION_CLOUD_AUDIT_LOG.md` (~190 lines: schema, append-only contract, hash-chain semantics, exporter usage, 7-year FERPA retention, counsel notes on hashed actor_id correlation map maintained separately outside app for forensic correlation under DSAR/subpoena). 24 Django tests (test DB locked by stale Windows processes in sandbox; pure-function smoke + URL resolve + imports + AST-parse all green). **Cross-agent verification:** 5/5 modules import OK; `AUTHENTICATION_BACKENDS[0]==LegacyHashUpgradeBackend` invariant preserved; `AGREEMENT_VERSIONS=={"v1.0","v2.0"}` + `MIGRATION_CLOUD_MAA_DEFAULT_VERSION=="v1.0"` + `MAA_TEXT_DRAFT_VERSIONS=={"v2.0"}` (flip still NOT performed — counsel signoff pending); new migration `0020_migration_cloud_audit_event` sole new leaf; `makemigrations --dry-run --check` → "No changes detected"; **all 8 zero-tolerance gates 0** (`scan_drf_schema_coverage`/`scan_money_float`/`scan_migration_model_imports`/`scan_tenant_isolation_marker_quality`/`scan_pii_logging_smell`/`scan_print_statements`/`scan_bare_except`/`scan_subprocess_shell_true`); architectural boundary held via import-scan test asserting zero `reqwest/httpx/requests/urllib3/aiohttp` in either sibling's `extractors/`. **Honest deferred v3.39+:** counsel signoff PDF + MAA v2.0 flip; FACTS/Skyward write-path unblock (currently `read_only_*` prefix interim); SDK 1.0.0 graduation after 90-day field-test; weekly `accounts-verify-audit-chain` Celery beat; counsel-approved audit retention purge command; `webhook.subscription.deleted` + `legacy_hash.decrypt` emit sites; HSM-stored per-event root-key signature; signed Tauri appliance (Apple notarization + Windows code-signing); CI gate hash-locking `companion_*/canonical_headers.json` against Django SOT. Test totals: **20 vitest + 51 Rust + 56 Python (Docker) + 6 Py SDK + 6 JS SDK + 2 Django (date align) + 18 Django (metrics+health) + 24 Django (audit) = 183 new tests.**
- **Migration Cloud v3.34.0 honest-deferred closeout (v3.37.0, 2026-05-19):** [`docs/CSS_RETIREMENT_DOCKET.md`](docs/CSS_RETIREMENT_DOCKET.md#2026-05-19--v3370-migration-cloud-v3340-honest-deferred-closeout-5-agent-parallel-fan-out-non-css-wave) — 5-agent parallel fan-out (7th consecutive MC wave: v3.26→v3.28→v3.31→v3.32→v3.33→v3.34→v3.37; v3.35/v3.36 were marketing + glocal non-MC waves on the SW timeline) closes every v3.34.0 honest-deferred end-to-end. SW `sms-v3.37.0-migration-cloud-deferred-closeout-2026-05-19`. **Critical architectural boundary documented this wave (do not relitigate):** vendor data extraction (PowerSchool/Blackbaud/Veracross/Alma/FACTS/Skyward) lives in `companion-extension/` ONLY (operator's own authenticated browser tab = security boundary); `companion-tauri/` + `companion-docker/` handle (a) RMC platform handshake — login to RunMyCampus itself, fetch MAA, sign, sealed-box upload — and (b) canonical-CSV file ingest only (operator manually exports CSV from SIS via SIS's own export UI, drops file into appliance). Original v3.35.0 Agent 4 prompt describing programmatic SIS login in siblings was blocked by Anthropic Usage Policy cyber-content classifier; rescoped + lesson durably documented in auto-memory `feedback_companion_siblings_no_programmatic_sis_login.md`. (1) **Agent 1 — Companion popup tenant switcher + key fingerprint UI:** `companion-extension/src/lib/tenant_switcher.ts` (getKnownTenants/addTenant/setActiveTenant/removeTenant/recordFingerprint/markVerified/formatFingerprintForDisplay/fetchAndRecordPubkey with pluggable storage backend; no `any` across exports), `companion-extension/src/popup/popup.ts` (`wireTenantSwitcher(doc, cfg)` with dropdown/add/remove/refresh/verify wiring; tolerates missing DOM IDs); backend `companion_server_pubkey_view` extended with explicit `schema_context(tenant.schema_name)` wrap on `?tenant=<slug>` path + `tenant_slug` echo in JSON + info log; 6-part-hyphenated marker `# tenant-isolation-allow: companion-pubkey-anonymous-fetch-explicit-slug-lookup`; 8 Django + 14 vitest. (2) **Agent 2 — Webhook verifier SDK `accept_legacy=` API:** v3.35.0 already shipped the dispatcher-side dual-emit + `MIGRATION_CLOUD_EMIT_LEGACY_HEADERS` setting + 90-day window doc; v3.37.0 adds the customer-facing SDK API surface — `verify(headers, body, secret, *, accept_legacy=True)` in both Python (`runmycampus_webhook_verifier.verifier`) and JS (`@runmycampus/webhook-verifier`); `VerifyResult` dataclass / type carries `used_legacy_header_family` flag so subscribers can warn-log; case-insensitive header lookup; `LEGACY_*_HEADER` constants exported; `WEBHOOK_VERIFICATION.md` + `WEBHOOK_HEADER_MIGRATION_2026.md` updated; 6 Django + 4 Python SDK + 4 JS SDK (all green; 44/44 Python suite, 41/41 vitest). (3) **Agent 3 — MAA v2.0 promotion dashboard:** new `apps/migration_cloud/views_maa_promotion.py::MAA_V2_PromotionDashboardView` at `/super/migration/maa-v2-promotion/` (staff-only via `@method_decorator(staff_member_required, name="dispatch")`; 4 panels — readiness via in-process import of v3.35.0 verifier script (NOT subprocess, NOT user input), draft-status live from `settings.MIGRATION_CLOUD_MAA_DEFAULT_VERSION` + `MAA_TEXT_DRAFT_VERSIONS`, counsel attestation list, campaign progress per `campaign_version`); `templates/migration_cloud/super/maa_v2_promotion.html` uses `|truncatewords:30` so dashboard never flashes wall of operator narrative; URL added with `# rbac-allow: super-staff-view-maa-promotion-status`; 16 Django tests (5 verifier + 2 attestation hash + 2 draft regression + 4 mgmt-command + 1 attestation uniqueness + 4 dashboard view + 2 draft refusal + 1 verifier logging hygiene). (4) **Agent 4 (RESCOPED) — Tauri + Docker RMC handshake + canonical-CSV ingest:** `companion-tauri/` Rust modules `rmc_handshake.rs` (login/fetch_maa_text/sign_and_submit_maa/fetch_receipt; `Credentials` + `SessionToken` impl `ZeroizeOnDrop`; reqwest with `rustls-tls` + `cookie_store(false)` EXPLICIT; `subtle::ConstantTimeEq` for token compare) + `canonical_csv.rs` (parse_csv/match_canonical_domain ≥3-hit threshold/canonicalize_row/canonical_serialize deterministic/seal_box via sodiumoxide `crypto_box_seal`/seal_box_and_upload) + `canonical_headers.json` 23-domain mirror via `include_str!`; Tauri commands `rmc_login`/`rmc_fetch_maa`/`rmc_sign_maa`/`rmc_ingest_csv`; vanilla-TS 4-step wizard. `companion-docker/` Python mirrors `app/rmc_handshake.py` (`RMCSession.{login,fetch_maa_text,sign_and_submit_maa,fetch_receipt,clear_token}`; httpx `verify=True`; no cookie jar; `hmac.compare_digest` for verbatim text compare) + `app/canonical_csv.py` + FastAPI `POST /ingest/csv` (multipart, `Authorization: Bearer`, streams to `SpooledTemporaryFile`, never to disk). All `extractors/` directories carry `// honest-stub:` / `# honest-stub:` markers — passwords/tokens/signature_text/cell values never logged. New doc `docs/COMPANION_SIBLINGS_HANDSHAKE_AND_CSV_INGEST.md` (~190 lines) documents the architectural boundary + per-vendor manual-export procedure + sealed-box guarantee + receipt-id chain. 8 Rust inline `#[test]` + 10 Rust integration tests + 19 Python tests (14 pass + 5 skip cleanly on PyNaCl/FastAPI absence in sandbox). (5) **Agent 5 — Webhook subscription audit + replay + idempotency guard:** new `WebhookSubscriptionAuditView` (GET, staff-only, `# rbac-allow: super-staff-webhook-audit-view-deliveries`) at `/super/migration/operator/webhooks/<sub_id>/audit/` renders last 200 deliveries paginated 50/page filterable by status + date range — ALWAYS hides signature bytes and payload body content (only `signed: yes/no` pill + `payload_bytes_length` integer); new `WebhookDeliveryReplayView` (POST, staff-only, `# rbac-allow: super-staff-webhook-manual-replay`) creates fresh `MigrationCloudWebhookDelivery` with `replay_of`/`replayed_by`/`replayed_at` + FSM reset (status=pending, attempt_count=0) + re-signs against subscription's CURRENT secret via dispatcher's `_canonical_payload_bytes` + `_sign` + immediate `deliver_due_task.delay()` (returns 409 if original payload empty or original subscription deleted); `idempotency_key=""` on replay row deliberately bypasses 24h dedupe guard; `templates/migration_cloud/operator/webhook_audit.html` matches `webhook_delivery_log.html` style + `control_plane_skeleton.html` base; `SECURITY_KEYS.md` § 4 "Webhook Replay & Audit (v3.37.0)" appended; 20 Django tests (3 access + 5 view-behavior incl. assertNotIn on `b'X-RunMyCampus-Signature: sha256='` AND `b'X-Migration-Cloud-Signature: sha256='` AND payload marker bytes + 5 replay + 4 idempotency + 3 log-hygiene). Migration 0017 + idempotency guard were already in place from a prior wave — additive on top. **Cross-agent verification 5/5 modules OK; `AUTHENTICATION_BACKENDS[0]=LegacyHashUpgradeBackend` invariant preserved; `AGREEMENT_VERSIONS=={"v1.0","v2.0"}` + `MIGRATION_CLOUD_MAA_DEFAULT_VERSION=="v1.0"` (promotion plumbing wired, flip NOT performed — one-config-flip ready awaiting counsel signoff PDF); makemigrations --dry-run --check → "No changes detected"; all 8 zero-tolerance gates 0** (`scan_drf_schema_coverage`, `scan_money_float`, `scan_migration_model_imports`, `scan_tenant_isolation_marker_quality`, `scan_pii_logging_smell`, `scan_print_statements`, `scan_bare_except`, `scan_subprocess_shell_true`). **Honest deferred v3.38+:** counsel signoff PDF + actual v2.0 flip; FACTS/Skyward write-path unblock pending counsel docket; `companion-extension/` scaffolding (manifest.json/vite.config.ts/package.json — currently missing from worktree per Agent 1 report) reconstruction; per-vendor CSV pre-processors in Tauri/Docker `extractors/` (currently honest-stub by architectural-boundary design); webhook verifier SDK 0.1.0 → 1.0.0 stabilization after 90 days field-tested.
- **Migration Cloud deferred-item closeout (v3.34.0, 2026-05-18):** [`docs/CSS_RETIREMENT_DOCKET.md`](docs/CSS_RETIREMENT_DOCKET.md#2026-05-18--v3340-migration-cloud-deferred-item-closeout-5-agent-parallel-fan-out-non-css-wave) — 5-agent parallel fan-out (6th consecutive MC wave: v3.26→v3.28→v3.31→v3.32→v3.33→v3.34) closes every v3.33.0 honest-deferred item end-to-end. SW `sms-v3.34.0-migration-cloud-deferred-closeout-2026-05-18`. (1) **Per-tenant CompanionKeypair** — `MigrationCloudCompanionKeypair.tenant` FK + per-tenant one-active constraint (`uniq_active_keypair_per_tenant` partial-unique on `is_active=True`); service refactor — `ensure_active_keypair(tenant)` / `rotate_active_keypair(tenant)` / `decrypt_with_active_or_versioned(tenant, ...)`; views resolve `request.tenant` + `CompanionDecryptHookView` cross-checks `bundle.school == request.tenant` (403 `tenant_mismatch`); migration `0015_companion_keypair_per_tenant.py` (single leaf; pure AddField + UniqueConstraint + RunPython backfill); 12 new + 27 preserved Django tests; SECURITY_KEYS.md § 3 updated. (2) **Companion siblings** — `companion-tauri/` (Tauri 2.x + Rust + sodiumoxide; 16 files; capability allowlist denies fs/shell/process) and `companion-docker/` (FastAPI + PyNaCl in python:3.12-slim; 15 files; non-root + cap_drop ALL + no-new-privileges) as honest scaffolds at workspace root (legal-isolation parity with companion-extension/); identical sealed-box X25519+XSalsa20-Poly1305 wire format across all 3 siblings; `docs/COMPANION_SIBLINGS.md` decision matrix. (3) **Webhook verifier SDKs as proper packages** — `packages/runmycampus-webhook-verifier-py/` (PEP 621 hatchling, PyPI name `runmycampus-webhook-verifier`, stdlib-only) + `packages/runmycampus-webhook-verifier-js/` (npm name `@runmycampus/webhook-verifier`, dual ESM+CJS via tsup, WebCrypto + node:crypto, zero runtime deps); SHARED `test/fixtures/canonical-cases.json` (16 cases) asserted byte-for-byte + SHA-256 equal across both languages; 40 Python + 37 vitest pass; tag-only release workflows with PyPI OIDC + npm provenance; standalone v3.33.0 vendored copies preserved through v4.0.0 marked DEPRECATED. (4) **Per-vendor `legacy_hash_created_at` + FACTS/Skyward counsel docket** — `store_legacy_hash(..., legacy_hash_created_at_source=...)` extended with ISO-8601 string parse + future-clamp + structured-log flags; companion-extension extractors `blackbaud.ts` / `veracross.ts` / `alma.ts` emit vendor timestamp via documented field chain; `VENDOR_COVERAGE.md` 6-row matrix with explicit `Field used` + `Strictness` columns (PowerSchool YES strict; Alma promoted YES strict; BB/Veracross PARTIAL; FACTS/Skyward NO write-blocked); `docs/FACTS_SKYWARD_WRITE_PATH_COUNSEL_REVIEW.md` open docket framing CFAA / DMCA § 1201 / state computer-trespass / *Power Ventures* / *Sony Betamax* questions for external counsel — FACTS/Skyward write paths remain literal `// honest-stub:` (no feature flag workaround); 8 Django + 6 vitest. (5) **MAA v2.0 promotion plumbing + upstream watch** — `resolve_active_version_for_tenant(tenant)` + preview-only `resolve_preview_version_for_tenant(tenant)` (opt-in tenants see v2.0 with `is_preview=True`; captured signature is STILL v1.0); `MASignView` constant-time SHA-256 verifies `submitted_signature_text` matches active version; 3 new 400 codes (`draft_version`, `draft_signature_attempt`, `signature_text_mismatch`) — 3 independent gates guarantee draft body can NEVER be captured; `docs/MAA_V2_PROMOTION_CHECKLIST.md` flip + rollback procedure; `scripts/check_django_cryptography_compat.py` (stdlib-only PyPI poller) + Celery beat `upstream-watch-django-cryptography` Mondays 05:00 UTC + `docs/UPSTREAM_WATCH.md` protocol — watches always exit 0, NEVER auto-upgrade; SECURITY_KEYS.md § 7 Internal Fernet Shim migration plan; 19 tests. **Cross-agent live shell smoke 5/5 OK**; `AUTHENTICATION_BACKENDS[0]=LegacyHashUpgradeBackend` invariant preserved; `AGREEMENT_VERSIONS=={'v1.0','v2.0'}` + `MIGRATION_CLOUD_MAA_DEFAULT_VERSION=='v1.0'` (unchanged — promotion NOT performed; one-config-flip ready); migration leaf single (`0015_companion_keypair_per_tenant`); `makemigrations --dry-run --check` → "No changes detected"; all zero-tolerance gates 0 (`scan_drf_schema_coverage`, `scan_money_float`, `scan_migration_model_imports`, `scan_tenant_isolation_marker_quality`, `scan_pii_logging_smell`, `scan_print_statements`, `scan_bare_except`, `scan_subprocess_shell_true`). Supersedes [[project-migration-cloud-deferred-closeout-v3-33-2026-05-18]] for the deferred-item closeout role. **Honest deferred v3.35+**: counsel signoff PDF (`docs/legal/maa_v2_signoff.pdf`) + actual v2.0 flip; BB `user-modified-time` → strict password-change timestamp upgrade if vendor exposes; FACTS/Skyward write-path unblock pending counsel signoff in docket; operator first-pull `cargo check` + `docker build --check` + `npx tsc --noEmit` toolchain verification for companion siblings; webhook verifier SDK 0.1.0 → 1.0.0 stabilization after 90 days field-tested.
- **Migration Cloud platform deferred-item closeout (v3.33.0, 2026-05-18):** [`docs/CSS_RETIREMENT_DOCKET.md`](docs/CSS_RETIREMENT_DOCKET.md#2026-05-18--v3330-migration-cloud-platform-deferred-item-closeout-5-agent-parallel-fan-out-non-css-wave) — 5-agent parallel fan-out closes every v3.32.0 deferred item end-to-end. (1) **Companion deepening:** `CompanionUploadReceipt.key_version` persisted; `MigrationCloudCompanionKeypair.private_key_encrypted` promoted to `EncryptedBinaryField` (crypto-pending marker removed); `decrypt_with_active_or_versioned` returns typed `DecryptResult`; auto-fingerprint-verify on EVERY upload + `last_verified_fingerprint_at`; FACTS + Skyward safe-read DOM extractors (writes remain `// honest-stub:` + console.warn); PowerSchool DataDirector canned-report path; migration `0011_companion_keypair_encrypted_and_receipt_keyversion.py`; 11 Django + 16 vitest. (2) **REST API globalization:** DRF `DEFAULT_THROTTLE_CLASSES` wired scope-aware (activates ONLY on `/api/v1/migration/...`); `X-RateLimit-Soft-Warn: 1` header at >80%; `TokenRotationChainView` paginated + cycle-defended; standalone webhook verifier SDK (Python stdlib-only + JS browser/Node) + 3-example WEBHOOK_VERIFICATION.md; `MIGRATION_CLOUD_SSE_TRANSPORT` setting (`wsgi-fallback`|`asgi-daphne`) + `docs/SSE_DAPHNE_DEPLOYMENT.md` runbook; 35 tests (30 SimpleTestCase green); v3.32 19/19 regression-clean. (3) **Schoolops analytics + i18n:** 5-locale email templates `templates/schoolops/email/locale/{en,fr,es,pt,ar}/low_meal_balance.{txt,html}` (10 files; Arabic `dir="rtl"`); `sms_templates.py::SMS_LOW_BALANCE_BY_LOCALE` (normal + very-low forms, hard 160-char cap, privacy gate omits numeric when balance <$1); `/super/schoolops/meal-plan-analytics/` staff-only dashboard (4 panels: 30-day rolling, top-10, cooldown effectiveness, per-locale breakdown); `MigrationCloudWebhookSubscription.event_classes` JSONField (default `["migration.*"]`) + dispatcher honor + `schoolops.meal_plan.low_balance_triggered` publication with PII-free payload; migration `0012_webhook_event_classes.py`; 20 tests. (4) **Hash + crypto rotation:** MultiFernet rotation via `_resolve_fernet_key()` honoring `settings.DJANGO_CRYPTOGRAPHY_KEYS` newest-first list; new `key_rotation.py::{rotate_all_encrypted_columns, verify_no_orphan_ciphertexts}` (per-row `transaction.atomic()` + raw-SQL UPDATE bypass + `hmac.compare_digest` + tenant-isolation-allow + `KEY_ROTATION_LOG = "logs/key_rotation_{utc_iso}.jsonl"` NEVER logs key material); `legacy_hash_created_at_source` kwarg on `store_legacy_hash` + `VENDOR_COVERAGE.md` (PowerSchool YES, BB/Veracross/FACTS/Skyward NO, Alma PARTIAL); new beat `accounts-key-rotation-monthly` `crontab(hour=4, minute=0, day_of_month="1")` first-of-month orphan-verifier (read-only, emails on orphans, NEVER auto-rotates); `python manage.py rotate_encryption_keys` CLI; `docs/SECURITY_KEYS.md` "Automated Rotation Tooling" section; 16 tests (incl. assertNotIn leak guards); AUTHENTICATION_BACKENDS[0] invariant verified. (5) **MAA v2.0 + compliance:** `MAA_TEXT_V2_0` constant with `[DRAFT v2.0 — PENDING COUNSEL REVIEW]` header + 5 verbatim phrases ("scope of access", "data minimization", "no retention beyond migration", "right to withdraw at any time", "data subject rights"); default stays v1.0; `MAA_TEXT_DRAFT_VERSIONS = {"v2.0"}`; `MigrationAuthorizationAgreement.signature_text_sha256` CharField(64) auto-computed; migration `0013_maa_signature_sha256.py` + RunPython backfill; `MASignView` refuses draft versions (400); `docs/DPA_TEMPLATE.md` GDPR Art. 28 + NY Ed Law § 2-d; `docs/DSAR_RUNBOOK.md` 30-day SLA + redaction + attestation; SECURITY_KEYS.md "Cross-System Trust Anchors" section (6 anchors → storage/rotator/blast-radius/recovery + cascade map); **new zero-tolerance scanner `scan_pii_logging_smell.py`** baseline 0 day 1; 30 tests (27/27 SimpleTestCase green). **Cross-agent smoke 5/5 OK**; `AUTHENTICATION_BACKENDS[0]=LegacyHashUpgradeBackend` invariant preserved; 5 v3.33-relevant CELERY_BEAT_SCHEDULE entries present (`accounts-key-rotation-monthly` new); migration leaves cleanly resolved via existing `0014_merge_0011_0013.py`. All zero-tolerance gates clean (`scan_drf_schema_coverage 0`, `scan_money_float 0`, `scan_migration_model_imports 0`, `scan_tenant_isolation_marker_quality 0`, `scan_pii_logging_smell 0` new).
- **Migration Cloud platform hardening (v3.32.0, 2026-05-18):** [`docs/CSS_RETIREMENT_DOCKET.md`](docs/CSS_RETIREMENT_DOCKET.md#2026-05-18--v3320-migration-cloud-platform-hardening-5-agent-parallel-fan-out-non-css-wave) — 5-agent parallel fan-out closes every v3.31.0 deferred item end-to-end. (1) **Companion server-pubkey + popup + key-rotation:** `MigrationCloudCompanionKeypair` model (one-active-row partial-unique constraint) + `services/companion_keypair.py` (ensure/rotate/decrypt_with_active_or_versioned, best-effort zeroize, PyNaCl-unavailable typed exception, Fernet wrap via Agent 5's shim); new views `companion_server_pubkey_view` (GET anonymous, returns public_key_b64 + key_version + fingerprint, NEVER private bytes) + `CompanionKeypairRotateView` (staff POST); `CompanionDecryptHookView` refactored — operator no longer pastes inline; companion-side `server-pubkey.ts` (chrome.storage.session cache, force-refresh) + `key-rotation.ts` (verifyServerKeyFingerprint with constant-time XOR); popup wires MAA-signed badge + sign button; `pynacl>=1.5,<2.0` in requirements; 15 Django tests + 10 vitest. (2) **Vendor domain expansion:** PowerSchool gains attendance + grades + enrollments via per-student HTML detail; Blackbaud gains enrollment + attendance + grades; Alma gains courses + sections (both union into canonical `sections`); shared `withConcurrency` worker pool (cap 6); non-fatal-per-student isolation; 14 new vitest. (3) **REST API ops UX + hardening:** Celery beat entry `migration-cloud-webhook-deliver-due` (30s); token rotation flow with `rotated_to` FK + `grace_until` + 7-day grace; `apps/migration_cloud/api/rate_limiting.py` (tenant sliding-window quotas: 1000 webhook/hr soft-warn 800 hard-reject 429; token quotas per-scope); webhook delivery `deferred_until`/`deferred_reason`; operator UI `views_token_admin.py` + `views_webhook_admin.py` + 6 templates at `/super/migration/{tokens,webhooks}/`; migrations 0007 + 0009 merge; 29 tests. (4) **Schoolops tail:** 3 admin pages + reverse-relation inlines on Route/HostelRoom/CanteenMeal; `signals.py` False→True transition + `tasks.py::notify_low_meal_plan_balance` (StudentGuardian receives_email-honored, SMS hook auto-discovered) + 7-day cooldown + daily sweep task; migration 0013 `last_low_balance_notification_sent_at`+`low_balance_notification_count`; `replay_dfv_assignments.py` walks DFV→first-class and DELETES DFV on success; shared `_assignment_promotion_helpers.py`; 30 tests. (5) **Hash sunset + runbook + intake + secret wrap:** `accounts-sunset-stale-legacy-hashes` switched to `crontab(hour=3, minute=0, day_of_week="mon")` Mondays 03:00 UTC; `docs/SECURITY_KEYS.md` runbook (~395 lines: 5 key types + MultiFernet rotation + incident response + audit log); `apps/migration_cloud/services/legacy_hash_intake.py` canonical helper; `EncryptedBinaryField` in `apps/accounts/legacy_hashes/encryption.py`; `MigrationCloudWebhookSubscription.secret_ciphertext` wrapped (removed `# crypto-pending:` marker); migration 0008 AlterField+RunPython re-encrypt; 25 tests; v3.28 regression-clean. **Cross-agent smoke 5/5 OK**; `AUTHENTICATION_BACKENDS[0]=LegacyHashUpgradeBackend` invariant preserved; all 3 new CELERY_BEAT_SCHEDULE entries present; migration graph cleanly merged via 0009. All zero-tolerance gates clean (`scan_drf_schema_coverage 0`, `scan_money_float 0`, `scan_migration_model_imports 0`, `scan_tenant_isolation_marker_quality 0`).
- **Migration Cloud platform productionization (v3.31.0, 2026-05-18):** [`docs/CSS_RETIREMENT_DOCKET.md`](docs/CSS_RETIREMENT_DOCKET.md#2026-05-18--v3310-migration-cloud-platform-productionization-5-agent-parallel-fan-out-non-css-wave) — 5-agent parallel fan-out closes every v3.28.0 deferred item end-to-end: (1) **Companion per-vendor extractors** under `companion-extension/src/vendors/` — REAL extraction for PowerSchool (HTML student/staff lists, Enroll_Status→enrollment_status), Blackbaud (OData paged + per-student relationships→guardians), Veracross (people.csv role-partitioned), Alma (GraphQL SchoolUsers); FACTS + Skyward HONEST STUBS (ASPX `__VIEWSTATE` / rotating CSRF); 8/8 vitest contract tests pass. (2) **Companion-upload receiver + MAA + libsodium** — `apps/migration_cloud/companion_receiver.py` (4 views: maa_text / sign / upload / decrypt) + `MigrationAuthorizationAgreement` (verbatim signature_text at sign time) + `CompanionUploadReceipt` (unique `client_idempotency_key`) + `CompanionCiphertextBlob` (FileField); migration `0006_companion_receiver_and_maa.py`; companion-side `crypto.ts` (libsodium sealed-box X25519+XSalsa20-Poly1305, `ENCRYPTION_SCHEME = "libsodium-secretbox-x25519-sealed"`); 14 tests; assertLogs proves receiver never logs ciphertext/plaintext/signature_text. (3) **REST API completion** at `apps/migration_cloud/api/` — bulk multipart artifacts (50f/100MB/500MB, sha256 dedup), SSE progress stream (30s heartbeat, 60s graceful), `MigrationCloudAPIToken` scoped tokens (`bundles:read|write`, `templates:read`, `artifacts:write`, `reconcile:run`, `tokens:manage`, `webhooks:manage`; sha256-only-persisted; `ScopedAPIPermission` enforces `ACTION_SCOPE_REQUIREMENTS` SOT), webhook FSM (`MigrationCloudWebhookSubscription`+`MigrationCloudWebhookDelivery`+`WebhookDeliveryStatus`, retry `[1m,5m,30m,2h,12h,24h]→exhausted`, canonical-JSON HMAC-SHA256), public Redoc + OpenAPI at `/api/v1/{schema,docs}/`; migration `0005_api_tokens_and_webhooks.py`; 33/33 SimpleTestCase + 22/22 alpha regression = 55/55. (4) **First-class schoolops assignment models** at `apps/schoolops/models.py` — `TransportAssignment` + `HostelAssignment` + `MealPlanBalance` (all Decimal money, `is_low` property); migration `0012_assignment_models.py`; 3 landers promoted with graceful-degradation (first-class→DFV fallback→quarantine); backfill mgmt command `promote_dyna_assignments.py` (`--tenant` required, `--apply` to write, per-row atomic, idempotent); registry 24 first-class. (5) **Hash verifier productionization** at `apps/accounts/legacy_hashes/` — `encryption.py` backend-selecting (upstream `django_cryptography` OR internal Fernet shim AES-128-CBC+HMAC-SHA256; reports `internal_fernet_shim` here since `django.utils.baseconv` removed in Django 5); migration `0033_encrypt_legacy_hash_fields.py` wraps 3 legacy fields + adds `legacy_hash_created_at` + `legacy_hash_sunset_email_sent_at`; 12-month sunset Celery FSM (`sunset_task.py` — Active→Email-eligible→Grace-expired→null+set_unusable_password); `LegacySetupView` one-time-setup-link landing page; `scripts/encrypt_existing_legacy_hashes.py` backfill; 13 tests; v3.28's 13 OK + 6 SKIP regression-clean. Cross-agent integration smoke 5/5 modules OK, `AUTHENTICATION_BACKENDS[0] = LegacyHashUpgradeBackend` invariant preserved. All zero-tolerance gates clean.
- **Migration Cloud platform completion (v3.28.0, 2026-05-18):** [`docs/CSS_RETIREMENT_DOCKET.md`](docs/CSS_RETIREMENT_DOCKET.md#2026-05-18--v3280-migration-cloud-platform-completion-5-agent-parallel-fan-out-non-css-wave) — 5-agent parallel fan-out closes the deferred v3.26 items end-to-end: (1) wizard UI surfacing the canonical-template download (`MigrationCloudCanonicalTemplatePickerView` + 20-domain picker page + intake-page partial); (2) Companion browser-extension scaffold at top-level `companion-extension/` (Manifest V3 + TypeScript + Vite, 6 vendor host patterns mirrored across manifest/popup/content/messages, mirrors `DOMAIN_CANONICAL_HEADERS` 20-domain contract); (3) public REST API alpha at `apps/migration_cloud/api/` (DRF `BundleViewSet` + `CanonicalTemplateViewSet` with `@extend_schema` on every action, `@idempotent_post`, scoped tenant isolation; mounted at `{super,portal}/migration/api/v1/`); (4) per-student assignment landers (`transport_assignments` / `hostel_assignments` / `cafeteria_assignments`) — registry now 24 total domains; (5) foreign hash verifier at `apps/accounts/legacy_hashes/` covering 6 SIS vendors (PowerSchool PBKDF2-SHA512 / Blackbaud-Veracross-Alma bcrypt / FACTS PBKDF2-SHA1+bcrypt / Skyward SHA-512+bcrypt) + `LegacyHashUpgradeBackend` lazy re-hash on first login + User-model migration `0032_legacy_password_hash`. 7/7 cross-agent integration checks pass. All zero-tolerance gates clean.
- **Four strategic moves (v3.27, 2026-05-18):** [`docs/STRATEGIC_MOVES_v3_27.md`](docs/STRATEGIC_MOVES_v3_27.md) — Marketplace as developer platform (publisher signup + ratings + AppVersion + webhook delivery log), real workflow engine (orchestration ProcessDefinitionVersion + event log + SLO + Celery + public JSON API; visual workflow versioning), PDP + ABAC + field-level RLS (`apps.policies.pdp.decide`, `PolicyRule`/`PolicyDecisionLog`, FieldCatalogEntry sensitivity_tier + DLP redaction), close-the-help-center-loop (notify-submitters signal, KB ranked search, `/status/`, tenant health dashboard, AutoTicketRule.run). 54 new tests; all green.
- **CSS retirement audit + every polish wave (v2.0 → current):** `docs/CSS_RETIREMENT_DOCKET.md`
- **FACTS / Skyward write-path counsel docket (v3.34.0, 2026-05-18):** [`docs/FACTS_SKYWARD_WRITE_PATH_COUNSEL_REVIEW.md`](docs/FACTS_SKYWARD_WRITE_PATH_COUNSEL_REVIEW.md) — open docket framing the questions external counsel MUST answer (CFAA / DMCA § 1201 / state computer-trespass / *Power Ventures* / *Sony Betamax*) before FACTS or Skyward write paths can be unblocked. Companion `facts.ts` / `skyward.ts` writes remain literal `// honest-stub:` markers in v3.34.0; no feature flag (even default-off) is permitted as a workaround. Read-path safe-DOM scraping of authenticated user's own directory printouts is unchanged. Companion `apps/accounts/legacy_hashes/VENDOR_COVERAGE.md` records both as `NO (write-blocked)` strictness.
- **Companion siblings (v3.34.0, 2026-05-18):** [`docs/COMPANION_SIBLINGS.md`](docs/COMPANION_SIBLINGS.md) — extension vs Tauri vs Docker decision matrix + security tradeoffs + operator workflow. Top-level sibling directories at the workspace root (intentionally outside `beta/school-management-system/` for legal-isolation parity with `companion-extension/`): `companion-tauri/` (Tauri 2.x + Rust + TS desktop for IT-locked-down districts and thick-client SIS) and `companion-docker/` (FastAPI inside python:3.12-slim container for in-DMZ on-prem extraction). Both are honest scaffolds in v3.34.0 — toolchain + crypto contract + IPC/HTTP surface only; per-vendor extractors deferred to v3.35+.
- **Customer-facing webhook verifier packages (v3.34.0, 2026-05-18):** [`packages/`](packages/) — proper PyPI + npm packaging of the standalone webhook verifier SDKs shipped at v3.33.0. `packages/runmycampus-webhook-verifier-py/` (PyPI name `runmycampus-webhook-verifier`, hatchling backend, stdlib-only) and `packages/runmycampus-webhook-verifier-js/` (npm name `@runmycampus/webhook-verifier`, dual ESM+CJS via tsup, TypeScript 5, WebCrypto + node:crypto fallback). Both ship zero runtime dependencies, typed-error strict mode (`VerificationError` + 4 subclasses), optional clock-skew enforcement on `X-RunMyCampus-Timestamp` (default 300s), and a SHARED `test/fixtures/canonical-cases.json` (16 cases including unicode/CJK/escaped/nested) asserted byte-for-byte and SHA-256 equal across both languages — drift between the twins would silently break customer signature verification. 40 Python tests + 37 vitest tests pass; tsc strict + tsup dual-build green. Publish workflows `.github/workflows/release-webhook-verifier-{py,js}.yml` trigger ONLY on `webhook-verifier-{py,js}-*` tags (not push-to-main), use PyPI OIDC trusted publishing and npm provenance. Vendored standalone copies at `apps/migration_cloud/api/{webhook_verifier_sdk.py,static/runmycampus_webhook_verifier.js}` preserved through v4.0.0 for air-gapped customers but marked DEPRECATED. `docs/SECURITY_KEYS.md` § 4 "Customer integration path" + `apps/migration_cloud/api/static/WEBHOOK_VERIFICATION.md` updated to point customers at the packages first.
- **Tenant config cascade pattern:** `apps/platform_runtime/runtime_defaults_first_class.py` + `apps/siteconfig/domain_ownership.py`
- **Configurability contract (7-layer decision tree):** referenced in `docs/CSS_RETIREMENT_DOCKET.md`; details in memory.
- **Service worker version:** `static/js/service-worker.js` — bump on every wave that ships new CSS/JS, so cache invalidates cleanly after deploy.
- **AI gateway access (app code):** `services/ai_helpers.py` — `invoke_with_request`, `normalize_gateway_metadata`, `record_feedback`. App code under `apps/` MUST NOT import `services.ai_gateway` directly. Allowlisted infrastructure exceptions: `apps/portal/ai_provider.py`, `apps/portal/views_ai_gateway.py`, `apps/migration_cloud/ai_bridge.py`, `apps/platform_runtime/ai_providers.py`, `apps/siteconfig/management/commands/aggregate_ai_metrics.py`. Enforced by `scripts/scan_ai_gateway_boundary.py` (CI baseline 0).
- **AI deployment posture (v3.52.14):** `services/ai_deployment_posture.py` maps `RMC_DEPLOYMENT_PROFILE` to gateway tiers (`online` + `LITELLM_*` → cloud first; `edge` → Ollama). Canonical doc: `docs/AI_DEPLOYMENT_POSTURE.md`. Verifier: `scripts/verify_render_online_ai_posture.py`.
- **Sentry SDK access:** `apps/observability/tracing.py` — `start_named_transaction`, `set_transaction_status`, `finish_transaction`, `trace_view`, `set_tags`. App code MUST NOT import `sentry_sdk` directly outside `apps/observability/`. Enforced by `scripts/scan_sentry_boundary.py` (CI baseline 0).
- **MAA v2.0 promotion procedure (v3.34.0, 2026-05-18):** [`docs/MAA_V2_PROMOTION_CHECKLIST.md`](docs/MAA_V2_PROMOTION_CHECKLIST.md) — counsel-pending v2.0 body promotion pre-conditions (signoff PDF + verbatim phrases + backwards-compat + re-sign campaign + DSAR / DPA cross-link audit), flip procedure (env var `RMC_MAA_DEFAULT_VERSION=v2.0` + remove `"v2.0"` from `MAA_TEXT_DRAFT_VERSIONS`), rollback procedure (env revert + re-add to draft set + NEVER delete historic v2.0 signature rows). Plumbing implemented in `apps/migration_cloud/services/maa_text.py::{resolve_active_version_for_tenant, resolve_preview_version_for_tenant}`; receiver enforces active-version SHA match + refuses draft-body POST with `code=draft_signature_attempt`.
- **Upstream dependency watch (v3.34.0, 2026-05-18):** [`docs/UPSTREAM_WATCH.md`](docs/UPSTREAM_WATCH.md) — protocol for the upstream-compatibility watches RunMyCampus runs. Active watch: `django-cryptography` Django-5 compat (`scripts/check_django_cryptography_compat.py` + Celery beat `upstream-watch-django-cryptography` Mondays 05:00 UTC + task `accounts.watch_django_cryptography_upstream`). Watches always exit 0 (poller, not gate); audit JSON at `var/upstream-watch/<dep>-<timestamp>.json`. NEVER auto-upgrades — operator manually verifies + PRs the requirements bump.

## Architectural CI gates (self-enforcing rules)

AST-based scanners + CI workflows enforce platform contracts on every PR. Update a baseline ONLY when intentionally removing a violation; never to silence a new one.

| Scanner | Baseline | Workflow | Rule |
|---|---|---|---|
| `scan_tenant_queryset_safety.py` | **0** (v3.22 finish-line 2026-05-17 — 276→**0** via [`scripts/apply_tenant_isolation_finish.py`](scripts/apply_tenant_isolation_finish.py) running across `apps/` with category-by-filename auto-reasoning. **Q2 2027 ceiling 0 met 12 months early.** All 11-month future deadlines now permanently on-track. Final reason strings are auto-generated from filename patterns (e.g. `view-layer-scoped-via-request-school-or-role-graph` for `views*.py`); a human-review pass over auto-annotated markers is queued for the operator. Sister gate [`scan_tenant_isolation_marker_quality.py`](scripts/scan_tenant_isolation_marker_quality.py) (baseline 0) catches lazy reasons; the auto-generated reasons pass it because they are 3+-part hyphenated. Was 276 at v3.22 Q1 close; was 474 at v3.21 Q4 close; was 618 at v3.20; was 726 at v3.18 open.) | `tenant-isolation-scan.yml` | Tenant-scoped models need `school=` / `school_id=` / `school__isnull=` kwargs on `.filter/.get/.all/.update/.delete`. Mark safe cross-tenant queries with `# tenant-isolation-allow: <reason>`. |
| `scan_ai_gateway_boundary.py` | 0 | `architectural-boundaries.yml` | App code routes AI through `services.ai_helpers`, not `services.ai_gateway` directly. |
| `verify_ai_copilot_rbac_coverage.py` | **0** (v4.02.33 introduction 2026-06-02 — zero-tolerance from day 1; AST-scans `apps/**/*.py` for `invoke_with_request` / `invoke_with_request_stream` without RBAC markers; also flags `apps/*` direct `services.ai_gateway.invoke` outside infrastructure allowlist; scans `services/*` for direct gateway invoke without `invoke_service_layer_ai` / `guard_copilot_invoke`. Excludes management commands + batch allowlist. Runtime belt-and-suspenders in `services.ai_helpers` via `guard_copilot_invoke`. Baseline `var/security-audit-baseline-ai-copilot-rbac-coverage.json`.) | `architectural-boundaries.yml::ai-copilot-rbac-coverage` | Every user-facing copilot invoke must declare RBAC coverage; central guard in `services/ai_copilot_rbac.py` is SOT. |
| `verify_ai_naming_honesty.py` | n/a (pass/fail gate, not a finding count) | `architectural-boundaries.yml::ai-naming-honesty` | AI-consolidation seals (`docs/AI_AND_SELF_HEALING_CONSOLIDATION_PLAN.md` §5). Fails CI if: a rules surface named copilot/AI (#1 manager rail feed, #8 policy lookup, #12 support copilot) gains an inference call (`invoke_with_request` / `run_ai_prompt`) or its honest label reverts to "copilot"; the tenant AI-strip gate flag `rmc_ai_layer_enabled` stops being emitted; the hub anomaly-nudge fan-out cap `_MAX_NUDGE_SCHOOLS` is removed/unbounded (>50); or the portal "legacy" AI-gateway coupling breaks (assist dock ↔ `ai_chrome_config`, `rmc-ai-stream-bridge` in the viewport engine, `portal:ai_stream` route). stdlib-only, no DB. Companion behavioural test: `apps/platform_runtime/tests/test_ai_consolidation_seals.py`. |
| `scan_sentry_boundary.py` | 0 | `architectural-boundaries.yml` | `sentry_sdk` is fenced inside `apps/observability/`. |
| `scan_print_statements.py` | 0 | `architectural-boundaries.yml` | Use `logging`, not `print()`, in `apps/` and `services/` outside management commands. |
| `scan_bare_except.py` | 0 | `architectural-boundaries.yml` | Always specify the exception type. Bare `except:` catches `KeyboardInterrupt` / `SystemExit`. |
| `scan_migration_model_imports.py` | 0 | `architectural-boundaries.yml` | Migrations use `apps.get_model("X", "Y")` for historical-state safety; do not import live models. |
| `scan_import_reference_integrity.py` | **0** (v4.03.x introduction 2026-06-15 — seals the phantom/wrong-module/absent-import loophole class this codebase repeatedly shipped: an `apps.*` import naming a module or symbol that doesn't exist, surfacing as an uncaught `ImportError` 500 or — worse — silently swallowed by a broad `except` so a counter/list pins to 0/empty forever. Stdlib AST + filesystem; no Django/runtime dep so it runs in the deps-free boundary job. Resolves all reference forms statically: absolute `from apps.A.B import x`, bare `import apps.A.B`, relative `from .x import y`, and `importlib.import_module("apps.…")`/`__import__` string literals. Symbol-level resolution AST-parses the target module; PEP-420 namespace packages (dirs w/o `__init__.py`, e.g. `apps/api/`), `from x import *` re-exports, module-level `__getattr__`, and dynamic `globals().update(...)` re-exports are treated as opaque (skip symbol check) to guarantee zero false positives. Imports inside a `try/except (ImportError\|ModuleNotFoundError\|Exception\|BaseException\|bare)` block — including `except <NAMED_TUPLE>:` where the tuple includes a guard exc (e.g. `_GATE_SOFT_FAILURES`) — are excused as deliberate optional/degrade patterns. Symbol collection over-collects (whole-tree walk) to bias toward false negatives, never positives. Introduction caught 1 real bug at first run (an unregistered duplicate `GenerateRegionalReportsCommand` in `siteconfig/.../i18n_commands.py` importing a nonexistent `ReportCompilationService`; retired — the real command lives in `reports/.../generate_regional_reports.py`); 35 other first-run hits were scanner FPs resolved by the namespace-pkg / `globals()` / named-tuple-guard refinements above. Skips migrations (own gate) + test files. Baseline `var/security-audit-baseline-import-reference-integrity.json`.) | `architectural-boundaries.yml::import-reference-integrity` | Every `apps.*` import (absolute / relative / dynamic-literal) must resolve to a real module + symbol. Guard intentional optional/deferred targets with `try/except ImportError` or a `# import-ref-allow: <reason>` marker on the import line or the line above. |
| `verify_get_model_integrity.py` | **0** (v4.03.x introduction 2026-06-15 — RUNTIME companion to `scan_import_reference_integrity.py` that closes the one reference path the static gate cannot: dynamic `get_model("app","Model")` for an **unregistered** model. A model class can be physically defined in a `models_*.py` (so a static scan "finds" it and passes) yet never be registered — no migration, not in any installed app — so `get_model` raises `LookupError` at runtime. Exactly the bug shipped in `apps/evals/import_services.py` (`get_model("evals","GradeImportJob")` / `"GradeImportRowLog"`, defined only in the migration-less `evals/models_enhanced.py`) — a static check is a guaranteed false negative there. This verifier AST-collects every literal dynamic-model-reference call shape in `apps/`+`services/` — `get_model("a","B")` / `apps.get_model(...)` / `django_apps.get_model(...)` / single dotted `get_model("a.B")`, AND `ContentType.objects.get/filter(app_label="x", model="y")` literal lookups (same registry question, same silent-`DoesNotExist` failure mode) — then asks the **live registry** `django.apps.apps.get_model(...)` whether each resolves; a `LookupError` is a finding. Needs Django → runs in `ci.yml::django-tests` after `manage.py check` (NOT the deps-free architectural-boundaries job). Excuses: `try/except (LookupError\|Exception\|BaseException\|bare\|named-tuple-guard)` and `# get-model-allow: <reason>` marker (line or line-above); non-literal args + migrations + test files skipped. At introduction: 42 literal calls checked (41 `get_model` + 1 `ContentType`), 0 unresolved (proven via negative tests that it flags the unregistered `evals.GradeImportJob`, an absent app, AND an unregistered ContentType lookup, and excuses a `LookupError`-guarded call). 11 stdlib unittests lock the AST layer (`scripts/tests/test_verify_get_model_integrity.py`). Baseline `var/security-audit-baseline-get-model-integrity.json`.) | `ci.yml::django-tests` | Every `get_model('app','Model')` literal AND `ContentType.objects.get(app_label=,model=)` literal must resolve against the live Django app registry (catches absent app, absent model, AND defined-but-unregistered). Guard with `try/except LookupError` or `# get-model-allow: <reason>`. |
| `verify_template_reference_integrity.py` | **0** (v4.04.x introduction 2026-06-25 — fourth and final RUNTIME member of the reference-integrity family (imports + get_model/ContentType + URL-names + now templates), sealing the `TemplateDoesNotExist` loophole the others cannot: a literal template path passed to `render(request,"x.html")` / `render_to_string` / `TemplateResponse` / `get_template` / `select_template` (or assigned to a class `template_name`/`template_names`) that names a template which does not exist — an uncaught `TemplateDoesNotExist` 500 the first time the view is hit, or a silently-empty render when swallowed. `audit_template_render_safety.py` walks `templates/**/*.html` for `{% include %}`/`{% extends %}` targets but NEVER sees the Python side that names the template to render. AST-collects every literal template path in `apps/`+`services/`+`config/` (positional + `template_name=`/`template=`/`template_names=` kwargs + class-body attrs; `render` gated on a literal `request` first arg so custom `.render()` methods don't false-positive; a literal must be path-like — no whitespace + an extension), then asks Django's own loader `get_template`/`select_template` whether each resolves — ground truth, so third-party templates (`admin/base_site.html`, DRF) resolve exactly as at runtime and never false-positive. `select_template`/list args use 'any member resolves' semantics. Needs Django → runs in `ci.yml::django-tests` after the url-name step. **Introduction caught 6 REAL broken references on first run** (4 live 500s: routed `/finance/notifications/` + `backend_classroom_create` page views + the wired deadline-reminder email; 2 a grade-publication email pair) — all fixed by creating the 6 missing templates faithfully (emails extend `emails/base_branded` civic grammar; pages mirror `finance/requests.html` + the backend shell), driving **862 literal targets checked → 0 unresolved**. Excuses: `try/except (TemplateDoesNotExist|Exception|BaseException|bare|named-tuple-guard)` and `# template-ref-allow: <reason>` marker (line or line-above); non-literal args + migrations + test files skipped. 18 stdlib unittests (`scripts/tests/test_verify_template_reference_integrity.py`). Baseline `var/security-audit-baseline-template-reference-integrity.json`.) | `ci.yml::django-tests` | Every literal template path passed to `render`/`render_to_string`/`TemplateResponse`/`get_template`/`select_template` (or assigned to `template_name[s]`) must resolve via Django's loader. Guard intentional dynamic/optional templates with `try/except TemplateDoesNotExist` or `# template-ref-allow: <reason>`. |
| `verify_settings_key_integrity.py` | **0** (v4.04.x introduction 2026-06-25 — fifth member of the reference-integrity family (imports + get_model/ContentType + URL-names + templates + now Django settings), sealing the settings-attribute loophole: a bare `settings.NAME` read (or `getattr(settings,"NAME")` with NO default) for a key the deployed config never defines raises `AttributeError` at runtime — a 500 on the path that first touches it, or a silently-swallowed dead branch; `manage.py check` never catches it and a static scan can't know the full settings surface (env-var driven / sub-module split / third-party defaults). Ground truth = `hasattr` against the LIVE `django.conf.settings` after `django.setup` (same approach as `verify_get_model_integrity` against the app registry), so zero FPs. AST-collects `settings.NAME` (UPPER_SNAKE, READ context only — assignment targets skipped; file must `from django.conf import settings`) + `getattr(settings,"NAME")` with no default, across `apps/`+`services/`+`config/`. Excuses: `try/except (AttributeError|ImproperlyConfigured|Exception|BaseException|bare|named-tuple-guard)`, a `getattr` default, the same-line `settings.X if hasattr(settings,"X") else …` guard, and `# settings-key-allow: <reason>`; non-literal/lowercase attrs + migrations + test files skipped. Introduction: **506 literal reads checked, 0 unresolved** — the sole first-run hit (`apps/evals/notifications.py` `settings.SITE_SETTINGS`) was a correctly hasattr-guarded inline ternary, which taught the gate that guard pattern (not a one-off marker). 12 stdlib unittests (`scripts/tests/test_verify_settings_key_integrity.py`). Baseline `var/security-audit-baseline-settings-key-integrity.json`.) | `ci.yml::django-tests` | Every literal `settings.NAME` read (and `getattr(settings,'NAME')` with no default) must resolve on the live `django.conf.settings`. Guard optional settings with a `getattr` default, `try/except AttributeError`, a same-line `hasattr(settings,'NAME')`, or `# settings-key-allow: <reason>`. |
| `verify_field_reference_integrity.py` | **0** (v4.04.x introduction 2026-06-25 — the ORM **field**-name member of the reference-integrity family (imports + get_model/ContentType + URL-names + templates + settings + now ORM field names), sealing the last "literal reference → runtime registry → `FieldError` 500" class: a literal field-name string passed to a field-resolving queryset method — `.order_by("crated")` / `.values("emial")` / `.only("naem")` — where a typo or renamed/removed field becomes an uncaught `FieldError` the first time the queryset is evaluated (model `_meta` is ground truth; AST can't prove a field exists). Conservative / false-NEGATIVE-biased so it stays a zero-baseline gate without ever reddening CI on a clean tree: checks positional string args of `order_by`/`values`/`values_list`/`only`/`defer`/`distinct` ONLY where the receiver chain head is a directly-resolvable `<Capitalised>.objects` resolving to exactly ONE registered model by class name (0/>1 name-collision → skipped); any chain with `annotate`/`alias`/`extra`/`raw`/`union`/`intersection`/`difference` skipped (pseudo-fields); each literal normalised (leading `-` stripped, `?`/`pk` skipped, only the FIRST `__` segment checked so related-traversal / transform / lookup tails never false-positive); `filter`/`exclude`/`get` kwarg-`__` form + `F()` intentionally OUT of scope v1. **Caught 7 REAL latent `FieldError` 500s on first run** (1716 field refs checked), ALL FIXED → 0: 6 in `apps/analytics/ml/grade_prediction_features.py` (`.only("seq1"/"seq2"/"exam")` — the `Evaluation` fields are `seq1_score`/`seq2_score`/`exam_score`; its `except (AttributeError,TypeError,ValueError)` did NOT catch `FieldError`, so the grade-prediction ML feature 500'd whenever it ran + read `None` always) + 1 in `apps/academics/bulk_attendance.py` (`StudentProfile.objects.only("external_id")` — no such field; the bulk-attendance CSV apply path 500'd on every call since 2026-05-26; fixed to match the CSV `student_external_id` against ANY of `student_code`/`admission_number`/`exam_candidate_number`, so the upload tolerates whichever id the school exported). Excuses: `try/except (FieldError|FieldDoesNotExist|Exception|BaseException|bare|named-tuple-guard)` + `# field-ref-allow: <reason>` (line or line-above); non-literal args + migrations + test files skipped. Baseline `var/security-audit-baseline-field-reference-integrity.json`.) | `ci.yml::django-tests` | Every literal field name passed to `.order_by`/`.values`/`.values_list`/`.only`/`.defer`/`.distinct` on a directly-resolvable `<Model>.objects` chain must resolve via the model's `_meta.get_field`. Guard with `try/except FieldError` or `# field-ref-allow: <reason>`. |
| `verify_relation_path_integrity.py` | **0** (v4.04.x introduction 2026-06-25 — the ORM **relation-path** member of the reference-integrity family, closing the gap `verify_field_reference_integrity.py` DELIBERATELY leaves open: that gate's `_FIELD_METHODS` excludes `select_related`/`prefetch_related` (relation, not scalar-field, resolution), so a literal relation path naming no field/relation on the head model — `Evaluation.objects.select_related("subject")` — is an uncaught `FieldError` ("Cannot resolve keyword 'subject' into field") the first time the queryset evaluates, missed by every other gate (the import gate sees `Evaluation` resolves; the field gate skips `select_related`). Same conservative / false-NEGATIVE-biased design as the field gate — checks positional string args of `select_related`/`prefetch_related` ONLY where the receiver chain head is a directly-resolvable `<Capitalised>.objects` resolving to exactly ONE registered model (0/>1 collision → skipped); any chain with `annotate`/`alias`/`extra`/`raw`/`union`/`intersection`/`difference` skipped; only the FIRST `__` segment checked (deep traversals `a__b__c` never false-positive); `Prefetch(...)` objects / `F()` / variables are not string literals so never seen; v1 checks NAME existence only (`get_field` does not raise `FieldDoesNotExist`) — proving the segment is a relation vs scalar, and chasing the tail across models, is deferred (GenericForeignKey / private-field edges keep that FP-prone); only `FieldDoesNotExist` is a finding (other lookup errors swallowed). **Caught 1 REAL latent `FieldError` 500 on first run** (1123 relation refs checked): `apps/reports/management/commands/generate_regional_reports.py` — `Evaluation.objects.filter(...).select_related("component", "subject")` (the canonical `generate_regional_reports` command; the duplicate at `siteconfig/.../i18n_commands.py` was already retired by the import gate). `evals.Evaluation` has NEITHER `component` NOR `subject` (its relation is `subject_assignment` → `SubjectAssignment.subject`), so the command crashed with `FieldError` the moment its queryset evaluated. Fixed to `.select_related("subject_assignment__subject")` + `eval.subject_assignment.subject` / `eval.final_score` reads (was `eval.subject`/`eval.score`, neither a field), driving **1122 refs checked → 0 unresolved**. Excuses: `try/except (FieldError|FieldDoesNotExist|Exception|BaseException|bare|named-tuple-guard)` + `# relation-path-allow: <reason>` (line or line-above); non-literal args + migrations + test files skipped. 19 stdlib unittests (`scripts/tests/test_verify_relation_path_integrity.py`). Baseline `var/security-audit-baseline-relation-path-integrity.json`.) | `ci.yml::django-tests` | Every literal relation path passed to `.select_related`/`.prefetch_related` on a directly-resolvable `<Model>.objects` chain must have its first segment resolve via the model's `_meta.get_field`. Guard with `try/except FieldError` or `# relation-path-allow: <reason>`. |
| `verify_url_name_integrity.py` | **0** (v4.04.x introduction 2026-06-24 — third RUNTIME reference-integrity gate, sealing the `NoReverseMatch` loophole the static + get_model gates cannot: a literal `reverse("name")` / `reverse_lazy("name")` or `{% url "name" %}` whose name resolves to no registered URL pattern, surfacing as an uncaught `NoReverseMatch` 500 at request time — or, when caught by a broad `except`, a silently dead link/redirect. AST-collects every literal-first-arg `reverse(...)`/`reverse_lazy(...)` in `apps/`+`services/` plus `{% url "x" %}`/`{% url 'x' %}` in `templates/**/*.html`, then asks the **live URL resolver** whether each resolves. **The FP trap this gate was built around:** RunMyCampus is HOST-SPLIT across six urlconfs (`config.urls`/`public_urls`/`tenant_urls`/`api_urls`/`docs_urls`/`manager_urls` — the manager host is routed by middleware, NOT a settings constant, so it is discovered by globbing `config/*urls*.py`, not by reading settings `*_URLCONF`), and `get_resolver()` only sees `ROOT_URLCONF`. A naive verifier that checks only the default resolver falsely flags every name registered solely in a host-split urlconf or under `admin:`. The fix unions registered names across ALL `config/*urls*.py` modules + settings `*_URLCONF` + `django.contrib.admin.autodiscover()`, then for any miss falls back to the authoritative `reverse()` message (`"is not a valid view function or pattern name"` = genuinely unknown vs `"...pattern(s) tried"` = valid-but-needs-args), and skips `"..."` placeholder names. At introduction this turned a first-cut **128**-finding baseline into **0** — all 128 were host-split/admin false positives (each `manager_*`/`sales:*`/`tenant_*`/`marketing_*`/`admin:*`/`demo_*` name verified present in its host urlconf), against **3784** literal references checked. Needs Django → runs in `ci.yml::django-tests` after the get_model step (NOT the deps-free boundary job). Excuses: `try/except (NoReverseMatch\|Exception\|BaseException\|bare\|named-tuple-guard)` and `# url-name-allow: <reason>` marker (line or line-above). `redirect()` is intentionally NOT covered (its first arg is ambiguously a URL / name / model); non-literal args + migrations + test files skipped. 9 stdlib unittests lock the AST/regex/guard layer (`scripts/tests/test_verify_url_name_integrity.py`). Baseline `var/security-audit-baseline-url-name-integrity.json`.) | `ci.yml::django-tests` | Every literal `reverse('name')` / `reverse_lazy('name')` AND `{% url "name" %}` must resolve against the live URL resolver (unioned across all host-split urlconfs + admin). Guard intentional dynamic/optional targets with `try/except NoReverseMatch` or `# url-name-allow: <reason>`. |
| `verify_offline_capability_implementation.py` | **0** (introduction 2026-06-25 — seals the offline-first "label without code" class. The sibling `verify_offline_manifest_taxonomy.py` proves a manifest *declares* the `OFFLINE_QUEUED_WRITE` capability flags with the right shape but NOT that any are backed by code, so a surface could ship `enable_offline_homework_sync=true` with zero producer/applier = theater. This gate asserts every declared capability has a real **client producer** AND **server applier** on at least one of the two offline rails — SODP/OfflineAction (`platform_runtime/offline_queue.py::_apply_payload` + `rmcOfflineEnqueue`) or WAL (`wal_stream/writers.py::_REGISTRY` ∩ `consumers._ALLOWED_DOMAINS` + `rmcWAL.append`). Stdlib-only AST+text scan so it runs in the deps-free boundary job (never imports Django/app). Also fails on (a) a NEW `enable_offline_*` taxonomy flag with no `_CAPABILITY_SPEC` row and (b) a stale spec row for a dropped flag — keeps the two rails from drifting. **Latent** capabilities (producer+applier real but no UI surface yet, e.g. homework's missing `data-rmc-offline-form="homework_submission"`) are reported NON-fatally, not findings. Introduction: 4 capabilities checked, 0 findings, 1 latent (homework). Canonical two-rail map: `docs/OFFLINE_TWO_RAIL_ARCHITECTURE.md`. Tests `scripts/tests/test_verify_offline_capability_implementation.py` (7). Baseline `var/security-audit-baseline-offline-capability-implementation.json`.) | `architectural-boundaries.yml::offline-capability-implementation` | Every `enable_offline_*` capability declared in the offline manifest taxonomy must have a real client producer + server applier on the SODP or WAL rail. Add new capabilities to `_CAPABILITY_SPEC` with their rail mapping; ship a producer + applier before declaring the flag. |
| `scan_drf_schema_coverage.py` | 0 | `architectural-boundaries.yml` | DRF view classes in `apps/api/` carry `@extend_schema` (or `@extend_schema_view`). Mark intentional exclusions with `# drf-spectacular-allow: <reason>`. |
| `scan_role_strings.py` | 385 (reconcile 2026-06-26 — `scan_role_strings.py --compare` was red on origin/main; +32 net legitimate role-keyed config sites committed by PEERS across 6 files (`siteconfig/cockpit_live_banner_program.py` audience banners, `siteconfig/forms_cockpit.py` ×3 role-keyed cockpit forms, `siteconfig/cockpit_context.py` parent/student, `siteconfig/tenant_experience_presets.py` `ROLE_PRESET_BUCKETS`/`ADMIN_OPERATOR_ROLES`/role-normalizer, `dashboard/first_run_zero_state.py`, `academics/views_discipline_api.py`) minus the `portal_sidebar_items.py` −2 removal — all role-keyed CONFIG (preset buckets, audience frozensets, role normalizers), NOT a security regression; re-baselined to match deployed code per the established drift-reconcile pattern. JSON `finding_count` 353→385; one in-flight peer file (`views_discipline_api.py`) verified identical to committed HEAD so the baseline matches origin/main, and the gate fails only on NEW literals (`return 1 if new else 0`) so absorbed untracked-peer additions stay green. Was 353 (v4.02.90 reconcile 2026-06-14 — `scan_role_strings.py --compare` CI gate was red on HEAD; +4 net (56 new / 52 removed) from role-permission list/set churn across the academics/api/accounts/sidebar waves — all legitimate role-permission sites (allowed-role lists, preview-role sets, session role values) incl. extended roles `IT_ADMIN`/`BURSAR`/`HOD`/`LEADERSHIP` that are NOT in `User.Role` TextChoices; re-baselined to match deployed code per the drift-reconcile pattern (not a security regression). Was 349 (v4.02.x reconcile 2026-06-06 — `check_documented_baselines.py` caught doc-vs-JSON drift after the tenant-invite/signup-verification wave bumped the JSON baseline +2 for legitimate new role sites; doc reconciled to match. Was 347 (v4.01.40 reconcile 2026-06-02 — `check_documented_baselines.py` caught doc-vs-JSON drift; JSON -41 from workflow-10x role-registry cleanup waves; was 388 (v4.00.62 audit reconcile 2026-05-29 — `check_documented_baselines.py` caught doc-vs-JSON drift; JSON +4 from interop waves; was 384 (v3.99.14 audit reconcile 2026-05-28 — `check_documented_baselines.py` caught doc-vs-JSON drift; doc was 372 from v3.94.0 but JSON baseline accumulated +12 sites from natural feature growth across v3.95.x→v3.99.x waves R/S/T/U/1553; was 372 (v3.94.0 aggressive-validation reconcile 2026-05-26 — `check_documented_baselines.py` caught doc-vs-JSON drift; doc was 292 from v3.62.2 era but JSON baseline accumulated +80 sites from natural feature growth across v3.63→v3.93.x template-marketplace/wizard/local-first waves. v3.93.x work added zero role-string sites in `setup_studio`/`brand_experience`/`billing`/`compliance`/`customersuccess`/`migration_cloud`/`platform_runtime`. Drift-detector only — new sites should still carry `# role-string-allow: <reason>` markers; was 292 at v3.62.2 reconcile; was 285 at v3.59.8 reconcile; was 268 at v3.23.12 reconcile; was 271 at v3.7.2 QA wave-2; was 272 at v2.36; was 367 at v2.25) | `architectural-boundaries.yml` | Hardcoded role names (`ADMIN`/`TEACHER`/`PARENT`/`STUDENT`/`PROPRIETOR`) must reference `apps.platform_runtime.role_registry` or `User.Role` TextChoices. SOT modules: `role_registry.py`, `accounts/models.py`, `accounts/permissions.py`. Mark other allowed sites with `# role-string-allow: <reason>`. **`--compare` is line-insensitive (keyed on `(path, role)` multiset since 2026-06-15)** — cosmetic line-drift no longer trips the gate; a brand-new role literal or one more occurrence in a file still does. |
| `scan_access_resolver_fragmentation.py` | **0** (FULL burndown 2026-07-02, same day as introduction — all 23 consumer sites migrated to the canonical facade `apps/accounts/effective_access.py` (student-data/grades-edit/invoice ×10, school-permission ×6, module-gate ×1, role-checks ×6 incl. api_v1's local any-role helper renamed to delegate); ZERO-TOLERANCE from here — every new object/module/role access check goes through the facade, where PDP/ReBAC/decision-logging wire in once. Was 13 after the first slice; was **23** at introduction 2026-07-02 — 10 consumer sites migrated to the new canonical facade `apps/accounts/effective_access.py` (student-data x7, grades-edit x1 via student360/academics/api_v1; invoice-view x3 via finance) same day the gate landed; facade added to scanner SOT exemptions. Was **23** at introduction 2026-07-02 — one-way RATCHET on authorization sprawl: AST-counts consumer call-sites of the fragmented role/hierarchy/object-gate helpers (`has_role`/`has_role_hierarchy`/`api_user_has_any_role`/`_user_has_any_role`/`can_access_module`/`can_view_student_data`/`can_edit_student_grades`/`can_view_invoice`/`can_edit_invoice`/`has_school_permission`) OUTSIDE their SOT modules (`accounts/permissions.py`+`decorators.py`, `schools/tenant_access.py`, `api/permissions.py` exempt — helpers may live there and call each other; tests+migrations skipped). Does NOT consolidate anything — freezes the sprawl while the PDP/`has_feature_permission` consolidation waves burn it toward 0. Baseline `var/security-audit-baseline-access-resolver-fragmentation.json`.) | `architectural-boundaries.yml::access-resolver-fragmentation` | New authorization checks route through `User.has_feature_permission` / the PDP (`apps/policies/pdp.py::decide`) — never one more call to a fragmented helper. **`--compare` is line-insensitive (`(path, name)` multiset)**; mark deliberate sites with `# access-resolver-allow: <reason>`. |
| `scan_assert_in_production.py` | 0 | `architectural-boundaries.yml` | `assert` is stripped under `python -O`; load-bearing checks must raise explicit exceptions. Mark type-narrowing-only allowed asserts with `# assert-allow: <reason>` on the same line. |
| `scan_magic_numbers.py` | **979** (v4.05.x reconcile 2026-06-27 — `check_documented_baselines.py` was red on origin/main: doc said 912 but the JSON baseline had reached 979 across codex's committed waves (width-to-width sweeps `91dd4a5cd`/`c2999ee04` + the config-SOT "Wave A" `aa43a98d6`) without the doc being updated. Benign config constants (SLO latency `threshold_ms`, page-horizon/Ask-AI-dock geometry); reconciled doc→JSON to match deployed code per the standard pattern. The billing/subscription wave's only new literal (a model `max_length`) carries a `# magic-number-allow` marker, so it does NOT change the count. Was **912** — v4.04.51 reconcile 2026-06-22 — IM-8 follow-up retired the pre-existing `services/messaging_ai.py` `body_excerpt[:240]` query-preview literal by reusing the new `_QUERY_PREVIEW_CHARS` constant (the same constant added for the IM-8 thread-summary site), −1 → 912. Prior: 913 at v4.02.90 reconcile 2026-06-14 — `scan_magic_numbers.py --compare` CI gate was red on HEAD again; net +29 (40 new / 11 removed) from the academics lesson/timetable-solver + analytics ML + owner-onboarding waves (query caps, solver iteration bounds, cache TTLs, ML params — all benign config constants), re-baselined to match deployed code; current scan == 913; was **884** (v4.02.90 reconcile 2026-06-07 — `scan_magic_numbers.py --compare` CI gate was red on HEAD after the signup-verify/KB/cockpit-200x waves; net +10 (77 new SVG preview coords + cache TTLs, 67 removed), re-baselined to match deployed code; current scan == 884; was **874** (v4.02.90 reconcile 2026-06-07 — `check_documented_baselines.py` caught doc-vs-JSON drift; JSON +41 from natural feature growth across the world-globe + AI-gateway parameter-fallback waves; was **833** (v4.01.40 reconcile 2026-06-02 — `check_documented_baselines.py` caught doc-vs-JSON drift; JSON -60 from workflow-10x constant-extraction cleanup; was **893** (v4.00.62 audit reconcile 2026-05-29 — `check_documented_baselines.py` caught doc-vs-JSON drift; JSON +167 from natural feature growth; was **726** (v3.59.8 reconcile 2026-05-22 — `check_documented_baselines.py` caught doc-vs-JSON drift; doc was 560 from v3.23.2 but JSON baseline accumulated +166 sites from natural feature growth across waves v3.24→v3.59.x; was 560 at v3.23.2 line-shift re-baseline; was 559 at v3.7.2 QA wave-2, was 496 at v3.7.2-w1, 493 at v2.61+O4, was 485 at v2.58+N1, was 482 at v2.47 — drift caught by `check_documented_baselines.py` on each wave; natural growth from new constants in code) | `architectural-boundaries.yml` | Integer literals `abs(v) >= 100` outside `config/` / `*/constants.py` should be named constants. Drift-detection only. Mark allowed sites with `# magic-number-allow: <reason>`. **`--compare` is line-insensitive (keyed on `(path, value)` multiset since 2026-06-15)** — cosmetic line-drift no longer trips the gate; a brand-new value or one more occurrence of a value in a file still does. |
| `scan_subprocess_shell_true.py` | 0 | `architectural-boundaries.yml` | `subprocess.*(shell=True)` and `os.system(...)` are forbidden (command injection). Mark allowed sites with `# shell-true-allow: <reason>`. |
| `scan_money_float.py` | **0** (v3.18 introduction 2026-05-17 at baseline 26; same-day full burndown to 0 via per-site `# money-float-allow: <reason>` markers across 6 files — categorical reasons used: `display-precision-acceptable` (JSON response sites), `ratio-not-money` (percent / confidence score), `scalar-coerce-input` (nuance-engine inputs), `gateway-input-format` (stripe processor wrapper). Companion helper [`apps/finance/json_decimal.py`](apps/finance/json_decimal.py) ships `amount_str()` + `DecimalJSONEncoder` for new code paths that need precision-preserving serialization.) | `architectural-boundaries.yml::money-float` | `float(<money_expr>)` calls on amount/balance/fee/tax/etc. paths in `apps/finance/`, `apps/billing/`, `apps/marketplace/`, `payment/` — Decimal→float silently corrupts ledger sums. **Zero-tolerance gate** post-burndown. Mark intentional sites with `# money-float-allow: <reason>` on the same line. |
| `scan_locale_display.py` | **0** (introduction 2026-06-25 — zero-tolerance from day 1; the regression-seal for the by-hand tenant currency-resolution fix (`b084a85d4` — `region_format.format_currency` now resolves the request's pinned tenant's currency) + the operator-revenue `$`-burndown (`a7f815b8f`). **Caught 4 REAL bugs on first run**: a hardcoded `₦` in `siteconfig/admin_dashboard.py::AdminDashboardWidget.get_formatted_value` (Naira on every operator currency widget regardless of platform default), a THIRD missed operator `$` in `cockpit_platform_pulse_service.py` MRR-delta text, and 2 marketplace `${{ }}` sites in `publisher_dashboard.html` (publisher MRR/payout — whose `mrr_currency` was itself hardcoded `"USD"`). All 4 burned to 0 by routing through `platform_currency_symbol()` / `PLATFORM_DEFAULT_CURRENCY` + a context `currency_symbol`. A same-day follow-up EXTENDED the detectors to `%`-format + `.format()` shapes and caught a **5th** real bug the f-string-only pass missed — `siteconfig/cockpit_panels_realdata_service.py::_fc_fmt_mrr` (`"$%.1fk" % …` / `"$%.0f" % value` operator MRR forecast), also fixed to `platform_currency_symbol()`. Stdlib AST (Python) + regex (Django templates); no Django dep → runs in the deps-free boundary job. Flags four zero-FP shapes: a Python f-string literal segment ENDING in a currency glyph immediately before an interpolation (`f"${x}"` / `f"₦{x:.0f}"`); a glyph glued to a printf conversion that is the LEFT operand of `%` (`"$%.2f" % v`); a glyph glued to a `str.format` field on the formatted literal (`"${}".format(v)`); and a glyph glued to a Django variable in any template — `.html` OR an email/SMS `.txt` body, scoped to a `templates/` path so `requirements.txt`/fixtures never match (`${{ x }}`). The `%` / `.format` detectors are AST-anchored to the real `%` op / real `.format()` receiver, so a shell-style `"${HOME}"`, a regex, or a docstring never false-positives. Deliberately does NOT flag `"$" + str(x)` concat (too ambiguous); JS template literals `${x}` (single brace) never match the `{{`-requiring template pattern. Symbol set is single-char glyphs (`$€£₦¥₹₵₱₩฿₪₫`). Excuses: `# locale-display-allow: <reason>` (Python, same line or line-above) / `<!-- locale-display-allow: <reason> -->` (template, same line or above); tests + migrations skipped. 29 stdlib unittests (`scripts/tests/test_scan_locale_display.py`) — incl. a temp-tree file-discovery test proving `.txt` email templates ARE scanned and non-template `.txt` is not. Baseline `var/security-audit-baseline-locale-display.json`.) | `architectural-boundaries.yml::locale-display` | Tenant-facing money must render through `\|format_currency` / `format_currency_tenant` (or, for the platform's OWN revenue, `platform_currency_symbol()`) — never a hardcoded currency glyph glued to a value. A school billing in XAF/NGN must not see the platform-default `$` on its own invoices/statements/emails. Mark intentional symbol-only sites with `# locale-display-allow: <reason>` (Python) or `<!-- locale-display-allow: <reason> -->` (template). |
| `scan_tenant_isolation_marker_quality.py` | **0** (v3.22 introduction 2026-05-17 — audits the **reason string** on every `# tenant-isolation-allow:` marker across 802 sites in 222 files. Flags: empty reasons, single-word placeholders (`TODO` / `FIXME` / `WIP` / `later` / `unknown`), lazy phrases (`fix later` / `see above` / `temporary`), reasons under 8 characters, single-token generics. All 802 existing markers passed at baseline write — zero-tolerance from day 1. Uses `tokenize.COMMENT` so string-literal occurrences (e.g. in feature-registry notes) are not misclassified as markers.) | `architectural-boundaries.yml::tenant-isolation-marker-quality` | Sister gate to `scan_tenant_queryset_safety.py` (count) and `verify_tenant_scoping_burndown.py` (schedule). This one catches the "lazy marker" anti-pattern the forward-progress verifier explicitly cannot detect — a developer adding the marker with a non-reason like "TODO" instead of fixing the query. |
| `scan_locale_coverage.py` | n/a (per-locale drift detection, not a global integer; baseline records each locale's translated count and only regressions trip CI) | `architectural-boundaries.yml::locale-coverage` | Walks every `.po` under `locale/<code>/LC_MESSAGES/django.po` for the 17 expected locales (per memory v3.13) and counts msgid → non-empty msgstr ratio. Stub locales (`yo`/`ha`/`sw`/`pid`/`fr`) legitimately ship with 0 translated and fall back to English; the gate trips only when a locale that USED to have translations regresses. Complements `verify_i18n_catalog_fresh.py` (which asserts msgid-set freshness). |
| `verify_service_worker_version.py` | n/a (shape + monotonicity gate, not a finding count) | `architectural-boundaries.yml::service-worker-version` | Reads `static/js/service-worker.js`, asserts `CACHE_VERSION` matches `sms-vMAJOR.MINOR.PATCH[-slug]-YYYY-MM-DD`, and (with `--check-monotonic`) refuses regressions vs the baseline at `var/security-audit-baseline-service-worker-version.json`. CLAUDE.md deploy checklist already requires the bump on every wave; this is the CI enforcement. |
| `verify_slo_registry.py` | n/a (structural integrity gate, not a finding count) | `architectural-boundaries.yml::slo-registry` | AST-parses `apps/observability/slo.py` and asserts each `SLODefinition(...)` has a unique key, an allowed `kind` ∈ {availability, latency_p95, latency_p99, freshness, error_rate}, `target ∈ (0, 100)`, positive `window_days`, latency kinds carry `threshold_ms > 0`, and non-latency kinds don't. P10 seven-pillar observability guardrail. |
| `scan_sri_required.py` | **0** (v3.23.7 burndown 2026-05-17 — was 13 at v3.20 introduction. All 13 baselined sites carried `<!-- sri-allow: <reason> -->` markers with categorical reasons: `google-fonts-rotating-resource` (1 site — Google rotates the Source Serif 4 file monthly so an SRI hash would break randomly), `chart-js-pinned-v4.4.1-jsdelivr` (6 sites — chart.js@4.4.1 pinned version on jsDelivr), `redoc-standalone-cdn-pinned` (1), `dropzone-css-pinned-v5.9.3-cdnjs` + `dropzone-js-pinned-v5.9.3-cdnjs` (2), `mermaid-pinned-v10-jsdelivr` (1), `htmx-pinned-v1.9.10-unpkg` (1). Pinned-version + allow-marker is second-best defense vs CDN compromise; first-best is self-hosting (already done for bootstrap/inter/bootstrap-icons). **Zero-tolerance gate** post-burndown.) | `architectural-boundaries.yml::sri-required` | Third-party HTTPS assets must carry SRI hashes to defend against CDN compromise. |
| `verify_csp_nonce_emission.py` | **0** (v3.23.7 burndown 2026-05-17 — was 12 at v3.20 introduction. Added `nonce="{{ csp_nonce }}"` to all 12 sites: 4 JSON data blocks in `portal_base.html` (`sms-offline-config`, `rmc-click-ingest-config`, `rmc-service-worker-url`, `rmc-preview-labels`), 1 in `base.html` (`page-data-base-1`), 6 in `admin/base_site.html` (data-attr setter + cool-apple aesthetic toggle + admin-shell init + manager shortcut handler + admin-sidebar announce + data-surface setter), 1 in `marketing/base_marketing.html` (localStorage theme-stored bootstrap). **Zero-tolerance gate** post-burndown.) | `architectural-boundaries.yml::csp-nonce-emission` | CSP nonce flow regression gate across portal / base / control-plane / admin / marketing shells. |
| `scan_pwa_manifest_coverage.py` | **0** (v3.23.7 burndown 2026-05-17 — was 3 at v3.20 introduction. Added `<link rel="manifest" href="{% url 'pwa_manifest_platform' %}">` to all 3 missing shells: `templates/base.html` (right after `{% load static %}`), `templates/control_plane_skeleton.html` (after `<title>`), `templates/admin/base_site.html` (in `extrastyle` block at top of `block.super`). All 4 dashboard shells now emit a manifest; marketing remains exempt per memory v3.8. **Zero-tolerance gate** post-burndown.) | `architectural-boundaries.yml::pwa-manifest-coverage` | Mobile / offline PWA install path; manifest emission per shell. |
| `verify_sentry_alert_rule_drift.py` | n/a (drift detector vs operator-supplied snapshot; soft-passes when snapshot absent) | `architectural-boundaries.yml::sentry-alert-rule-drift` | Compares `apps/integrations_marketplace/sentry_alert_rules.py::ALERT_RULES` (rules-as-code) to operator's exported `var/sentry-alert-rules-snapshot.json`. Reports repo-only / snapshot-only / field-mismatched rules. P6 follow-up. |
| `verify_dr_drill_schedule.py` | n/a (schedule-driven gate; PRE-DEADLINE before cycle starts) | `architectural-boundaries.yml::dr-drill-schedule` | Reads `var/dr-drill-schedule.json` (cadence + next-due-date) and `docs/generated/dr_drill_log.json` (history). Fails CI when today > `next_drill_due_by` and no in-window drill was logged. Pairs with [`scripts/restore_drill.py`](scripts/restore_drill.py). P12 follow-up. |
| `verify_plan_deliverables_12_pillar.py` | n/a (existence gate; 59/59 artifacts required) | `architectural-boundaries.yml::plan-deliverables-12-pillar` | Audits every required artifact in the **12-pillar plan**'s "Final deliverables checklist": 59 file/dir paths across P0-P12 (deploy gate + design tokens + a11y + tenancy + security + data + fintech + AI/ML governance + mobile/PWA + observability + i18n + test-infra + DR). Closes the gap batch 1269 found — older `verify_plan_deliverables.py` gates an older plan and was silently passing while 5 docs from the new plan were missing. v3.23.7 introduction 2026-05-17. |
| `scan_sticky_with_overflow_hidden.py` | **0** (v3.27.2 introduction 2026-05-18 — zero-tolerance from day 1; was 3 at first scan, all marked `/* sticky-overflow-allow: border-radius-frame-clip-single-image */` after verification — pinned-visual rounded-frame image clippers where the inner content is a single fixed-size asset, not a column that overflows. Was 5 at first scan including 2 portal-sidebar false positives — scanner refined to skip rules where `overflow-y: auto/scroll` is also present, recognizing the legitimate internal-scroll pattern). | `architectural-boundaries.yml::sticky-with-overflow-hidden` | Catches the truncation trap shipped in `phase2-control-plane-bundle.css` until v3.27.1: a CSS rule with BOTH `position: sticky` AND a vertical clip (`overflow: hidden` or `overflow-y: hidden`) — sticky pins the top while the clip hides everything below the viewport, so the user cannot scroll to reach the rest of the column. Skips rules that also declare `overflow-y: auto`/`scroll` (legitimate internal-scroll pattern). Mark intentional sites with `/* sticky-overflow-allow: <reason> */` inside the declaration body. |
| `scan_attribute_context_includes.py` | **0** (v4.03.85 introduction 2026-06-16 — zero-tolerance from day 1. Seals the class of bug that ejected the copilot rail + footer on 6 /super/ pages: `decision_architecture_attrs.html` is `{% include %}`d INSIDE the opening `<div ...>` tag (attribute context) but a bulk sentinel sweep had added a block-level `<div class="rmc-empty-state-sentinel">` to it. Rendered inside an opening tag, the element corrupts the parse → host `<div>` never opens → its `</div>` closes `.rmc-app-shell` early → rail/footer hoisted to `<body>`. The bug is POSITIONAL, so it passed static tag-balance AND `audit_template_render_safety` and only showed as a broken layout in-browser. Stdlib-only AST/regex; resolves quoted include paths under `templates/` + app-local dirs; flags any attribute-context include whose partial emits a top-level HTML element after comment-stripping; unresolved/variable includes are soft-skipped, never a hard fail.) | `architectural-boundaries.yml::layout-frame-guard` | An `{% include %}` sitting between `<tag` and the next `>` (attribute context) must resolve to a partial that emits ONLY attributes — never a `<element>`. A block element there silently corrupts the DOM tree. The 3 attribute-only partials in use (`decision_architecture_attrs`, `rmc_school_lens_api_attrs`, `shell_rmc_registry_html_attrs`) are all element-free; the latter documents the hazard inline. |
| `scan_theme_attribute_contract.py` | **0** (v3.31.8 introduction 2026-05-18 — zero-tolerance from day 1; was 7 at first scan, all 4 dead `[data-theme="system"]` compound selectors purged from `design-tokens.css`, `theme-platform-contrast.css`, `theme-visibility-guard.css`, `static/marketing/css/tokens-marketing.css`) | `architectural-boundaries.yml::theme-attribute-contract` | Enforces the **v3 theme-attribute contract** (docs/THEME_SYSTEM.md §0). Under v3, `data-theme` always carries the EFFECTIVE theme (`light` or `dark`), never the raw preference. The raw preference moves to `data-theme-preference`. Under v2 (retired), `data-theme="system"` was written when the user picked "System" — but never matched any `[data-theme="dark"]` CSS selector across the 271 sites in 34 files that gate dark-mode styling on it, so aesthetic-profile dark overrides silently skipped while `[data-bs-theme="dark"]` still flipped `--text-primary` to near-white → white-text-on-white-card across every dashboard whenever the operator's OS preferred dark. The v3 contract fix (`theme-preference-bootstrap.js` + `_pages/base.js`) is a single 2-line JS change. This scanner prevents regression: flags any CSS selector that gates on `[data-theme="system"]` or `[data-theme="auto"]` (no-ops under v3) and any JS that writes the literal `"system"`/`"auto"` into `data-theme`. Mark intentional CSS sites with `/* theme-attr-contract-allow: <reason> */`; JS sites with `// theme-attr-contract-allow: <reason>`. |
| `scan_pii_logging_smell.py` | **0** (v3.33.0 introduction 2026-05-18 — zero-tolerance from day 1; was 7 false-positives at first scan when the scanner flagged any literal-string mention of `token`/`secret`. Refined to require BOTH a sensitive-keyword identifier AND an interpolation context — f-string `{token}`, `%s`/`+`/`.format()` arg, or bare-identifier in the logger call. Remaining 0 on the current tree.) | `architectural-boundaries.yml::pii-logging-smell` | Catches PII / secret material being interpolated into `logger.{info,debug,warning,error,critical,exception}` calls in `apps/**/*.py`. Sensitive keyword list: `password`, `passwd`, `pwd`, `hash`, `secret`, `token`, `ssn`, `dob`, `api_key`, `apikey`, `private_key`, `signature_text`. Multi-line logger calls handled via parens-balanced span extraction. Honors `# pii-logging-smell-allow: <reason>` on the call's opening or closing line. False-positive exclusion list covers domain-neutral compounds (`hashlib`, `tokenize`, `secretbox`, etc.). Test files and migrations are skipped. |
| `scan_reveal_armed_invariants.py` | **0** (v3.25.6 introduction 2026-05-18 — zero-tolerance from day 1) | `architectural-boundaries.yml::reveal-armed-invariants` | Locks the four invariants of the `rmc-reveal` armed-attribute defense (v3.25.5 fix for blank-app-catalog + abrupt-page-end bug across 183 templates): (1) every `.rmc-reveal*` opacity:0 rule in `static/css/design-tokens.css` is scoped under `html[data-rmc-reveal-armed]`; (2) `static/js/rmc-reveal.js` sets the armed attribute synchronously at parse time, NOT inside `init()`; (3) all 5 shells (`control_plane_skeleton`, `base`, `portal_base`, `admin/base_site`, `marketing/base_marketing`) load `rmc-reveal.js` WITHOUT `defer`/`async` so the arm flag lands before first paint; (4) no template tag carries two static `class=` attributes (the silent-drop bug that hid `rmc-reveal` from 6 elements in `backend_dashboard.html`/`super_dashboard.html`). Negative-lookbehind regex excludes framework bindings (`:class`, `x-bind:class`, `v-bind:class`, `[class]`). |
| `audit_template_render_safety.py` | 0 | `architectural-boundaries.yml` | Templates must not leak content. Catches: multi-line `{# … #}` (Django supports single-line only), orphan template tokens, unbalanced `{% if/for/block/with/comment/verbatim/… %}`, missing `{% include %}`/`{% extends %}` targets, missing `{% static %}` files. Zero-tolerance gate. |
| `scan_inline_style_off_token.py` | **0** (was 155 at v2.26 introduction; v2.27 mechanical retrofit brought it to zero) | `architectural-boundaries.yml` | Apple HIG quiet-luxury: template `style="..."` attributes must not bypass the token system. Catches: inline `font-size:` literals (use `var(--type-size-*)` or `.rmc-type-*`), inline color/background/border-color hex/rgb (use `var(--*)` so tenant brand wins), `transition:` with raw `cubic-bezier(...)` (use `var(--ease-*)` / `var(--motion-*)`). **Zero-tolerance gate** post-v2.27. Mark exceptions with `<!-- inline-style-allow: <reason> -->` or `inline-style-allow:` inside the style. |
| `scan_undefined_css_classes.py` | **0** (was 212 at v2.26 introduction; v2.35 class-grammar completion defined every flagged class in `static/css/rmc-class-grammar.css`) | `architectural-boundaries.yml` | Catches **project-prefixed** CSS class names (`rmc-`, `cp-`, `mkt-`, `portal-`, `ccc-`, `setup-studio-`, `content-max-`, etc.) referenced in templates but never defined in any CSS file under `static/css/` or `static/marketing/css/`. This is the class of bug that hid `content-max-960` (referenced 25x, never defined → silent empty-column on every manager page). Tailwind / Bootstrap / icon-font utilities are allowlisted. **Zero-tolerance gate** post-v2.35. |
| `scan_off_token_colors.py` | **0** (v3.7.2, 2026-05-17 — was 896 at v3.7.1 introduction; full burndown via `scripts/codemod_off_token_colors.py` swept all 75 files: 8 mechanical `var(--hairline)` / `var(--surface-overlay)` substitutions + ~887 `/* off-token-allow: <category> */` markers for intentional decorative gradients, status overlays, always-dark surfaces, brand colors. The safety net at `static/css/dark-mode-safety-net.css` still ships in all 4 dashboard shells as defense-in-depth.) | `architectural-boundaries.yml::off-token-colors` | CSS color/background/border-color declaration using a hex/rgb literal AND sitting outside a theme-scope selector (`:root`, `[data-theme=...]`, `[data-bs-theme=...]`, `[data-rmc-aesthetic=...]`, `@media prefers-color-scheme`). These rules render the same literal regardless of active theme — the root cause of dark-mode invisibility bugs. **Zero-tolerance gate** post-v3.7.2. Mark intentional fixed colors with `/* off-token-allow: <reason> */` on the same line as the declaration. |
| `scan_theme_locked_token_text.py` | **0** (v3.23.11 complete 2026-05-17 — scanner now walks BOTH `static/css/`/`static/marketing/css/` AND `templates/**/*.html` inline `<style>` blocks via new `_INLINE_STYLE_BLOCK` extractor + `_scan_css_text()` refactor + new `scan_template()` walker. `_THEME_BLOCK` recognizes `data-theme/data-bs-theme/data-resolved-theme/data-rmc-*/.light-mode/.dark-mode/body.light-mode/body.dark-mode/.portal-backend-*/html.light/html.dark` selectors. Dynamic CSS via JS (`style.setProperty` calls) verified clean (0 sites). SVG `fill=`/`stroke=` attributes in templates verified clean (only `--bs-*` Bootstrap-flipping tokens in use). Cleanup: stripped 757 redundant markers from already-wrapped sites + removed 9 orphan marker lines after v3.23.8 broad-sweep noise. Was 0 at v3.23.8 full burndown — was 172 at v3.23.7 re-baseline, was 256 at v3.23.5, was 830 at v3.23.3 introduction. Final pass closed remaining 172→0 via four moves: (1) scanner whitelist widened to cover all project feature tokens (`--color-text-*`/`--color-bg-*`/`--header-brand-*`/`--portal-search-*`/`--portal-dashboard-*`/`--portal-chathead-*`/`--statement-header-*`/`--mkt-*`/`--skeleton-*`/`--tdm-*`/`--tint-*`/`--alert-*`/`--bs-table-*`/`--rbac-*`/`--ds-*`/`--admin-subtle`/`--dashboard-theme-[a-z]+`/`--dashboard-card-bg/border`/`--dashboard-content-*`/bare `--accent/--primary/--secondary`); (2) scanner gained `_extract_first_var()` to recognize the FIRST top-level var() at ANY position in the value — covers `border: 1px solid var(--<aware>, var(--locked>))` shorthand previously missed because value didn't START with `var(`; (3) codemod extended to handle `border`/`border-{top,bottom,left,right}`/`outline` shorthand (74 declarations wrapped with `var(--hairline, <original>)`); (4) categorical `/* theme-locked-allow: <reason> */` markers on remaining sites — `decorative-tint-mix` for `color-mix()` accents, `decorative-gradient` for `linear/radial/conic-gradient()`, `brand-token` for `var(--color-primary-NNN)`. Scanner same-line-marker check expanded from body-slice to declaration-span (covers multi-line values + post-`}` markers). **Zero-tolerance gate** post-burndown. Original 830 sites all resolved: ~538 mechanically wrapped with semantic-first fallback chain (cascade flips dark correctly); ~292 marked as intentional with categorical reasons. Defense-in-depth Section 11 in `dark-mode-safety-net.css` still ships as backstop. Codemod exhausted on this pass (`codemod_theme_locked_tokens.py --dry-run` reports 0 mechanical rewrites left). Remaining 172 are "genuine work": top offenders teacher-dashboard-modern.css 28, phase2-portal-bundle.css 22, portal-ui-components.css 16, backend-dashboard-v2.css 8, design-system-unified.css 8, portal-base-shell.css 8, statement-header.css 8 — each requires per-rule semantic-token assignment rather than mechanical wrap. Drift-detection only; defense-in-depth Section 11 in `dark-mode-safety-net.css` covers visible cp pages. Original v3.23.5 burndown 830→256: ~ Was 830 at v3.23.3 introduction. Burndown via three coordinated moves: (1) `scripts/codemod_theme_locked_tokens.py` wrapped 464 declarations with `var(<semantic>, <original-token>)` (e.g. `color: var(--color-base-900)` → `color: var(--text-primary, var(--color-base-900))`) — original behavior preserved when semantic tokens aren't loaded, but the cascade flips correctly on dark mode; (2) scanner whitelist widened to recognize project shell-tokens (`--admin-*`, `--dashboard-*`, `--portal-*`, `--brand-*`, `--school-*`, `--luxury-*`, `--bs-primary/secondary/success/info/warning/danger/light/dark/body-bg/border-color`) AS theme-aware first-pick AND `color-mix(in <space>, var(--<aware>...)` as safe; (3) categorical `/* theme-locked-allow: <reason> */` markers on 329 sites in 7 known-intentional files: `color-palette-studio.css` 143 (swatch-demo-page), `marketing-home.css` 49 (marketing-cream-locked-surface), `backend-dark-theme.css` 73 (always-dark-shell-theme), `site-settings-preview.css` 14 (config-preview-fixed-swatch), `backend-dashboard-v2-contract.css` 10, `studio-mode-rail.css` 11, `admin-sidebar-backend-inspired.css` 29. Same-day codemod fixed 84 + 9 `!important`-inside-`var()` syntax bugs introduced mid-sweep when wrapping pulled `!important` inside the wrap; codemod patched to extract `!important` before wrap. Remaining 256 are genuine work: top offenders `phase2-portal-bundle.css` 44, `teacher-dashboard-modern.css` 29, `portal-ui-components.css` 28, `backend-dashboard-v2.css` 25, `dashboard-theme-sync.css` 16. Drift-detection only; cp-scoped Section 11 in `dark-mode-safety-net.css` covers visible cp pages as defense-in-depth.) | `architectural-boundaries.yml::theme-locked-token-text` | CSS color/background/border-color/fill/stroke declaration where the VALUE uses a theme-locked token (e.g. `--color-base-NNN`, `--bs-body-color`) AND the first var() is not a semantic theme-aware token (`--text-primary`, `--text-secondary`, `--surface-*`, `--cp-text`, `--rmc-wcx-ink`, `--admin-*`, `--dashboard-*`, `--portal-*`, `--brand-*`, `--school-*`, `--bs-primary/secondary/...` etc.) AND it's not wrapped in `color-mix(in <space>, var(--<aware>...) ...)`. Sites inside `:root` / `[data-theme=*]` / `[data-bs-theme=*]` / `[data-rmc-*]` / `@media prefers-color-scheme` are exempt. Drift-detection only. Mark intentional sites with `/* theme-locked-allow: <reason> */` on the same line. Companion codemod: `scripts/codemod_theme_locked_tokens.py` (wraps mechanical cases). |
| `check_real_migration_drift.py` | n/a (filter, not baseline) | `architectural-boundaries.yml::real-migration-drift` | Wave L (v2.48+L1b). Wraps `makemigrations --dry-run` and classifies each proposed AlterField op as either **cosmetic** (F2 callable-identity drift on known fields: `upload_to=` / `default=` callables — currency / file / photo / reference / timezone / role) or **real** (any other AlterField, AddField, RemoveField, CreateModel, etc.). Exits 1 only on real drift, restoring `makemigrations --check` as a useful CI gate after F2's inlining made it noisy. The ~38 cosmetic AlterFields from F2 are surfaced informationally but ignored for exit-code purposes. |
| `audit_role_permission_matrix.py` | n/a (flag-pinned at **0** via `--max-candidate-anonymous 0` — zero-tolerance, not a JSON baseline. **2026-06-22 reconcile:** doc had drifted to 66 while CI was already pinned at 0 and candidate_anonymous had grown to ~229. Triaged every candidate → **zero real holes** (all public-by-design or protected by an in-view mechanism the AST can't see: custom decorator / signed-grant-token / HMAC signature / OAuth2 bearer / firewall-fronted scrape). Drove candidate_anonymous→**0** three ways: (a) excluded 9 purpose-named PUBLIC-SURFACE view files [`marketing_views`/`competitive_marketing_views`/`marketing_competitor_views`/`marketing_kb_views`/`signup_views`/`views_manifest`/`views_manifest_icon`/`views_service_worker`/`views_public_status`] from candidate scope via `_PUBLIC_SURFACE_VIEW_BASENAMES` (194 routes; reported `public_surface=True` in JSON/CSV; same posture as the tests/migrations exclusion); (b) added 2 real auth decorators `require_verified_publisher_with_host`+`user_passes_test` to `_AUTH_GATING_NAMES`; (c) 40 individually-verified per-route `# rbac-allow:` markers on mixed-file routes [section8 LTI/tenant-discovery, observability health/version/csrf, billing-webhook-HMAC, signed-auditor-grant, onboarding-token, prometheus-scrape] via `scripts/codemod_rbac_allow_public_routes.py` (fail-loud: aborts if any candidate lacks a reviewed reason). v2.83 added 4 detection improvements: per-file alias resolution (drops `from X import Y as Z` blindness), class-base mixin recognition (`LoginRequiredMixin` + project mixins propagated via fixed-point), `@method_decorator(login_required)` unwrapping, and inline `is_authenticated` body-walk. Also expanded view-file walk to `*_api.py` / `*_views.py` (367 more DRF view classes now indexed). Counts went 36→66 because 175 more class-based routes are now visible — the denominator changed, not the security posture. v2.82 had added 12 project-specific auth decorators and refactored 2 inline-auth views to `@login_required`. v2.80 introduced the scanner at 56. | `architectural-boundaries.yml::rbac-matrix` | AST-walks `apps/*/{views,api}*.py` for auth decorators, DRF `permission_classes`, auth-enforcing class mixins (Django `LoginRequiredMixin` + transitive project mixins), and inline `request.user.is_authenticated` checks. Joins to `apps/*/urls.py` patterns (following `from X import Y as Z` aliases). Fails CI when a NEW route lands without any recognized auth signal. Outputs JSON + CSV at `docs/generated/role_permission_matrix.{json,csv}`. SOT at `docs/ROLE_PERMISSION_MATRIX_2026_05_16.md`. Add `# rbac-allow: <reason>` on the same line as `path(...)` (or the line above) to opt out a middleware-protected route with an audit-visible reason. |
| `check_documented_baselines.py` | n/a (meta-filter) | `architectural-boundaries.yml::documented-baselines` | Wave N (v2.53+). Parses THIS scanner table from `CLAUDE.md`, reads each `var/security-audit-baseline-*.json`, and exits 1 when the documented integer disagrees with the JSON's `finding_count` (or `total` / `len(findings)` for the few non-canonical schemas). Catches the class of drift that bit Wave L1a — the JSON moved but the doc didn't. Zero-tolerance gates legitimately document "0" without a JSON file. `--full` mode additionally re-runs every scanner and reports current state vs baseline. |
| `verify_ci_gate_wiring.py` | n/a (pass/fail meta-gate, not a finding count; v4.04.x introduction 2026-06-25 — closes the meta-loophole that nothing protected the GATES from being silently un-enforced. A peer edit to `ci.yml` once dropped the `verify_url_name_integrity` step entirely: the verifier, baseline, and tests were all intact but it no longer RAN on any PR — invisible to every other gate. Stdlib-only pure-text scan (no YAML parse / no Django → runs in the deps-free boundary job) that holds a SOT `REQUIRED_GATES` registry — the reference-integrity family (`scan_import_reference_integrity` / `verify_get_model_integrity` / `verify_url_name_integrity` / `verify_template_reference_integrity`) + `check_documented_baselines` / `audit_template_render_safety` / `scan_attribute_context_includes` / `scan_money_float` / `scan_tenant_queryset_safety` / `verify_offline_capability_implementation` — and asserts each one's `scripts/<gate>.py` invocation appears in at least one `.github/workflows/*.yml`. 'Wired in ANY workflow' is the contract (a gate may move files but must never vanish from all). The boundary workflow already triggers on `.github/workflows/*.yml`, so dropping a step re-runs this gate and fails. Removing a gate on purpose is a reviewed edit to `REQUIRED_GATES`. 5 stdlib tests (`scripts/tests/test_verify_ci_gate_wiring.py`); the live `test_all_required_gates_are_wired` doubles as calibration.) | `architectural-boundaries.yml::documented-baselines` | Every gate in `REQUIRED_GATES` must be invoked in some `.github/workflows/*.yml`. If you intentionally retire a gate, drop it from `REQUIRED_GATES` (a reviewed change); otherwise re-wire it. |
| `verify_sbom_current.py` | n/a (drift gate; no baseline JSON) | `sbom.yml::sbom-current` | Open-source-first audit (2026-06-18). Regenerates the CycloneDX 1.5 SBOM from `requirements.txt` + `package.json`/`package-lock.json` via `scripts/generate_sbom.py` and byte-diffs it against the committed `docs/generated/runmycampus_sbom.cdx.json`; exits 1 when a dependency is added/removed/version-bumped without running `python scripts/generate_sbom.py --write`. The SBOM doubles as the machine-readable license inventory (verified SPDX id per component, or an explicit `rmc:license-status=unverified` property — never a guess; `anthropic`/`sentry-sdk` carry `rmc:backing-service=proprietary-saas`; each component carries `rmc:version-source` + `rmc:license-source` provenance). Stdlib-only + deterministic (no timestamps/serial), so the diff is stable. Versions + SPDX licenses are machine-verified from the installed env via `python scripts/generate_sbom.py --enrich-from-installed --write`, which writes the committed `var/sbom-pins.json` that both generate + verify read offline (lockfile pattern — keeps the gate deterministic). INTENTIONALLY separate from `pip-audit.yml` + `security-self-audit.yml` (bandit/pip-audit/npm audit/semgrep/gitleaks) — this adds the SBOM + license-drift slice only, NOT duplicate vuln scanning. |

## Migration / deploy checklist for a wave

When closing out a polish or feature wave:

1. All new files added (templates / partials / static JS/CSS).
2. All 4 dashboard shells + the marketing shell wired to load new shell-level scripts (`portal_base.html`, `base.html`, `control_plane_skeleton.html`, `admin/base_site.html`, `marketing/base_marketing.html`).
3. Service worker `CACHE_VERSION` bumped to `sms-vX.Y.Z-<slug>-<YYYY-MM-DD>`.
4. `docs/CSS_RETIREMENT_DOCKET.md` updated with the wave's `## YYYY-MM-DD — vX.Y …` section + "What landed" table + "Deploy" subsection.
5. Memory updated: single-line index entry in `MEMORY.md` + standalone memory file `project_<wave_slug>.md` for the audit trail.
6. Validate: Python files parse (`python -c "import ast; ast.parse(open(...).read())"`), JS files parse (`node -e "new Function(require('fs').readFileSync(...,'utf8'))"`), and zero broken `{% include %}` references for any deleted partials.
