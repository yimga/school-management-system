# Generated by Django 5.2.10
# Idempotent: add target_roles to KBArticle only if missing (fixes seed_kb_articles NOT NULL on Render).

from django.db import migrations, models


def add_target_roles_if_missing(apps, schema_editor):
    """Add target_roles to portal_kbarticle; no-op if column already exists."""
    from django.db import connection

    with connection.cursor() as cursor:
        if connection.vendor == "postgresql":
            cursor.execute("""
                DO $$
                BEGIN
                    ALTER TABLE portal_kbarticle
                    ADD COLUMN target_roles JSONB NOT NULL DEFAULT '[]'::jsonb;
                EXCEPTION WHEN duplicate_column THEN NULL;
                END $$;
            """)
        else:
            cursor.execute("PRAGMA table_info(portal_kbarticle)")
            cols = [row[1] for row in cursor.fetchall()]
            if "target_roles" not in cols:
                cursor.execute(
                    "ALTER TABLE portal_kbarticle ADD COLUMN target_roles TEXT NOT NULL DEFAULT '[]'"
                )


def noop_reverse(apps, schema_editor):
    pass


class Migration(migrations.Migration):

    dependencies = [
        ("portal", "0024_kbcategory_target_roles"),
    ]

    operations = [
        migrations.SeparateDatabaseAndState(
            state_operations=[
                migrations.AddField(
                    model_name="kbarticle",
                    name="target_roles",
                    field=models.JSONField(
                        blank=True,
                        default=list,
                        help_text="List of role codes that can see this article (empty = all).",
                        verbose_name="Target roles",
                    ),
                ),
            ],
            database_operations=[
                migrations.RunPython(add_target_roles_if_missing, noop_reverse),
            ],
        ),
    ]
