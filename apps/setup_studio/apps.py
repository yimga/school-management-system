from django.apps import AppConfig


class SetupStudioConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.setup_studio"
    verbose_name = "Setup Studio (guided onboarding)"
