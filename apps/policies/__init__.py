# Policy Registry: single entry point for "how should this tenant behave?" (Tenant Blueprint + Policy).
# All nine resolvers (Section 4.6): import from apps.policies.resolvers (e.g. TerminologyResolver, ChannelResolver).
