# Event Outbox: domain events for notifications, automation, and integrations (RunMyCampus blueprint).
# Public API: ``apps.events.services.emit_event`` / ``apps.events.bus.publish``;
# in-process handlers: ``apps.events.bus.subscribe``; webhooks: ``apps.events.models.WebhookSubscription``.
