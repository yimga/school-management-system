from django.apps import AppConfig


class StudioOsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.studio_os"
    verbose_name = "Studio OS (unified workspace)"
