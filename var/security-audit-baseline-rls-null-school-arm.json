{
 "finding_count": 0,
 "generated_at": "2026-09-03",
 "note": "RATCHETED TO ZERO 2026-09-03: all 40 armless policies were replaced by the per-app *_rls_force_and_null_arm_postgresql migrations (canonical NULL-arm form; NULL = platform/pre-tenant scope, the same reviewed outcome as metadata/0012, siteconfig/0129 and people/0076). Original context: tables whose `school` FK is NULLABLE but whose RLS USING/WITH CHECK clause omits the `school_id IS NULL` arm -- for such a row NULL::text = '<uuid>' is NULL, so USING is false and the platform-level row is INVISIBLE; the same clause as WITH CHECK rejects the INSERT with 42501. RLS is a no-op under USE_DJANGO_TENANTS=1 (the deployed cloud topology); this is RLS-MODE readiness for the sovereign edge. May only shrink -- it is now zero and any new armless policy on a nullable school FK must fail --compare."
}