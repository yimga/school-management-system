# Mercado Pago — Lane 2 evidence (SFDP 1443)

Repo-complete: `apps/finance/gateways/mercado_pago.py`, normalizer.

Operator: OAuth access token in Integration; webhook secret; supervised charge; flip `mercado_pago_latam` to **verified_live**.
