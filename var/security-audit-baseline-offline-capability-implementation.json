{
  "detection_mode": "structural (js tokeniser + python ast) since 2026-07-21",
  "finding_count": 0,
  "generated_at": "2026-07-21",
  "note": "Every OFFLINE_QUEUED_WRITE capability declared in the offline manifest taxonomy has a real client producer AND server applier on the SODP or WAL rail. Latent capabilities (producer+applier exist but no UI surface yet) are reported non-fatally and are NOT findings. Zero-tolerance from introduction. 2026-06-25: enable_offline_homework_sync flipped latent->live by the canonical LMS homework loop (templates/portal/student_assignment_detail.html carries data-rmc-offline-form=homework_submission; offline_queue._apply_lms_submission writes academics.LMSSubmission). 2026-07-21 (globalization program 1.2): finding_count was 0 under a detection method that could not fail -- the gate concatenated every static/js file into one blob and re-searched it, so a producer mentioned only in a // comment satisfied it, and server appliers matched a \\bNAME\\b text search over offline_queue.py that a # comment or docstring satisfied. Verified by replay: on a synthetic tree where enable_offline_fee_payment_sync existed ONLY as a comment plus an unrelated string literal, the pre-fix gate printed OFFLINE_CAPABILITY_IMPLEMENTATION_PASS. Detection is now structural: producers must be real rmcOfflineEnqueue({action_type: '...'}) / rmcWAL.append('<domain>', ...) call sites in a tokenised source (comments dropped, strings atomic), and SODP appliers must be dispatched by _apply_payload's real AST control flow onto a member that exists in a real enum. finding_count stayed 0 -- the live tree is genuinely honest; enable_offline_fee_payment_sync in particular has a real producer (static/js/rmc-offline-portal-forms.js wirePaymentReceipt), a real applier (offline_queue._apply_payment_receipt) and a real UI surface (templates/finance/invoice_detail.html). Must-fire negative controls: scripts/tests/test_verify_offline_capability_implementation.py::CommentOnlyMustFireTests. Follow-up: a RUNTIME proof (that a queued action actually round-trips) belongs in the unused Playwright harness playwright.offline-indexeddb.config.js / npm run test:e2e:offline-multiday; structural detection proves wiring, not round-trip.",
  "latent_known": []
}
